# CODIVIO — CHANGELOG

Chronological, checkpoint-based project history. Where a real Git commit exists, its short hash is given — **no hash is invented for uncommitted work.** Most Phase 2 work in this log is still uncommitted in the working tree (expected; see `PROJECT_STATE.md` → Git status expectations).

## Phase 0 — Foundation

- `c3c1594`, `21ee301`, `97ab1fb` — Initial Codivio project foundation (React/TypeScript/Vite scaffold, repo history merge).
- `d033834` — React root mounting completed.
- `3a912d4` — Cloudflare Workers foundation added (dependencies, `vite.config.ts`, initial `wrangler.jsonc` — Assets-only, no Worker script yet, no D1).
- `6baac88` — Cloudflare SPA asset routing aligned.
- `84e0425` — Codivio branding/metadata normalized.
- **Result:** clean repo, working build, base Cloudflare Assets deployment plumbing. **Test status:** build passing.

## Phase 1 — Public Website MVP

- `5a3b18e` — Public navigation routes added.
- `cff7d57` — Homepage hero section.
- `4b9540c` — Tool slugs + coming-soon status.
- `386f010` — MVP tool registry expanded.
- `b1f3f8d`, `46b7c71`, `890700e` — Shared tool-page foundation, routing, responsive styling.
- `c6e7446` — App wrapped with `BrowserRouter`.
- **Uncommitted (this session), building on the above:**
  - Full Phase 1 UI/CSS fix pass: missing CSS classes filled in (verified against real JSX, not copied blindly from a WIP reference file), `.content-section` grid bug fixed, footer link contrast fixed for WCAG AA, mobile tap-target sizing fixed, `aria-pressed`/`aria-expanded` added.
  - Homepage 12-tool selection: `featured` flag introduced on the 34-tool registry, `HOMEPAGE_FEATURED_TOOLS` derived view, redundant per-category preview sections removed from Home.
  - Popular Tools slider, QR Tools slider, PDF Tools slider, Blog slider — each an independently-flagged (`popular`/`qr`/`pdf`) horizontal, touch/swipe, scroll-snap slider reusing the same established track/nav pattern (see `codivio-ui` skill).
  - Basic SEO: `usePageMeta` hook sets `document.title` + meta description per route.
  - 404 handling: `NotFoundPage` replaces silent redirects for unknown routes and invalid tool slugs.
  - **Test status:** `npm run typecheck` / `npm run build` passing at each step; registry count verified at 34 throughout; no regressions found via build-output hash comparison.

## Phase 2.1 — Admin Architecture Audit

- Read-only inspection and plan (no code changes). Produced the Phase 2 checkpoint sequence (2.1–2.11) now tracked in `PROJECT_STATE.md` and the `codivio-admin` skill.
- **Test status:** N/A (audit only).

## Phase 2.2 — Worker + D1 Foundation

- **Uncommitted.** Added `worker/index.ts` (minimal Worker: serves the SPA via `env.ASSETS`, real `/api/health` querying `env.DB`), `migrations/0001_init_schema.sql` (mirrors `database/schema.sql`), and wired `main`/`assets.binding`/`d1_databases` into `wrangler.jsonc`.
- **Test status:** `npm run build` passing (both Worker and client bundles). `wrangler dev` resolved bindings correctly in local mode, then the local `workerd` runtime crashed (access violation) on this Windows machine — an environment limitation, not a code/config defect. Live `/api/health` could not be verified locally as a result. Public SPA regression confirmed via identical build-output file hashes before/after.

## Asset Normalization

- **Uncommitted.** `public/Assets/branding/codivio-logo.png.png` → `public/assets/branding/codivio-logo.png`; `Reference/homepage-reference.png.png` → `reference/homepage-reference.png`. Case-only directory renames done via a two-step swap (required on Windows' case-insensitive filesystem). Image content verified byte-identical before/after.
- **Test status:** `npm run build` passing; confirmed the previous `Assets`/`assets` case-collision in `dist/client/` no longer occurs.

## Local Git Quality Gates

- **Uncommitted.** `.githooks/pre-commit` + `pre-push`, `scripts/{postinstall,pre-commit,pre-push}-check.mjs`, `core.hooksPath` configured, `postinstall` script added to `package.json`.
- **Test status:** each hook script run directly and confirmed to PASS/FAIL correctly; verified they do not modify source or run network calls.

## GitHub Connection Verification

- Read-only check. Confirmed `origin` → `github.com/azbloknot-png/Codivio.git`, `main` tracks `origin/main`, remote reachable. No changes made.

## Security Review (Taste Skill install request)

- Read-only review of the external `Leonxlnx/taste-skill` GitHub repository before allowing any installation. Declined to execute the third-party `npx skills add` CLI; instead manually copied the one requested, reviewed-safe `redesign-skill/SKILL.md` into `.claude/skills/redesign-skill/`.
- **Test status:** N/A (review only; no code execution).

## Skill System Creation

- **Uncommitted.** Audited 12 candidate project-skill ideas, created 4: `codivio-test-gate`, `codivio-database`, `codivio-admin`, `codivio-ui` (each cross-checked for zero duplication with `CLAUDE.md` or each other). `codivio-memory` deliberately deferred to this checkpoint.
- **Test status:** file existence, frontmatter validity, and cross-skill duplication all checked manually; no application tests applicable.

## Phase 2.3 — Authentication + Sessions

- **Uncommitted.** Added `worker/auth.ts` (login/logout/session/bootstrap endpoints, PBKDF2 password hashing, hashed session tokens, email-keyed failed-login lockout), `worker/types.ts` (shared D1/Env types, factored out of `worker/index.ts`), `migrations/0002_auth_sessions.sql` (`sessions` + `login_attempts` tables, `users.updated_at`/`last_login_at` columns). `worker/index.ts` extended with routing to the new endpoints; its existing `/api/health` logic untouched.
- Added `vitest.config.ts` (kept separate from `vite.config.ts` — see `DECISIONS.md`) after discovering `npm test` had started silently trying to boot the local `workerd` runtime and crashing, once Checkpoint 2.2 added `main` to `wrangler.jsonc`. This was a real, newly-surfaced regression in the test setup, not a Phase 2.3 code defect — fixed before any auth tests were written against it.
- Added `tests/auth.test.ts`: 19 real, executed tests covering password hashing/verification, session token generation/hashing, bootstrap (including the "already bootstrapped" refusal), login success/failure/lockout/inactive-user rejection, session validation/expiry, logout invalidation, and a static SQL-parameterization check. All run against a lightweight in-memory D1 mock, itself cross-checked query-by-query against a real SQLite engine (`node:sqlite`) — which caught and fixed one real bug (SQLite's `CURRENT_TIMESTAMP` format not string-comparing correctly against JS `toISOString()`, which would have silently disabled the login-lockout feature).
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 20/20 (1 pre-existing + 19 new). `npm run build` — PASS, public SPA output byte-identical to before (regression check via file hash). Live Worker/D1 integration (`wrangler dev` → real HTTP round-trip) remains unverified — same pre-existing local `workerd` limitation as Checkpoint 2.2, re-confirmed rather than re-diagnosed from scratch.

## Phase 2.4 — Admin UI + Protected Route

- **Uncommitted.** Added `src/admin/AdminApp.tsx`: `AdminLoginPage` (`/admin/login`, calls the real `POST /api/auth/login`, no frontend-only auth, no password/token storage), `useSession`/`resolveSessionState` (server-session-backed auth state via `GET /api/auth/session`), `ProtectedAdminRoute` (`/admin` — loading/unauthenticated/authenticated states, redirects unauthenticated visitors to `/admin/login` with no redirect loop), and `AdminShell` (header with user email + logout, responsive sidebar with 7 nav placeholders — only Dashboard live, the rest marked "Soon" and non-clickable, no dead links, no fake statistics).
- `src/App.tsx`: exported the existing `usePageMeta` hook for reuse, added the `/admin/login` and `/admin` routes without touching any existing route.
- `src/styles.css`: new Admin-scoped CSS reusing existing tokens/patterns (`.brand`, `.contact-form`, `.primary-button`, `.empty-state`) per `codivio-ui`; mobile sidebar becomes a drawer at ≤900px with a 44px+ touch-target toggle; explicit `:focus-visible` styles added for the new interactive elements.
- `worker/auth.ts`/`worker/index.ts` **not modified** — Phase 2.4 only consumes the existing Phase 2.3 API.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 32/32 (1 foundation + 19 auth + 12 new admin-UI). The 12 new tests combine real unit tests of extracted pure logic (`describeLoginError`, `resolveSessionState`) with structural/static source checks for redirect/routing behavior — no `@testing-library/react`/jsdom is installed, and adding one was out of scope; this distinction is documented in the test file itself. `npm run build` — PASS (Worker bundle unchanged at 10.31kB, confirming zero backend impact; client CSS/JS grew as expected for the new UI).

## Phase 2.5 — RBAC

- **Uncommitted.** Added `shared/rbac.ts` (centralized role/permission matrix: 4 roles, 12 permissions, `hasPermission`/`getPermissions`/`isValidRole`/`displayName`, all fail-closed for unrecognized roles). Added `worker/rbac.ts` (`authorize()` — the reusable authenticate→authorize(permission) pipeline for future protected endpoints, correctly returning 401 vs 403). Extended `worker/auth.ts`: factored out `resolveAuthenticatedUser` from `handleSession`'s inline logic (now reusable), exported `jsonError`, applied `hasPermission(..., "admin.access")` to `GET /api/auth/session`. Extended `src/admin/AdminApp.tsx`: role badge now uses `shared/rbac.ts#displayName`; sidebar nav items are filtered per-role via the same shared matrix (a role without the permission doesn't see the item; a role with it but no built feature sees "Soon").
- Reviewed the user-specified initial permission matrix for security/architectural problems before implementing — found none; implemented as given (see `DECISIONS.md`).
- Deliberately did **not** add Pages/Tools/Users CRUD endpoints just to exercise `authorize()` — none exist yet (Phase 2.9/2.10+); the only current real integration point is `GET /api/auth/session`. Documented as a limitation, not worked around with fake endpoints.
- Extracted the shared `FakeD1` test mock from `tests/auth.test.ts` into `tests/helpers/fake-d1.ts` so `tests/rbac.test.ts` doesn't duplicate it.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 49/49 (32 pre-existing + 17 new RBAC tests: permission-matrix correctness including fail-closed behavior, `authorize()` 401-vs-403 distinction, role-spoofing resistance via a request with a forged body/header, expired-session-is-401-not-403, error responses not leaking role/permission details, and a structural check that `ProtectedAdminRoute` never uses a client-side permission function to decide access). `npm run build` — PASS (Worker bundle 10.31→13.80kB; client CSS unchanged, JS +~1kB).

## Phase 2.6 — Audit Logging

- **Uncommitted.** Added `worker/audit.ts` (`auditLog(env, event)` — closed action vocabulary today: `AUTH_LOGIN_SUCCESS`/`AUTH_LOGIN_FAILURE`/`AUTH_LOGOUT`/`AUTH_BOOTSTRAP`/`AUTH_SESSION_EXPIRED`/`AUTHZ_DENIED`, controlled `result` vocabulary, fail-open write policy). Added `migrations/0003_audit_logs.sql` — extends the existing (Phase 0) `audit_logs` table with `actor_email`/`result`/`ip_address`/`user_agent` and 5 indexes (created_at, user_id, action, entity_type, result); no new table needed.
- Wired `auditLog` into every real current event: `handleLogin` (success, wrong-password, inactive-user, rate-limited), `handleLogout` (only when a real session existed), `handleBootstrap` (success, already-bootstrapped denial), `resolveAuthenticatedUser`'s session-expiry branch, and `worker/rbac.ts#authorize()`'s 403 path (plus `handleSession`'s own inline 403 check, for consistency). Plain 401s are deliberately not logged.
- **Discovered and fixed a real drift bug**: `database/schema.sql` had not been updated when migration `0002` (Phase 2.3, sessions/login_attempts/users columns) was added — it still only reflected migration `0001`. Brought back in sync with both `0002` and `0003` this checkpoint; re-verified to apply standalone against a real SQLite engine and produce the exact same schema as applying all three migrations in sequence.
- Extended `tests/helpers/fake-d1.ts` to handle `INSERT INTO audit_logs`, so every existing auth/RBAC test (which now triggers audit calls) kept passing without modification.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 64/64 (49 pre-existing + 15 new: audit-record creation with real server-derived actors, null-actor safety for unauthenticated events, actor-spoofing resistance via forged headers/body, zero secrets/passwords/tokens ever written across a full login+logout+denial cycle, `AUTHZ_DENIED`/`AUTH_BOOTSTRAP`-denial logging, parameterized-SQL static check, fail-open behavior under a simulated D1 write failure, and a real `node:sqlite` migration-structure test now part of the permanent suite). `npm run build` — PASS (Worker bundle 13.80→16.77kB; client CSS/JS byte-identical to Phase 2.5 — confirms this checkpoint made zero frontend change, as scoped).
- No Admin Audit Log UI or read endpoint was built — judged not "genuinely necessary" for verification, since unit/structural/real-SQLite tests fully cover this checkpoint's scope without one.

## Phase 2.7 — Settings Foundation

- **Uncommitted.** Added `shared/settings.ts` (centralized, fail-closed registry: 9 real setting keys across `general`/`system`/`google`/`advertising`/`affiliate`/`security`, 18 category names and 5 value types reserved for future growth; `validateSettingValue`/`parseSettingValue` for type/length/range validation — never `eval`, JSON only ever `JSON.parse`/`stringify`d). Added `migrations/0004_settings.sql` (new `settings` table, 2 indexes, 9 seed rows matching the registry exactly). Added `worker/settings.ts` (`GET /api/settings/public` — no auth, public rows only; `GET /api/admin/settings` — `settings.view`; `PATCH /api/admin/settings` — `settings.manage`, one key at a time, audited on both success and validation failure). Extended `worker/audit.ts` with the `SETTING_UPDATED` action.
- Refactored `src/admin/AdminApp.tsx`'s `/admin` route to nested routing (`Outlet`/`useOutletContext` — a `useAdminUser()` hook threads the already-verified session user to child pages) so `/admin` (Dashboard) and `/admin/settings` (new) are siblings instead of one hardcoded view — the extension point left as a comment in Phase 2.4.
- Added `src/admin/AdminSettingsPage.tsx`: General/Branding/Integrations/Security/System groups, all real data from `GET /api/admin/settings`; Branding shown as an honest "Coming soon" (zero registered settings there); Google/AdSense/Affiliate shown as read-only "Not configured" status badges (never editable, even for `settings.manage` — see `DECISIONS.md`, no real integration flow exists yet to legitimately flip them); General/Security/System settings are genuinely editable for a role with `settings.manage`, read-only text otherwise (`admin` role has `settings.view` but not `settings.manage`, so it now correctly sees a read-only Settings page).
- **Discovered and fixed test-writing mistakes while building this** (not production bugs): the Phase 2.4 structural test asserting the exact old `/admin` route JSX needed updating for the new nested-route structure; a new test helper (`patchSettings`) had to replace several call sites that were awaiting a raw `Request` object instead of invoking the handler; `seedDefaultSettings()` initially only seeded 6 of the 9 real rows. All caught by running the suite, none shipped.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 81/81 (64 pre-existing + 17 new: real-SQLite migration validation, authorized/unauthorized/unauthenticated read, authorized/unauthorized write, unknown-key and wrong-type/oversized-value rejection, public/private exposure boundary, audit logging on success and failure, actor-spoofing resistance, parameterized-SQL static check, and an honest "no JSON setting registered yet" check rather than a fabricated one). `npm run build` — PASS (Worker bundle 16.77→27.33kB; client CSS 19.75→21.90kB, JS 310.51→315.76kB for the new Settings page).

## Project Memory System

- Created `PROJECT_STATE.md`, `DECISIONS.md`, this `CHANGELOG.md`, and `.claude/skills/codivio-memory/SKILL.md`; added a minimal integration section to `CLAUDE.md` (§24).
- **Test status:** documentation-only; verified via file-existence and content-consistency checks, not the application test suite.

## Phase 2.9 — Pages Management

- **Uncommitted.** Extended the existing (Phase 0) `pages` table in place via `migrations/0005_pages_management.sql` — `description`/`meta_title`/`meta_description`/`canonical_url`/`is_indexable`/`created_by`/`updated_by` columns, the latter two FK'd to `users(id)` (empirically verified enforced via `node:sqlite`, including `ALTER TABLE ADD COLUMN ... REFERENCES`). The existing, still-unused `seo_settings` table was deliberately left alone (see `DECISIONS.md`).
- Added `shared/pages.ts`: centralized fail-closed validation (slug format + `RESERVED_PAGE_SLUGS` allowlist, per-field length limits, `draft`/`published`/`archived` status enum, canonical-URL scheme check rejecting `javascript:`/etc.) — asymmetric by design: `title`/`description`/`metaTitle`/`metaDescription` reject raw `<`/`>`, `content` does not (its safety boundary is the render side, not input filtering).
- Added `worker/pages.ts`: `GET/POST /api/admin/pages` and `GET/PATCH/DELETE /api/admin/pages/:id` (via `authorize()` — `pages.view`/`pages.manage`, both pre-existing in the Phase 2.5 matrix), plus unauthenticated `GET /api/pages/:slug` (`status='published'` enforced inside the SQL `WHERE`, never post-filtered; draft/archived/missing all return the same generic 404). Mass-assignment protection is structural (only named fields ever read from the body). Deleting a published page is blocked (409) until it's archived. Extended `worker/audit.ts` with `PAGE_CREATED`/`PAGE_UPDATED`/`PAGE_PUBLISHED`/`PAGE_ARCHIVED`/`PAGE_DELETED`.
- Added `src/admin/AdminPagesPage.tsx` (list + shared create/edit form, real data only — "No pages yet" when empty) and wired `/admin/pages` into the nested Admin route tree; the Pages nav item now links instead of showing "Soon".
- Added a public rendering foundation: a top-level `/:slug` route (`CmsPageRoute`/`CmsPageContent` in `src/App.tsx`) fetches the public endpoint and renders title/description/content as plain React text (never `dangerouslySetInnerHTML`), reusing the existing `.legal-content`/`.page-intro` styling. Route-collision safety is defense-in-depth: React Router's static-beats-dynamic ranking plus the explicit `RESERVED_PAGE_SLUGS` check both independently stop a page slug from shadowing `/admin`/`/api`/`/tools`/etc.
- **Discovered and fixed one test-mock bug while building this** (not a production bug): the shared `FakeD1` test helper was returning live object references from SELECT queries instead of independent snapshots, which silently broke a before/after status-comparison used by the audit-action logic (an UPDATE's in-place mutation was retroactively "changing" an already-returned "before" row, since both were secretly the same JS object). Fixed by shallow-copying rows on return from the mock's SELECT branches; caught by a failing test, not by inspection.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 103/103 (81 pre-existing + 22 new: real-SQLite migration validation with FK enforcement, unique-slug constraint, authenticated/unauthenticated/unauthorized read, authorized/unauthorized write, validation rejection incl. reserved slug and unsafe canonical URL scheme, duplicate-slug 409, draft-not-public/published-is-public, mass-assignment protection, XSS/content-safety incl. a static dangerouslySetInnerHTML-usage check, audit logging across create/publish/archive/delete plus the publish-before-delete guard, actor-spoofing resistance, and a parameterized-SQL static check). `npm run build` — PASS.

## Phase 2.10 — Tools Management

- **Uncommitted.** Extended the existing (Phase 0) `tools`/`categories` tables in place via `migrations/0006_tools_management.sql` — `tools` gained `icon`/`is_popular`/`seo_title`/`seo_description`/`created_by`/`updated_by` (pre-existing `featured`/`status`/`component`/`sort_order` columns reused as-is, not renamed). Seeded BOTH tables for the first time ever, from the real static registry in `src/App.tsx`: 4 categories (qr/pdf/image/other) and all 34 real tools — same slugs/names/descriptions/icons, `component='ToolPage'` for every row (the actual generic implementation component every tool route renders today).
- Added `shared/tools.ts`: centralized fail-closed validation (slug format, per-field length limits, a forward-looking `TOOL_CATEGORIES` allowlist broader than what's actually seeded, a controlled `ICON_NAMES` allowlist so icons are always identifiers, never markup).
- Added `worker/tools.ts`: `GET/POST /api/admin/tools` and `GET/PATCH/DELETE /api/admin/tools/:id` (via `authorize()` — `tools.view`/`tools.manage`, both pre-existing in the Phase 2.5 matrix). A tool category is only accepted once a real `categories` row exists for it (looked up, never auto-created). Mass-assignment protection is structural. Deleting an active tool is blocked (409) until deactivated. Extended `worker/audit.ts` with `TOOL_CREATED`/`TOOL_UPDATED`/`TOOL_ACTIVATED`/`TOOL_DEACTIVATED`/`TOOL_DELETED`.
- **No public `GET /api/tools` endpoint was built — a deliberate, documented decision** (see `DECISIONS.md`), not an oversight: the public site (Homepage, `/tools`, `/tools/:slug`) continues to read exclusively from the static registry in `src/App.tsx`, completely unchanged this checkpoint. The new tables are a real, fully CRUD-manageable Admin configuration surface with no public consumer yet; wiring the public site to it is explicit future work.
- Added `src/admin/AdminToolsPage.tsx` (list + shared create/edit form, real data only) and wired `/admin/tools` into the nested Admin route tree; the Tools nav item now links instead of showing "Soon". Category and Icon are both `<select>` pickers (Category limited to the 4 real seeded categories, Icon limited to `ICON_NAMES`) rather than free text.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 126/126 (103 pre-existing + 23 new: real-SQLite migration validation confirming all 34 tools/4 categories seeded correctly with FK/unique-slug enforcement, a static check that the existing 34-tool registry is untouched, duplicate-slug 409, authenticated/unauthenticated/unauthorized read, authorized create/update, unauthorized-mutation 403, validation rejection incl. an unseeded-but-allowlisted category, mass-assignment protection, actor-spoofing resistance, a static check confirming no `/api/tools` route was added plus that the admin payload excludes internal `category_id`/`component` fields, audit logging across create/deactivate/delete plus the deactivate-before-delete guard, icon-allowlist rejection of a raw `<svg onload=...>` payload, existing tool/admin/homepage route regression, and a parameterized-SQL static check). `npm run build` — PASS. No new HIGH/MEDIUM-confidence vulnerability found on a focused self-review against the full checklist (IDOR, privilege escalation, mass assignment, icon/SVG injection, SQL injection, route manipulation, inactive-tool exposure, forged actor/role).

## Phase 2.11 — Admin Security + Final Tests (Phase 2 Final Checkpoint)

- **Uncommitted.** Implemented the previously-open Security Headers item: `worker/security-headers.ts#withSecurityHeaders`, applied once at `worker/index.ts`'s single top-level `fetch()` to every response (API and static-asset alike) — `Content-Security-Policy` (strict: `script-src 'self'`, `style-src 'self'`, `object-src 'none'`, `frame-ancestors 'none'`, no `unsafe-inline`/`unsafe-eval`), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/microphone/geolocation/payment denied), `X-Frame-Options: DENY`, and conditional `Strict-Transport-Security` (HTTPS requests only). The CSP was derived by actually inspecting the built `index.html`/bundle (one same-origin script, one same-origin stylesheet, zero external hosts) rather than copied from a template.
- Replaced the one pre-existing inline `style={{ whiteSpace: "pre-wrap" }}` (the Phase 2.9 public CMS page body) with a CSS class (`.cms-page-content`) specifically so the CSP's `style-src` needs no `'unsafe-inline'` exception — a one-line, zero-risk change, not a refactor.
- **CSRF reviewed, no change made:** `SameSite=Strict` + `HttpOnly` + `Secure` (Phase 2.3) already blocks the session cookie on any cross-site request for this same-origin architecture; a token mechanism would add complexity with no additional protection. Documented as a final decision in `DECISIONS.md`.
- **Password hashing reviewed, no change made:** PBKDF2 at 100,000 iterations is below OWASP's current 600,000 recommendation, but raising it without being able to verify it stays inside Cloudflare Workers' per-request CPU budget (blocked by the standing `workerd`-on-Windows limitation) would trade a documented low-severity gap for an unverified one. Recorded as a deferred, environment-gated item — zero migration cost whenever it's raised, since the stored hash format self-describes its own iteration count.
- **Found and closed one real test gap:** `resolveAuthenticatedUser` already re-checks the live `users.status` on every request (not just at login), so deactivating a user mid-session correctly invalidates their still-unexpired session — this had never been directly tested (only login-time deactivation was). Added one test (`tests/rbac.test.ts`) confirming the existing behavior; no code change was needed, the implementation was already correct.
- **Full Phase 2 review performed** across authentication, sessions, RBAC, audit logging, settings, pages, tools, admin UI, and the database — re-verified via the existing (now 130-test) suite, a whole-tree static grep for `eval`/`new Function`/`dangerouslySetInnerHTML`/`document.write`/`innerHTML=`/hardcoded secrets/localStorage-for-auth (all clean, no matches outside comments/tests explaining their absence), and a fresh real-SQLite check that all 6 migrations apply both sequentially and via `database/schema.sql` standalone to an identical end state (table-by-table column comparison, not just table-name comparison).
- **Git hygiene note (no action taken):** a handful of pre-existing, unrelated-to-Phase-2 WIP artifacts remain untracked (`add_phase1f_b1.ps1`, `app_diff.txt`, `phase1f-b1.css`, `src/styles.css.phase1f-b1.backup`) — flagged for the user's own judgment, not created or deleted this session.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 130/130 (126 pre-existing + 3 security-header tests + 1 mid-session-deactivation test). `npm run build` — PASS. No FAIL-level finding anywhere in Phase 2.
- **PHASE 2 FINAL CHECKPOINT: PASS.** See `PROJECT_STATE.md`'s "Phase 2 Final Checkpoint" table for the full per-area breakdown. The only DEGRADED item is production readiness (no live Cloudflare deployment/real D1 has ever been attempted — an access/environment limitation, not a code defect).

## Production Deployment (2026-09-13, same day as Phase 2.11)

- Cloudflare authentication became available; the "Production readiness: DEGRADED" item above was resolved for real, not just re-labeled.
- Committed and pushed `ff0318c` ("feat: complete phase 2 admin foundation") — the full Phase 2.1–2.11 codebase, 59 files.
- Created the real D1 database ("codivio", region WEUR, id `69291a30-4db3-41e9-b461-08bd4994e015`; none existed before — confirmed via `wrangler d1 list` first), applied all 6 migrations to it (`wrangler d1 migrations apply --remote`), and verified the result (34 tools/4 categories/9 settings rows, matching local SQLite exactly).
- Deployed the Worker (`wrangler deploy`); live-verified homepage/`/admin/*`/`/api/health`/`/api/admin/pages` (401 without auth)/`/api/settings/public` against `https://codivio.online`.
- Found and fixed a real live-only gap: HTML/asset responses had none of the Phase 2.11 security headers (Cloudflare's Workers-with-Assets platform serves matching static assets from its edge without invoking the Worker), while `/api/*` responses had them correctly. Fixed by adding `assets.run_worker_first: true` to `wrangler.jsonc`; committed and pushed as `f9706f6` ("fix: complete production security headers"); redeployed and re-verified all 6 headers present on both HTML and API responses.
- Bootstrapped the first real production `super_admin` account via the existing one-time `POST /api/auth/bootstrap` mechanism (Worker Secrets `ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_BOOTSTRAP_PASSWORD`, set via `wrangler secret put`, never printed/committed); confirmed the account exists with real email/role/status only (`password_hash` never selected/printed); confirmed bootstrap self-disabled afterward (a second call correctly returns 403).
- Diagnosed and fixed a real login rate-limit friction point for normal admin use: raised `MAX_FAILED_ATTEMPTS` from 5 to 25 (`worker/auth.ts`) — reviewed and approved as a deployment-only change; committed and pushed as `dfe86f1` ("fix: increase admin login attempt limit"); redeployed and re-verified.
- Reset the production admin password once via the same `hashPassword()`/PBKDF2-SHA256 implementation the app itself uses (a local, uncommitted, one-off script computed the hash and applied it directly via `wrangler d1 execute --file`, then deleted itself) after the account's first login attempt failed — root-caused as an input-level mismatch (not a code defect; the same hashing/verification implementation is used for both write and read paths, and every other component checked out), confirmed by a real successful login + session check afterward.
- **Result:** Phase 2 (through `dfe86f1`) is live in production, not just locally verified. `PROJECT_STATE.md`'s "Cloudflare deployment status"/"Production readiness status"/"Database status"/"Worker status"/"Authentication status"/"Security Headers status" sections were updated accordingly during the Phase 2 Final Checkpoint Audit (below) after being found stale.

## Phase 2.13 — Admin + Homepage UI/UX Redesign

- **Uncommitted.** Pure frontend checkpoint — no worker/shared/migration/D1/security-header file touched. Skills used: `codivio-ui`, `codivio-admin`, `redesign-skill`.
- Official Codivio logo now used consistently (public header, footer, Admin login, Admin shell header, favicon) — replaces the generic `Code2` Lucide mark everywhere; the artwork itself was not altered, only clipped/sized via a new `.brand-logo` CSS class.
- Homepage hero's decorative floating QR/PDF/Image mockup cards replaced with a real right-side advertisement placeholder (`.hero-ad-slot`), correcting a standing gap against CLAUDE.md §23. Added a new "Image & Other Tools" slider (derived from the existing `category` field) between PDF Tools and Blog. The homepage's 12 featured tools were verified already exactly correct — no registry change needed.
- Admin navigation expanded from 7 to all 16 requested modules; `NAV_ITEMS.permission` is now optional so the 9 modules with no backend yet don't need a fabricated RBAC permission. Added `src/admin/AdminComingSoonPage.tsx` (shared, reusable, real category-chip content, zero fake data) for those 9 plus Users/CRM, Analytics, and Audit Log — 12 placeholder routes total (corrected 2026-09-14 from an initial miscount of 11).
- Redesigned the Admin Dashboard into 7 real KPI category cards (Traffic/Tools/Users-CRM/Revenue/Advertising/Affiliate/System) — every metric named in CLAUDE.md §7 renders "Not connected", nothing fabricated.
- Redesigned Admin Settings from 5 stacked sections into a 14-category tab layout; real settings (General/Analytics/Search Console/Advertising/Affiliate/Security/System) unchanged in behavior, the other 7 categories show an honest "Coming soon" panel.
- **Explicitly deferred, not silently dropped:** dedicated page UI for Advertising/SEO/Search Console/Blog/Social/Reports/Monetization/Affiliate/System Health (architecture-only this checkpoint, per the phase's own Strict Scope); new Tools-editor fields with no backing column (would be fake, unsaved UI).
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 137/137 (131 pre-existing + 6 new: full 16-module nav check, logo-consistency check, favicon check, hero-ad-slot-replaces-mockup check, exact-12-featured-tools check). `npm run build` — PASS. Verified live via the real Vite dev server (not just the production build): homepage and every changed/new module transform and serve with 200, no build/transform errors.

## Phase 2.14 — Homepage Final UI/UX + Responsive Polish

- **Uncommitted.** Doc-only correction first: Phase 2.13's report undercounted `AdminComingSoonPage` placeholder routes as 11 (forgot Analytics alongside Users/CRM and Audit Log as the 3rd permission-gated placeholder); actual, verified count is 12. Corrected in `PROJECT_STATE.md`/`CHANGELOG.md`/`DECISIONS.md`.
- Pure frontend, homepage-focused checkpoint — Phase 2.13's structure/logo/12-tool-list/all 5 sliders preserved and re-verified unchanged; no worker/shared/migration/D1/security-header file touched.
- Fixed a real, previously-unnoticed accessibility gap: `.hero-search`/`.nav-search` removed the default focus outline (`outline:0`) with no visible replacement at all — added a `:focus-within` style matching the existing `.page-search` pattern.
- Added branded `:focus-visible` styles (matching the existing Admin convention) to tool cards, slider prev/next buttons, the mobile-menu button, the slider's own tabbable track, and nav/footer links/brand — none of these had one before.
- Added a skip-to-content link (`PageShell`) targeting a new `id="main-content"` on HomePage's `<main>` — scoped to HomePage only for this checkpoint, not sitewide (documented as a known partial-coverage limitation in `PROJECT_STATE.md`).
- Marked the decorative tool-card arrow icon `aria-hidden="true"`.
- Structural review confirmed no fixed pixel width in the stylesheet can cause horizontal overflow between 375–1440px outside two already-handled, hidden-on-mobile/off-canvas cases.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 146/146 (137 pre-existing + 9 new: homepage section-order check, PageShell header/children/footer order check, fixed-width responsive-safety check, hero-ad collapse check, skip-link/target check, decorative-icon check, focus-within check, 5-selector focus-visible check, single-H1 check). `npm run build` — PASS, bundle essentially unchanged (341.63 kB / 101.49 kB gzip, no dependency added). No browser/screenshot tool available in this environment — verified via typecheck, build, structural source tests, and real (HTTP-only) Vite dev-server transform checks, not actual rendered/visual confirmation.

## Phase 2.15 — Multilanguage System + UI Text Quality Fix

- **Uncommitted.** New: `shared/i18n/{languages,types,az,tr,en,index}.ts` (centralized `Translations` dictionaries, AZ default/TR/EN, same Worker+frontend-shared pattern as `shared/rbac.ts`/`shared/settings.ts`), `src/i18n/{LanguageContext,LanguageSwitcher}.tsx` (React context/hook + reusable switcher), `tests/i18n.test.ts` (new dedicated test file, 12 cases).
- Wired translations into `src/App.tsx` (public header/footer/hero/all 5 section headings), `src/admin/AdminApp.tsx` (nav — `NAV_ITEMS` const became `getNavItems(t)` — login screen, dashboard cards, logout button), `src/admin/AdminSettingsPage.tsx` (`SETTINGS_TABS` const became `getSettingsTabs(t)`, new "Language" tab added, `general.default_language` moved onto it from General), `src/admin/AdminComingSoonPage.tsx` (refactored from literal title/description/categories props to a single `moduleKey` prop resolved against the dictionaries), `src/admin/AdminPagesPage.tsx` and `src/admin/AdminToolsPage.tsx` (list/editor chrome — headings, buttons, empty states, Yes/No — not their form field labels).
- Fixed the awkward machine-translation-style setting descriptions named in this checkpoint's brief: "Whether Google Analytics has been configured" → "Is Google Analytics configured?" (and the equivalent fix for Search Console/AdSense/the affiliate program), in all 3 languages. Centralized and made consistent the "Coming soon"/"Not configured"/"Disabled"/"Configured"/"Enabled" status vocabulary across every Admin surface that shows it.
- Added `general.default_language` server-side validation to `shared/settings.ts` (rejects any value outside `az`/`tr`/`en`) — the one minimal, backend-adjacent change, explicitly within this checkpoint's "unless the existing settings architecture requires it" allowance. No migration, no schema change, no auth/RBAC/session/security-header file touched. The already-live production value (`"en"`) was deliberately left unmigrated.
- Language persistence is two layers by design, not two conflicting sources of truth: the existing public `general.default_language` Setting is the site-wide default for a new visitor (changeable only by a `settings.manage` role, via the same `PATCH /api/admin/settings` endpoint as any other setting); `localStorage` (`codivio_language`) is a given browser's own override, always checked first and never silently overwritten once set. `DEFAULT_LANGUAGE = "az"` is the synchronous fallback applied before either the stored value or the site default resolves — first paint is never network-blocked.
- Added `.language-switcher`/`.language-switcher-admin` CSS (`src/styles.css`) — no fixed pixel width, so longer AZ/TR option strings cannot overflow the header/Admin header/Settings tab.
- **Scope boundaries, documented not silently applied:** the 34-tool registry, category-filter pills, FAQ/About/Contact/legal page bodies, Blog content, and the Pages/Tools Admin editor form field labels remain English-only — translating page/tool *content* (vs. UI chrome) is a separate, larger content-localization effort. No hreflang/language-specific metadata was added (Phase 3 scope) — the architecture is compatible with adding it later without rework.
- One known, minor wording deviation: the AZ translation for "Is the affiliate program enabled?" uses "Tərəfdaşlıq proqramı aktivdir?" (matching `nav.affiliate = "Tərəfdaşlıq"` for internal consistency) rather than a literal "Affiliate proqramı aktivdir?" — a defensible synonym choice, flagged here rather than silently presented as an exact match.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 159/159 (147 pre-existing, 2 updated to match the `getNavItems`/translated-section-heading refactor + 1 net-new nav-completeness check, + 12 new in `tests/i18n.test.ts`: language/default-language values, dictionary completeness with no blank strings, interpolated-string correctness, the exact wording-fix assertions, server-side validation accept/reject, persistence-source checks against the real `LanguageContext.tsx` source, no-scattered-language-branch check, shared-switcher-component check, responsive-width check). `npm run build` — PASS (366.34 kB / 109.26 kB gzip JS, 27.90 kB / 5.64 kB gzip CSS). Not committed/pushed/deployed per this checkpoint's explicit instruction.

## Phase 2.15 follow-up — Homepage 4/2/1 Carousel Fix + Release

- Fixed a real bug found while verifying the previously-completed Phase 2.15 carousel work: all 5 homepage carousels (Popular/QR/PDF/Image & Other Tools/Blog) used a fixed pixel card width (`260px` desktop, `min(78vw,280px)` mobile), so the next card always partially "peeked" into view at every breakpoint instead of showing an exact 4/2/1 count. Fixed in `src/styles.css` by sizing each card as a container-relative `calc()` (`calc((100% - 48px)/4)` desktop, `calc((100% - 16px)/2)` tablet at `<=900px`, `100%` mobile at `<=650px`) so N cards + (N-1) gaps always sum to exactly 100% of the track width — the next card then starts exactly at the clipped edge with 0px showing. No JS change needed (the existing prev/next handlers already read `card.offsetWidth` live).
- Added `tests/homepage-carousel.test.ts` (8 cases: exact card-count CSS per breakpoint, regression guard against reintroducing fixed px/vw widths, gap-consistency check, overflow-clip check, 5-carousel architecture-reuse check, dynamic-scroll-amount check).
- **Committed, pushed, and deployed to production** — `6a6eb69` "feat: add multilingual ui and responsive tool carousels" (bundles this carousel fix together with the full, previously-uncommitted Phase 2.15 multilingual system above, per explicit user request to release both together). Pushed to `origin/main` (`ab715b5..6a6eb69`). Deployed via `wrangler deploy`, Cloudflare Worker Version ID `b63f637f-1823-4197-88ac-7c43e963f299`, live at `https://codivio.online` and `https://codivio.azbloknot.workers.dev`.
- **Production smoke test:** all public/admin page routes 200; `/api/health` connected; `/api/auth/session` and `/api/admin/pages` correctly 401 unauthenticated; `/api/settings/public` returning real rows; all 6 security headers present on `/`, `/admin/login`, `/api/health`. **Production bundle verification:** downloaded and grepped the live JS/CSS bundles (byte-identical hashes to the local build) to confirm AZ/TR/EN dictionary strings, the `codivio_language` persistence key, the corrected setting-description wording, and the new `calc()`-based carousel CSS were actually shipped, with the old fixed `260px`/`78vw` values confirmed absent. No browser-automation/visual verification was available or claimed — reported as a limitation, not glossed over.
- **Test status:** `npm test` — PASS, 167/167 (pre-commit and pre-push hooks each independently re-ran the full suite + build and passed). `npm run typecheck`/`npm run build` — PASS.

## Phase 3.1 — SEO Title + Meta Description Foundation

- **Uncommitted.** New: `shared/seo/{types,site,pages,tools,duplicates,index}.ts` (a `shared/i18n/`-style framework-agnostic SEO metadata module — `SeoEntity` = route path + robots directive + `Record<Language,{title,description}>`), `src/seo/useSeo.ts` (the real `usePageMeta` implementation, re-exported from `src/App.tsx` so the 5 existing Admin call sites keep working unchanged), `src/admin/AdminSeoPage.tsx` (read-only Admin SEO preview, replacing that route's `AdminComingSoonPage` placeholder), `public/sitemap.xml` (new, static, matching the `robots.txt`/`llms.txt` pattern), `tests/seo.test.ts` (14 cases).
- **Full inventory audited first** (per this checkpoint's own instruction, before any implementation): 12 public routes confirmed from `src/App.tsx`'s actual `<Routes>` (home/tools/tools-slug/blog/faq/about/contact/privacy/terms/cookies/CMS-slug/404), and the tool registry re-verified at exactly 34 tools / 4 categories (not assumed unchanged).
- **Wrote unique, hand-crafted SEO title + meta description for all 34 tools and all 9 static public pages, in AZ/TR/EN (43 × 3 = 129 title/description pairs)** — each grounded in that specific tool/page's real function, not a template or a copy of the on-page H1, and not a mechanical translation between languages (verified: no entity's AZ/TR copy is byte-identical to its EN copy).
- **Robots decision:** every tool page is `noindex,follow` — `src/pages/ToolPage.tsx` renders only a "Tool coming soon" placeholder today (verified, not assumed), so indexing any of them would risk the exact misleading/thin-content problem Google's guidelines warn against; `follow` keeps internal link discovery working. All 9 static pages are `index,follow`. 404 is `noindex,nofollow`. Any `/admin/*` route defaults to `noindex,nofollow` automatically inside the hook (on top of the existing `robots.txt` disallow). A CMS page's existing `is_indexable` flag (already returned by the public API, never previously read by the frontend) is now wired through — a pure frontend fix, zero backend change.
- **Fixed 3 real, pre-existing bugs found during the audit, not new features:** `public/robots.txt`'s `Sitemap:` directive and `public/llms.txt` both pointed at a typo'd `codovio.online` instead of `codivio.online`; the 3 legal pages (Privacy/Terms/Cookies) shared one identical, generic meta description — each now has a distinct, real, multilingual one; `<html lang>` was hardcoded to `"en"` despite AZ being the Phase 2.15 default — fixed to `"az"` in `index.html` and kept in sync with the active language at runtime by `useSeo.ts`.
- **`usePageMeta` now also sets** canonical (`link[rel=canonical]`, always built from the hardcoded `https://codivio.online`, never the current origin/Worker hostname), `meta[name=robots]`, the full Open Graph set, and Twitter Card tags — all via `setAttribute`, never `innerHTML`/`dangerouslySetInnerHTML`; any admin-supplied canonical URL is re-validated as absolute `https://` before use.
- **Database:** no new/extended table. `tools.seo_title`/`seo_description`, `pages.meta_*`/`is_indexable`, and `seo_settings` (Phase 0/2.9/2.10) are left exactly as they are — reconciliation decision documented in DECISIONS.md ("SEO Metadata Architecture"): they remain the right shape for a *future* D1-driven SEO layer once the public site actually reads tool/page content from D1 (still not the case), not extended to multilingual now with zero real consumer.
- **hreflang:** not implemented — no distinct URL per language exists yet, so a correct hreflang implementation isn't possible; documented as required future Phase 3 technical SEO work.
- **Duplicate detection:** 0 duplicate titles, 0 duplicate descriptions across all 43 entities × 3 languages (test-enforced via new `shared/seo/duplicates.ts` utilities, also used by the Admin SEO preview).
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 181/181 (167 pre-existing + 14 new in `tests/seo.test.ts`: tool/page inventory coverage, duplicate detection, multilingual coverage, canonical/robots correctness, technical-SEO-file regression guards). `npm run build` — PASS (400.75 kB / 119.30 kB gzip JS, 29.99 kB / 5.97 kB gzip CSS). Not committed/pushed/deployed per this checkpoint's explicit instruction.

## Phase 3.2 — SEO Search-Intent Audit

- **Uncommitted.** New: `shared/seo/intent.ts` (search-intent dataset — primary intent for all 43 entities, plus 2 secondary/long-tail phrasings per tool, all per language), `tests/seo-intent.test.ts` (5 cases: intent-coverage, cannibalization, misleading-claim checks).
- **Real bug found and fixed**: `shared/seo/duplicates.ts#findLengthOutliers` measured the raw pre-suffix title instead of the actual rendered `<title>` (with " | Codivio" appended), producing 3 false "too short" flags and missing real "too long" ones. Fixed to measure `buildTitle(copy.title)`.
- **18 titles (mostly TR/AZ) trimmed** after the fix surfaced them as genuinely exceeding ~65 rendered characters — each trim removed a redundant word already implied elsewhere in the same title, never cut meaning; re-verified afterward that no trim introduced a new duplicate-title collision. No metadata outside this specific flagged list was touched.
- **Search-intent audit performed and documented**: every tool now has a `primary` search intent + 2 `secondary`/long-tail phrasings per language (AZ/TR/EN); every page has a `primary` intent per language — purely editorial judgment, no search-volume/ranking claim anywhere (per Phase 3.1 Step 16's restriction).
- **Cannibalization review performed, not assumed**: checked the real candidates for overlap (format-specific converter pairs, generic-vs-specific QR generators) and confirmed each targets a genuinely distinct primary intent — no two tools share an identical primary-intent string in any language (test-enforced). No copy changed for this finding.
- **Misleading-claim re-audit**: grepped all 258 title/description strings for superlative/guarantee language — 0 matches.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 187/187 (181 pre-existing + 1 new length-outlier-zero assertion + 5 new in `tests/seo-intent.test.ts`). `npm run build` — PASS (400.56 kB / 119.28 kB gzip JS, 29.99 kB / 5.97 kB gzip CSS). Not committed/pushed/deployed.

## Phase 3.3 — Keyword & Search-Intent Strategy

- **Uncommitted.** New: `shared/seo/keywords.ts` (extends `intent.ts` — per-tool category/long-tail/search-intent/commercial-intent/content-opportunity/related-tool/cannibalization-rating data for all 34 tools, plus `CATEGORY_KEYWORD_OPPORTUNITIES` for the 4 real categories), `tests/seo-keywords.test.ts` (6 cases).
- **Checked for real keyword-data sources first — none connected.** No Search Console/Ads Keyword Planner/similar integration exists. Did real, live qualitative web research instead (QRCode Monkey, Smallpdf, iLovePDF, TinyPNG, iLoveIMG) for competitor page-title/content-theme patterns — see DECISIONS.md for findings. No search volume/CPC/difficulty/traffic/rankings/impressions/clicks appears anywhere (test-enforced regression guard against ever adding a fabricated-metric field).
- **Built the full 34-tool keyword map** by extending, not duplicating, Phase 3.2's `TOOL_INTENT` — `getToolKeywordProfile(slug, lang)` is the single combined accessor. Added: 2 new long-tail phrases per tool per language (204 total), search-intent classification (found: overwhelmingly "transactional" — an honest finding, not forced variety), commercial-intent-applicability (true for 5 tools with real comparison-search behavior), a future content/blog opportunity per tool, 2 related-tool internal-linking candidates, and a SAFE/WATCH/CONFLICT cannibalization rating.
- **Cannibalization analysis: 0 CONFLICT, 8 WATCH, 26 SAFE.** WATCH covers the qr-code-generator hub vs. its 2 closest specific siblings and the 3 format-specific converter pairs already reviewed in Phase 3.2 — real, close relationships worth monitoring, not actual conflicts. No tool copy changed.
- **Category/blog architecture documented, not built:** future category-page keyword targets recorded for all 4 real categories (cross-checked against the real 12/8/11/3 split); each tool's content opportunity feeds Phase 3.4. No category routes or blog pages created.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 193/193 (187 pre-existing + 6 new). `npm run build` — PASS (422.57 kB / 124.58 kB gzip JS — the ~5.3 kB gzip growth from the new data files is noted, not hidden, in PROJECT_STATE.md). Not committed/pushed/deployed.

## Phase 3.4 — Structured SEO Content Architecture

- **Uncommitted.** New: `shared/seo/content.ts` (per-tool introduction/value-proposition/benefits/how-to/use-cases/FAQ for all 34 tools × 3 languages, plus `CATEGORY_CONTENT_BLUEPRINT` for the 4 real categories and `getContentBlueprint(slug, lang)` combining this with Phase 3.3's keyword data), `tests/seo-content.test.ts` (8 cases).
- **Re-verified `ToolPage.tsx` before writing any content**: all 34 tools still render only "Tool coming soon" — no real processing exists. Every how-to step describes the general real-world workflow (not a claim that Codivio performs it today), paired with one shared `TOOL_STATUS_NOTE` disclaimer and one shared, honest `SHARED_TRUST_MESSAGE` (pointing to the real Privacy Policy) instead of fabricating 34 unique per-tool privacy/processing claims. None of this checkpoint's explicitly banned phrases appear anywhere (test-enforced).
- **Wrote genuinely unique, tool-specific content for all 34 tools** — 3 benefits, 3 how-to steps, 3 use cases, 2 FAQ topics per tool per language; introductions and value propositions are new on-page content, distinct from (not copied from) Phase 3.1's already-audited meta title/description (test-enforced: zero duplicate introductions or value propositions within any language).
- **Reused, not duplicated, Phase 3.3's keyword architecture**: `getContentBlueprint` pulls `primaryKeyword`, `relatedToolOpportunity` and `futureContentOpportunity` straight from `shared/seo/keywords.ts` rather than re-authoring them.
- **Category content foundation**: purpose statement (AZ/TR/EN) + supporting topics for each of the 4 real categories, including category-level comparison-article ideas (e.g. "PDF to Word vs PDF to Excel"). No category route created.
- **Content-relationship mapping documented** (`CONTENT_RELATIONSHIP_MAP`): how-to/use-case/educational/comparison article types mapped onto existing fields rather than a new parallel dataset — feeds Phase 3.6 (internal linking) and 3.13 (topical authority).
- **Bundle-size impact: 0 bytes, measured not assumed.** `content.ts` is not re-exported through the `shared/seo` barrel (nothing in the live app reads it yet) — the production build hash and size came back byte-identical to Phase 3.3's.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 201/201 (193 pre-existing + 8 new). `npm run build` — PASS (422.57 kB / 124.58 kB gzip JS, unchanged from Phase 3.3). Not committed/pushed/deployed.

## Phase 3.5 — AI-Structured Content / AI Discoverability / GEO

- **Uncommitted.** New: `shared/seo/ai.ts` (`SITE_IDENTITY`, `getAiToolProfile`/`getAiCategoryProfile`, relationship queries, 6 deterministic answer functions — all reusing Phase 3.1/3.3/3.4 data, no new content authored), `tests/seo-ai.test.ts` (11 cases). Modified: `public/robots.txt`, `public/llms.txt`.
- **Fixed a real, pre-existing `robots.txt` bug found during this phase's mandated audit**: `Disallow: /admin/`/`Disallow: /api/private/` only applied to the file's last `User-agent` group (per the robots.txt spec, there is no fallback to `User-agent: *`) — meaning these paths were never actually disallowed for general crawlers via robots.txt. Fixed by repeating the disallow lines in every group; not a security exposure (admin routes are protected by real application auth regardless) but a real SEO-hygiene gap.
- **Fixed a real, pre-existing error**: `OAI-AdsBot` is not a documented OpenAI crawler (verified via live web search against OpenAI's actual docs: `GPTBot`/`OAI-SearchBot`/`ChatGPT-User`). Replaced with the real, currently-documented AI answer/retrieval crawlers: `OAI-SearchBot`, `ChatGPT-User` (OpenAI), `Claude-SearchBot`, `Claude-User` (Anthropic), `PerplexityBot` (Perplexity). Training-only crawlers intentionally left unlisted (fall under the general `*` allow) — documented as a deliberate non-decision, not silently defaulted.
- **Updated `public/llms.txt`** with genuinely missing, verifiable facts: AZ/TR/EN language support, the real 9-page public-page list, the real 34-tool/4-category list, and an honest "in development" status disclosure — plus an explicit statement that the file doesn't affect Google ranking or guarantee AI visibility. No unverifiable fact added.
- **`shared/seo/ai.ts` reuses, never duplicates**, Phase 3.1's `TOOL_SEO`, Phase 3.3's `TOOL_KEYWORDS`, and Phase 3.4's `TOOL_CONTENT`/`CATEGORY_CONTENT_BLUEPRINT`. `SITE_IDENTITY.toolCount` is computed (`Object.keys(TOOL_SEO).length`), never hardcoded, so it can't silently drift from the real registry. No founding date/user count/employee count/awards/testimonials/social profiles were fabricated — `verifiedSocialProfiles` is an honest empty array.
- **No chatbot, external AI API, or LLM dependency added** — the "answer" functions are deterministic string builders over already-audited structured data, the same kind of thing a static FAQ page does.
- **Bundle-size impact: 0 bytes, measured not assumed** — `ai.ts` is not re-exported through the `shared/seo` barrel (same pattern as `content.ts`); the production build hash and size came back byte-identical to Phase 3.3/3.4's.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 212/212 (201 pre-existing + 11 new). `npm run build` — PASS (422.57 kB / 124.58 kB gzip JS, unchanged). Not committed/pushed/deployed.

## Phase 3.6 — Internal Linking Architecture

- **Uncommitted.** New: `shared/seo/internal-links.ts` (`isLinkableRoute`, `getRelatedToolLinks`, `getCategoryToolLinks`, `getToolBreadcrumb`, `getFutureContentOpportunities`, `getToolsIndexLink`/`getHomeLink` — all reusing Phase 3.1/3.3/3.5 data, no new relationship dataset authored), `tests/seo-internal-links.test.ts` (9 cases).
- **Audited Phase 3.3's `relatedToolOpportunity` data before building on it**: checked programmatically for self-links, duplicates, and invalid slugs across all 34 tools — zero issues found, nothing needed fixing this time.
- **`isLinkableRoute` is a provable route-safety gate**, computed from the real `PAGE_SEO`/`TOOL_SEO` registries rather than a hardcoded allowlist — test-enforced to reject `/admin`, `/admin/settings`, `/api/private/*`, a fake tool slug, a hypothetical category route, and a hypothetical per-post blog route, while correctly allowing every real page and every real tool page.
- **Category breadcrumb segments and future-content opportunities are represented as non-linkable data (`path: null`)**, never a fabricated `/tools/category/*` or `/blog/...` URL — no category routes and no blog articles were created, per this checkpoint's explicit instruction.
- **No rendering change made to `ToolPage.tsx`/`App.tsx`** — judged unnecessary since every acceptance criterion concerns relationship-data correctness, not rendered UI; `Homepage`'s existing curated tool selection was reviewed and confirmed unchanged.
- **No new translation strings added** — site-level anchors reuse existing `shared/i18n` dictionary entries; tool anchors reuse Phase 3.5's already-localized `getAiToolProfile(...).name`.
- **Bundle-size impact: 0 bytes, measured** — `internal-links.ts` is not re-exported through the `shared/seo` barrel; the production build hash/size came back byte-identical to Phase 3.3/3.4/3.5's.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 221/221 (212 pre-existing + 9 new). `npm run build` — PASS (422.57 kB / 124.58 kB gzip JS, unchanged). Not committed/pushed/deployed.

## Phase 3.7 — Schema.org / Structured Data

- **Uncommitted.** New: `shared/seo/schema.ts` (`buildOrganizationNode`, `buildWebSiteNode`, `buildStandardPageGraph`, `buildToolPageGraph` — all reusing Phase 3.1/3.5/3.6 data), `tests/seo-schema.test.ts` (9 cases). Modified: `src/seo/useSeo.ts` (new `schemaGraph` option + safe JSON-LD rendering), `src/App.tsx` (wired schema into 10 call sites: homepage, all 9 static pages, all 34 tool pages via `ToolRouteContent`).
- **Verified current (2026) guidance before choosing schema types, not outdated assumptions**: confirmed via live search that Google's `SoftwareApplication` rich-result eligibility requires `offers.price` + rating/review (Codivio's "coming soon" tools have neither — used plain `WebPage` instead, avoiding both fabrication and a misleading "working application" implication); confirmed Google fully retired FAQ rich results in Search as of May 7, 2026, for every site (not just previously-favored authoritative ones) — deferred `FAQPage` schema as a verified-zero-benefit addition, not a shortcut.
- **`Article` schema deferred**: `/blog` confirmed index-only, no per-post route/entity exists to describe honestly.
- **`WebSite` has no `SearchAction`**: the homepage search filters tools client-side with no query-navigable URL.
- **`BreadcrumbList`'s un-routed category segment is dropped from the schema entirely** (not a linkless `ListItem`) — only real, linkable segments plus the current page (no self-link) appear, reusing Phase 3.6's `getToolBreadcrumb` rather than a new breadcrumb dataset.
- **JSON-LD rendered safely**: `document.createElement("script") + .textContent`, never `innerHTML`/`dangerouslySetInnerHTML`, `<` escaped as defense in depth, always stripped on `/admin/*` regardless of input.
- **Real, measured bundle-size finding, reported honestly, not hidden**: wiring `schema.ts` into `App.tsx` transitively pulled in Phase 3.4's entire `TOOL_CONTENT` dataset (via `getToolBreadcrumb` → `getAiToolProfile` → `getContentBlueprint`) even though schema only needs a tool's name + one-line description. Client JS grew from `422.57 kB / 124.58 kB gzip` to **`527.65 kB / 149.79 kB gzip`** (+25.21 kB gzip, ~20%) — confirmed by rebuilding, not estimated. Not fixed this phase (would mean modifying Phase 3.4–3.6 files without a correctness defect); assigned to Phase 3.8 (Technical SEO + Performance) as a New Finding.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 230/230 (221 pre-existing + 9 new). `npm run build` — PASS (527.65 kB / 149.79 kB gzip JS — real, measured growth, documented above, not claimed as zero-impact). Not committed/pushed/deployed.

## Phase 3.8 — Technical SEO + Performance

- **Uncommitted.** New: `tests/technical-seo-3.8.test.ts` (6 cases, including a build-artifact check that greps the actual shipped `dist/` bundle). Modified: `shared/seo/ai.ts` (new `getToolDisplayName`), `shared/seo/internal-links.ts` (`getToolBreadcrumb`/`toolLink` now use it), `shared/seo/schema.ts` (`buildToolPageGraph` now uses it + `TOOL_SEO`'s own description directly).
- **Fixed the Phase 3.7 bundle regression.** Root cause confirmed exactly as diagnosed: `schema.ts`/`internal-links.ts` called the heavy `getAiToolProfile` (which pulls Phase 3.4's entire `TOOL_CONTENT` dataset via `getContentBlueprint`) just to get a tool's short display name. Added a lightweight `getToolDisplayName(slug, lang)` reading only the already-loaded `TOOL_SEO`, and switched both call sites to it; `buildToolPageGraph`'s JSON-LD description now also reads `TOOL_SEO` directly instead of Phase 3.4's `introduction` field.
- **Measured, not estimated**: client JS `527.65 kB / 149.79 kB gzip` → **`424.97 kB / 125.43 kB gzip`** — recovers all but +2.40 kB raw / +0.85 kB gzip of the regression versus the original `422.57 kB / 124.58 kB gzip` baseline. Verified empirically: grepped the actual built bundle for Phase 3.4 `TOOL_CONTENT`-only text (absent) and for real tool titles + `application/ld+json` (present, proving schema/JSON-LD still work). Vite's "chunk larger than 500 kB" warning is gone.
- **Verified JSON-LD's CSP compatibility rather than assuming it**: confirmed `script-src` does not restrict non-executable `application/ld+json` script elements in any current browser; cross-checked the deployed CSP (`worker/security-headers.ts`, untouched) still has no `unsafe-inline`. No CSP change made or needed.
- **Technical SEO audit (25-item checklist) completed** — everything from Phases 3.1–3.7 confirmed still correct/unchanged (metadata, canonical, robots, sitemap, OG/Twitter, JSON-LD safety, duplicate-metadata risk, route safety, 404 behavior, admin/public separation, asset loading, carousel architecture). hreflang remains deliberately deferred (standing decision, no per-language URLs exist). Redirect behavior (www/https at the DNS layer) is UNKNOWN — NOT VERIFIED, outside this codebase/environment.
- **Two real findings verified and documented, deliberately not fixed this phase**: (1) the site is CSR-only, so per-page SEO/JSON-LD only exists after JavaScript executes — a real limitation for non-JS-executing crawlers, fixing it would mean an SSR/SSG architecture change requiring Change Control, not a technical-SEO-audit fix; (2) the shared 1.58 MB logo PNG is used as favicon + OG image + 34×34px header logo simultaneously — real oversized-asset finding, not fixed since altering the logo is forbidden and no image-processing tool exists in this environment.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 236/236 (230 pre-existing + 6 new). `npm run build` — PASS (424.97 kB / 125.43 kB gzip JS — the fix, measured). Not committed/pushed/deployed.

## Phase 3.9 — E-E-A-T + Trust

- **Uncommitted.** New: `tests/eeat-trust-3.9.test.ts` (6 cases). Modified: `src/pages/ToolPage.tsx` (one trust-row wording fix, affects all 34 tool pages via the shared component).
- **Audited About, Contact, Privacy, Terms, Cookies, and the footer** — all found already honest and appropriately hedged; no fabricated company facts, credentials, awards, reviews, or testimonials found anywhere (test-enforced).
- **Fixed one real overclaim**: "Fast in-browser tools" (asserting a working performance/architecture capability no tool has) → "In-browser by design" (states the real, verifiable architectural intent without claiming current functionality or speed).
- **Independently verified, not trusted, the prompt's claim of a known "Codivio.online" Instagram/Facebook identity**: searched the repository (zero matches) and ran live web searches for a matching account on both platforms (zero matches — only unrelated similarly-named accounts found). Per the explicit anti-fabrication rule, **no `sameAs` was added to Organization schema and no social link was added anywhere.**
- **Two real trust gaps identified and deliberately left unfixed, each with a stated reason**: the Contact form is an honestly-disclosed non-functional placeholder with no verified support email to substitute (fabricating one is forbidden); the Blog has no author/date system (correctly deferred to Phase 3.13, since building one now would mean inventing editorial identities).
- **No worker/D1/auth/RBAC/CSP/security-header file touched** — confirmed via `git diff --stat`.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 242/242 (236 pre-existing + 6 new). `npm run build` — PASS (424.97 kB / 125.42 kB gzip JS, stable). Not committed/pushed/deployed.

## Phase 3.10 — Competitor SEO Gap Analysis

- **Uncommitted — research-only checkpoint, zero production files changed.** No new/modified source or test files.
- **Live-researched real competitors**: QRCode Monkey (direct competitor — homepage, robots.txt, and llms.txt all directly fetched and inspected), qr-generator.ai (direct competitor — homepage blocked direct fetch with HTTP 403, researched via search-result summaries instead, lower confidence, marked accordingly), Smallpdf's Merge PDF page and iLoveIMG's Compress Image page (adjacent/benchmark competitors).
- **Verified, not assumed, two real Codivio advantages**: QRCode Monkey's `robots.txt` is bare-minimum with no AI-crawler classification and no sitemap reference, and it has no `llms.txt` (confirmed via direct fetch: 404) — Codivio's Phase 3.5 work is ahead of this one specific competitor on this one dimension.
- **Identified the phase's single most actionable finding**: competitors render step-by-step instructions/FAQ/related-tools/breadcrumbs directly on live tool pages; Codivio has the equivalent data fully built (Phase 3.4/3.6/3.7) but not yet rendered on `ToolPage.tsx` — assigned to Phase 3.13, not implemented now.
- **No competitor content, branding, or code copied; no fabricated metrics used** — competitor-displayed trust claims (e.g., Smallpdf's user-count/rating claims) are reported as observed competitor claims only.
- Produced a full P0–P3 prioritized gap list, each item mapped to an existing Phase 3.11–3.15 slot or marked FUTURE / CHANGE CONTROL — no roadmap reordering.
- **Test status:** No production implementation changes; regression suite not required beyond research/baseline verification. Baseline verified directly against the repository (34 tools, 12 homepage-featured, all `shared/seo/*.ts` files present and unchanged from Phase 3.9's end state, confirmed via `git status`).

## Phase 3.11 — Backlink / Off-Page SEO Strategy

- **Uncommitted — research/strategy-only checkpoint, zero production files changed, zero external action taken.**
- **Verified Codivio's current off-page baseline: zero discoverable backlinks or brand mentions anywhere** — a direct web search for "codivio.online" and a negated self-site search both returned no matches (the search engine substituted an unrelated, similarly-named product). No DA/DR/traffic/backlink-count was fabricated to fill this gap.
- **Identified one concrete, real, name-verified opportunity**: an active ecosystem of "best free QR code generator" roundup/comparison articles (Bitly, TxtImpact, the-qrcode-generator.com, and others found via search) — gated as READY AFTER TOOL LAUNCH, since these publications test real functionality Codivio's QR tools don't have yet.
- **Researched real submission requirements for two well-known, legitimate platforms** (Product Hunt, AlternativeTo) via search — recorded their actual, verified process; nothing was submitted.
- **No spam/PBN/purchased-link/directory-blast strategy was proposed anywhere** — every tactic was classified SAFE/CONDITIONAL/DO NOT USE against current Google link-spam guidance.
- **Reconfirmed, per Phase 3.9's own finding**: no verified Codivio social profile exists, so no `sameAs`/social link was added anywhere and none is proposed for outreach.
- Produced a full P0–P3 prioritized off-page opportunity matrix (P0 = none — nothing is genuinely "critical" to pursue while zero tools are live), an anchor-text framework, an outreach workflow (not executed), and a tracking-field model (not implemented as a CRM) — all mapped to existing Phase 3.12–3.15 slots or marked FUTURE / CHANGE CONTROL.
- **Test status:** No production implementation changes; regression suite not required beyond research/baseline verification. Baseline reconfirmed via `git status` — identical to Phase 3.10's end state.

## Phase 3.12 — Google Search Console + Search Performance

- **Audit/access-status checkpoint — no production code changed.** No OAuth/service account/connector/GSC login exists in this environment, so console/API-level data (queries, impressions, clicks, CTR, position, index coverage, URL Inspection, country/device breakdowns, search appearance) is **BLOCKED — GSC ACCESS NOT AVAILABLE** across the board. None of it was fabricated or estimated.
- **Real, credential-free DNS verification performed**: a public TXT lookup on `codivio.online` found a `google-site-verification` record, confirming a Search Console property was verified for this domain at some point — the live console/property state behind it remains unverified.
- **Major finding: production is running a stale, pre-Phase-3 build.** Direct HTTP fetches of the live site show `index.html` and `robots.txt` matching the last commit (`6a6eb69`), not this session's uncommitted Phase 3.1–3.11 SEO work. Two concrete defects were found live in production that are **already fixed locally, just undeployed**: `robots.txt`'s `Sitemap:` line points to a typo'd domain (`codovio.online`, present since the first commit `21ee301`), and `/sitemap.xml` returns the SPA's HTML shell (`Content-Type: text/html`) instead of XML, because `public/sitemap.xml` has never been committed.
- **Resolved a prior UNKNOWN**: WebSearch confirms the GitHub repository `azbloknot-png/Codivio` is public and indexed (Phase 3.11's New Finding 2 is no longer "unknown visibility").
- **Verified, documented, not treated as a conflict**: Cloudflare auto-injects its own "Managed content" AI-bot block into the live `robots.txt` at the edge (Content-Signal + Disallow for training-oriented bots like GPTBot/ClaudeBot/CCBot/Bytespider/etc.), independent of and layered on top of the origin's own file — Phase 3.5's deliberate allow-rules for the separate search/answer-retrieval bot class (OAI-SearchBot, Claude-SearchBot, ChatGPT-User, Claude-User, PerplexityBot) are unaffected.
- **No GSC metric, ranking, index-coverage claim, or crawl date was invented anywhere in the report.**
- Findings mapped to existing Phase 3.13–3.15 slots or marked FUTURE / CHANGE CONTROL — no roadmap reordering. Deployment was not performed (out of this phase's scope and forbidden by standing instruction); the fix for both live defects already exists in the working tree and will ship whenever this SEO track is eventually committed/deployed.
- **Test status:** No production implementation changes; one DNS lookup, one live HTTP verification pass (robots.txt/sitemap.xml/homepage, with content-type headers), one WebSearch baseline check. `git status` reconfirmed unchanged from Phase 3.11's end state plus this phase's own memory-file edits.

## Phase 3 SEO Release — Commit, Push, Deploy

- **Explicit, user-approved controlled release** (separate request from the individual audit-only checkpoints) — committed and deployed the accumulated Phase 3.1–3.12 work after its own pre-deploy audit already returned PASS/READY.
- **Commit `4c558a6`** ("feat: release phase 3 seo foundation"): 34 files staged explicitly by path (never `git add -A`), reviewed via `git diff --cached` and a secret-pattern grep before committing. Excluded `tsconfig.tsbuildinfo` and all 4 pre-existing unrelated WIP files.
- **Pre-commit/pre-push gates**: `npm test -- --run` (242/242), `npm run typecheck` (clean), `npm run build` (424.97 kB / 125.42 kB gzip — unchanged from the Phase 3.8 baseline) all run once, all PASS, before staging; the repository's own pre-commit and pre-push hooks independently re-ran equivalent checks and also passed.
- **Pushed** `origin/main` from `6a6eb69` to `4c558a6`.
- **Deployed** via `wrangler deploy` against the existing, untouched `wrangler.jsonc` — Cloudflare Version ID `f8f48a76-be36-4cea-a80b-806a831691f5`. No DNS, binding, or secret was changed.
- **Live-verified** (real HTTP checks): homepage, `/tools`, one representative tool page, and `/api/health` all 200 (`database: connected`); `robots.txt` now correctly points its `Sitemap:` directive at `https://codivio.online/sitemap.xml` (typo gone); `sitemap.xml` now serves real, valid XML with the correct 9 URLs (a first check landed on a momentarily stale edge-cached response — confirmed via `CF-Cache-Status` to be ordinary CDN propagation lag, resolved within seconds, not a defect); the deployed JS bundle was grepped directly and confirmed to contain real Phase 3 code (`codivio-jsonld`, the Phase 3.9 trust-copy fix), not just updated static files.
- **Both Phase 3.12 production defects (sitemap-domain typo; missing sitemap.xml) are now resolved in production.**
- **No secrets exposed**: `/.env`, `/wrangler.jsonc`, `/.git/config` all verified to fall through to the SPA shell (no real file served); all 6 security headers still present.
- **No GSC claim made** — GSC access remains BLOCKED in this environment, unaffected by this deploy.
- Phase 3.13 was explicitly not started.

## Phase 3.13 — SEO Content Expansion + Topical Authority

- **Made Phase 3.4's content blueprint and Phase 3.6's internal-link/breadcrumb data actually visible** on all 34 tool pages for the first time — the single biggest carried-forward finding from Phase 3.10/3.11 is now resolved: visible breadcrumb, About/Benefits/How-it-will-work/Use-cases/FAQ sections, and a Related Tools list, all sourced from the existing, already-audited SEO architecture (no duplicate/parallel dataset created).
- **Content stays honest**: every "How it will work" section carries the existing in-development status note; the existing "coming soon" placeholder and Phase 3.9 trust row are unchanged; no banned overclaiming phrase was introduced (existing regression test still passes unmodified).
- **Added a 7-key `toolPage` i18n namespace** (AZ/TR/EN) for the new section headings/breadcrumb aria-label — the per-tool content itself was already localized since Phase 3.4.
- **Found and fixed a real bundle-size regression during implementation, not after shipping it**: lazy-loading `ToolPage.tsx` alone reintroduced the exact Phase 3.7 bundle size (527.55 kB) because `shared/seo/ai.ts` — imported eagerly by every page — still had a static import edge into the heavy `shared/seo/content.ts` for functions nothing eager actually called. Root-caused and fixed by moving `getAiToolProfile`/`getAiCategoryProfile`/`answerWhatIsTool` into `content.ts` itself (removing the edge, not just its usage). Remeasured: main chunk 438.05 kB / 128.20 kB gzip (vs. Phase 3.8's 424.97 kB / 125.42 kB gzip baseline — a modest, non-significant increase), with the tool-content dataset now shipping only in ToolPage's own 92.88 kB / 22.66 kB gzip lazy chunk — verified directly against the built output, not assumed.
- **No FAQPage schema added** for the newly-visible FAQ content — Phase 3.7's deferral stands; verified via a new regression test.
- **No new route was invented** — category breadcrumb segments remain non-linkable text, matching Phase 3.6's existing rule; no category landing pages were created.
- **Real, pre-existing limitation surfaced honestly, not hidden**: the tool-category display label (now visible in the breadcrumb, already visible in the existing eyebrow) is English-only regardless of selected language — a pre-existing Phase 2/3.6 gap, not something this phase introduced or was asked to fix.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 250/250 (243 pre-existing + 7 new). `npm run build` — PASS, main chunk 438.05 kB / 128.20 kB gzip, lazy ToolPage chunk 92.88 kB / 22.66 kB gzip. Real browser/visual rendering verification: **DEGRADED — browser automation unavailable** (the Cloudflare Vite plugin's local preview hit the same pre-existing, documented `workerd` access-violation crash from Phase 2) — relied on the test suite plus direct built-chunk inspection instead. Not committed/pushed/deployed, per this phase's own explicit instruction.

## Phase 3.14 — Free Tool → Premium Monetization Funnel

- **Built the FOUNDATION only, exactly as scoped**: a new `shared/monetization/` module (plan model, entitlement catalog, tool monetization metadata, a central `canUseFeature()` access decision engine, and a usage/quota shape) — one authoritative source of truth instead of scattered plan checks. No payment provider was integrated; none was even attempted.
- **Zero fabricated business data**: every plan's price is `null` (undecided, never a placeholder number); every plan's status is `"planned"` (none purchasable); every entitlement's status is `NOT_AVAILABLE` or `PLANNED` (none `AVAILABLE`, since nothing is actually gated anywhere in the product today) — verified by tests, not just asserted in prose.
- **A real, if narrow, security finding**: there is currently no attack surface for plan self-escalation to even secure, because nothing in this codebase reads a client-supplied `plan` value from a request, cookie, query string, or localStorage anywhere — confirmed by grep across `worker/`, `src/`, and the new module itself. `canUseFeature()` itself returns `FEATURE_UNAVAILABLE` for all 12 entitlements regardless of claimed plan or auth state, matching today's real product state exactly.
- **Major, honest architectural finding, not worked around**: Codivio has no public/customer account system — only Admin accounts exist. This makes real per-user entitlements/subscriptions impossible to model meaningfully today; a `user_entitlements`/`subscriptions` table was deliberately **not** created (would be schema with no way to be genuinely populated). Documented as a HIGH-importance finding for a future phase, not silently worked around with a fake table.
- **D1 migration 0007** adds `plans`/`plan_entitlements` (business-configurable catalog data, no customer dependency) and extends `tools` with 9 monetization columns, all honestly defaulted — verified structurally against a real SQLite engine, both standalone and as the updated `database/schema.sql` snapshot. No `usage_events` table either — no real usage exists yet to store.
- **Added `/pricing`** (the 10th real static page) reusing the existing SEO/schema/i18n architecture exactly — plain `WebPage` schema (no `Product`/`Offer`/`AggregateRating`), a disabled "Coming soon" upgrade button on every plan card, never a real or simulated purchase flow.
- **No new admin UI** — the existing `/admin/monetization` "coming soon" placeholder is untouched and still accurate; a real plan-management editor is deferred, per this phase's own "do not build an enormous admin system" instruction.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 259/259 (250 pre-existing + 9 new). `npm run build` — PASS, main chunk 444.41 kB / 129.95 kB gzip (vs. Phase 3.13's 438.05 kB / 128.20 kB gzip baseline — a modest, non-significant +6.36 kB raw / +1.75 kB gzip increase), lazy ToolPage chunk unchanged at 92.88 kB / 22.66 kB gzip. Not committed/pushed/deployed, per this phase's own explicit instruction.

## Phase 3.14 Remediation — Findings #1–#4

- **Checked 5 newly-added reference documents first** (`CODIVIO_MASTER_PLAN.md`, `ARCHITECTURE.md`, `SEO_STRATEGY.md`, `DATABASE_SCHEMA.md`, `DEPLOYMENT.md`), per instruction — no conflict found with actual code/tests/Git/production state.
- **Finding #1 (customer accounts) fixed**: new `customer_accounts` D1 table (migration 0008), the minimal identity anchor only (`id`/`email`/`status`/timestamps) — verified structurally and by test to have zero foreign-key or code-path connection to the Admin `users`/`sessions`/`audit_logs` tables or `worker/auth.ts`/`worker/rbac.ts`. Deliberately still no `customer_plan_assignments` or `subscriptions` table (no registration mechanism exists yet to populate real rows) — same "don't build schema with no way to be populated" reasoning already applied to migration 0007's deferred tables.
- **Finding #2 (pricing localization) fixed**: every user-visible `/pricing` string now reads from a new `pricing` namespace in the existing `shared/i18n` dictionaries (no second translation system) — verified non-empty and genuinely distinct per language, with plan/entitlement key-set parity against `shared/monetization` enforced by a test.
- **Finding #3 (funnel analytics) fixed**: `shared/monetization/funnel-events.ts` defines the 4 requested event names as a pure type/catalog — no firing, recording, or fabricated event anywhere, since no analytics sink exists yet to send one to (confirmed by grep before writing this).
- **Finding #4 (test typecheck) fixed**: new `tsconfig.test.json` (Option B — a separate config, not added to the main `tsconfig.json`) + `npm run typecheck:tests` script, fully decoupled from `npm run build`'s `tsc -b` step so a test-file type error can never break the production build. Required adding `@types/node` (dev-only, zero runtime/bundle impact) and fixing 2 small, genuine, pre-existing type errors in `tests/seo-schema.test.ts` that had never been caught before test files were type-checked.
- **No subscriptions, billing, or payment code was added** — consistent with this remediation's own explicit scope limit.
- **Test status:** `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS (new). `npm test` — PASS, 265/265 (259 pre-existing + 6 new). `npm run build` — PASS, main chunk 451.52 kB / 132.24 kB gzip (vs. the pre-remediation Phase 3.14 baseline of 444.41 kB / 129.95 kB gzip — a modest, non-significant +7.11 kB raw / +2.29 kB gzip increase from the new pricing translations), lazy ToolPage chunk unchanged at 92.88 kB / 22.66 kB gzip. Not committed/pushed/deployed, per this remediation's own explicit instruction.

## Phase 3.13 + 3.14 + Remediation — Release, Commit, Push, Deploy

- **Explicit, user-approved controlled release** covering the reviewed Phase 3.13, Phase 3.14, and Phase 3.14 remediation work together in one commit.
- **Commit `84e656b`** ("feat: release phase 3 seo and monetization foundation"): 38 files, staged explicitly by path. Excluded `tsconfig.tsbuildinfo`, the 4 pre-existing unrelated WIP files, and 5 newly-discovered reference documents left untracked (not part of this session's deliverables).
- **Pre-release gates**: `npm test` (265/265), `npm run typecheck`, `npm run typecheck:tests`, `npm run build` (451.52 kB / 132.24 kB gzip main, matching the reviewed baseline exactly) all run fresh, all PASS, before staging; the repository's own pre-commit/pre-push hooks independently re-verified and passed.
- **Database safety**: verified migrations 0007/0008 present and in order, `database/schema.sql` consistent, no destructive/payment/subscription table anywhere, `customer_accounts` structurally separate from `users` — all re-confirmed against a real SQLite engine immediately before commit. **Migrations were NOT applied to production D1** — included in the deployed commit as files only; applying them is a separate, explicit Change-Control action, and nothing in the deployed code reads/writes the new tables yet.
- **Pushed** `origin/main` from `4c558a6` to `84e656b`.
- **Deployed** via `wrangler deploy` — Cloudflare Version ID `d6cbb2e1-05c0-498f-9062-03d9931634b5`. No DNS, binding, or secret changed.
- **Live-verified**: homepage, `/tools`, `/pricing`, a representative tool page, `/admin/login`, `/admin`, `/robots.txt`, `/sitemap.xml` all 200; `/api/health` connected; `/api/auth/session` and `/api/admin/pages` correctly 401 unauthenticated; all 4 security headers present; sitemap serves real XML with 10 correct URLs; the live JS bundle (byte-identical to the reviewed local build) confirmed to contain the EN/AZ/TR pricing content and the honest "Coming soon"/"price to be announced" state, with zero checkout/buy-now language.
- Phase 3.15 was explicitly not started.

## Phase 3.14 — Production D1 Migration Consistency Fix

- **Determined the project's intended D1 migration policy from documentation, not assumption**: `DATABASE_SCHEMA.md` §2 requires `database/schema.sql` to stay "synchronized with the migration end state", and this project's own established precedent (every migration 0001–0006 was applied to production as part of its own release) confirmed production D1 is intended to track the committed migration chain, not remain indefinitely pending. No CI/CD pipeline and no build/deploy script applies migrations automatically — this is, and remains, a deliberate, manual, controlled step.
- **Inspected real production D1 state via Wrangler's own read-only mechanism** (`wrangler d1 migrations list codivio --remote`) before acting: confirmed migrations 0001–0006 already applied, 0007/0008 genuinely pending — matching exactly what was expected.
- **Re-verified both pending migrations were purely additive/idempotent** (no `DROP`/`DELETE`, only `CREATE TABLE IF NOT EXISTS`, `ALTER TABLE ADD COLUMN` with defaults, `INSERT OR IGNORE`, one scoped idempotent `UPDATE`) before applying anything.
- **Applied migrations 0007 and 0008 to production D1** via the controlled `wrangler d1 migrations apply codivio --remote` mechanism — both now show ✅ applied; `wrangler d1 migrations list` confirms zero pending migrations remain.
- **Verified the real post-migration schema and data, read-only**: `plans` (4 rows, every price genuinely NULL), `plan_entitlements` (12 rows), `customer_accounts` (0 rows — no fabricated customer data), `tools`' 9 new columns present with honest values, existing `tools` (34) and `users` (1) counts unchanged, no unexpected table created.
- **No application source code was changed** — confirmed no regression via `/api/health` (still `database: connected`), homepage/`/tools` (still 200), and the one D1-`tools`-reading endpoint `/api/admin/tools` (still correctly 401 unauthenticated, proving the schema change didn't break it).
- **Established the missing deployment rule** (`DEPLOYMENT.md` §7.1): every release introducing a required D1 migration must apply it via the controlled Wrangler mechanism as an explicit step, verify the resulting state, and record it — never via automatic application-startup schema mutation.
- No customer login/registration, billing, subscriptions, or monetization-architecture change was made — this was a database-consistency fix only.
- Not a new application deploy — production code/Version ID (`84e656b` / `d6cbb2e1-05c0-498f-9062-03d9931634b5`) is unchanged; only the production D1 schema state changed.

## FAQ Expansion + AI Discoverability Enhancement (pre-Phase-3.15)

- **Expanded every tool's FAQ from Phase 3.4's original 2 questions to 6**, genuinely tool-specific (not a copy-pasted template) — covering definition/format/technical detail, a real use case, an honest related-tool cross-reference, and an explicit "is this available right now?" status question, in AZ/TR/EN for all 34 tools (612 new question/answer pairs total). Reused the existing `shared/seo/content.ts#TOOL_CONTENT` structure — no second FAQ dataset, no `faq.ts`/`faq-data.ts` created.
- **A real content-accuracy bug was found and fixed before release, not just before merge**: several cross-tool references (e.g. "The Phone QR Code tool...") used a shortened name that didn't exactly match the tool's real registered title ("Phone QR Code **Generator**"). Caught by a new test that checks every "The X tool is designed for..." reference against the real `getToolDisplayName` output — 16 such mismatches found and corrected across QR/PDF/Image tools before this was ever shipped.
- **Added `getToolFaqs(slug, lang)`** to `shared/seo/content.ts` — a lightweight, deterministic FAQ-only accessor for the existing AI-answer architecture, reusing the same `TOOL_CONTENT` data (no duplication).
- **No FAQPage schema was added** — Phase 3.7's deferral stands, per this task's own explicit instruction.
- **FAQ UX upgraded from plain stacked text to native `<details>/<summary>`** (matching the site's existing homepage FAQ visual pattern) — real keyboard accessibility and scannability now that each tool has 6 items instead of 2, no new JS interaction code needed.
- **Bundle discipline verified, not assumed**: the FAQ expansion added real weight (34 tools × 3 languages × 4 new Q&A pairs), but because it lives inside `shared/seo/content.ts` — already isolated into `ToolPage`'s own lazy-loaded chunk since Phase 3.13 — the **main bundle measured byte-identical** before and after (451.52 kB / 132.24 kB gzip). The `ToolPage` lazy chunk grew from 92.88 kB / 22.66 kB gzip to 163.22 kB / 36.34 kB gzip, entirely within the route-specific chunk, never the critical-path bundle.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 270/270 (265 pre-existing + 1 updated count assertion + 5 new). `npm run build` — PASS.

## Phase 3 Finalization — Global FAQ + Admin FAQ Management + Route Scroll Fix

- **Fresh repo inspection (not memory) found three real gaps**: the public `/faq` page and Homepage FAQ preview were reading a hardcoded, English-only 6-question array; there was no Admin FAQ Management at all (no route, no Worker endpoint, no nav item); and there was no route scroll restoration (react-router-dom's plain `<Routes>` does not reset/restore scroll itself).
- **Global FAQ**: expanded to a real, honest 12-question AZ/TR/EN FAQ in a new `shared/seo/global-faq.ts` (`getGlobalFaqs(lang)`, static/deterministic, mirrors `getToolFaqs`); `FAQPage` and the Homepage FAQ preview now read it, and the old hardcoded array was removed.
- **Admin FAQ Management added**: new `site_faqs` D1 table (`migrations/0009_faq_management.sql`, seeded with the real global FAQ content, supports both `scope="global"` and `scope="tool"` with tool-slug validated against the real registry), new `faq.view`/`faq.manage` RBAC permissions (mirroring the `tools.*` matrix exactly — no privilege escalation for any role), full CRUD in `worker/faq.ts` (mirrors `worker/tools.ts`, audit-logged), and `src/admin/AdminFaqPage.tsx` (list + create/edit UI, reuses the existing responsive admin-table CSS — no new styles). Like Tools/Pages Management, this is a real Admin-managed configuration surface the public site does not read from yet.
- **Route scroll restoration added**: a new `ScrollRestoration` component resets scroll to top on a normal navigation, restores the saved position on Back/Forward, and scrolls a hash target into view when present — `history.scrollRestoration` set to `"manual"` so it doesn't fight the browser's own behavior.
- **Security review performed** (via the `security-review` skill's methodology) on the new Worker/RBAC/DB surface: no high-confidence vulnerabilities found — parameterized queries throughout, authorization before every DB access, no mass-assignment path, tool-slug references validated before being persisted.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 284/284 (270 pre-existing + 2 admin-nav-count assertions updated 16→17 + 12 new). `npm run build` — PASS.
- **Bundle impact (measured):** main chunk 451.52 kB/132.24 kB gzip → 467.29 kB/136.33 kB gzip (+15.77 kB raw / +4.09 kB gzip) — Admin pages aren't lazy-loaded in this app, so the new Admin FAQ page and expanded global FAQ content land in the main bundle; a real, proportionate cost of new site-wide functionality. `ToolPage` lazy chunk unchanged (163.22 kB/36.34 kB gzip).
- `59677c5` — committed, pushed (`b3536d5` → `59677c5`), migration `0009_faq_management.sql` applied to production D1 (`wrangler d1 migrations apply codivio --remote`, ✅), deployed (Cloudflare Version ID `42699b6f-6d62-4743-8e71-c7ccd29e3e77`). Live-verified: `/`, `/faq`, `/admin/faq` all 200; `/api/health` connected; `/api/admin/faqs` correctly 401 unauthenticated; the live main bundle confirmed by direct grep to contain the new FAQ content and scroll-restoration code.

## FAQ Management Source-of-Truth Remediation

- **Real gap found**: the release above made Admin FAQ Management write to `site_faqs`, but the public `/faq` page and Homepage FAQ preview still read `shared/seo/global-faq.ts`'s static content — Admin create/edit/deactivate/reorder had no effect on what visitors saw.
- **Fixed with a new public, unauthenticated `GET /api/faqs?language=xx`** (`worker/faq.ts#handlePublicFaqs`) returning only `question`/`answer` for `scope='global' AND status='active'` rows in the requested language, ordered by `sort_order` then `id` — never exposes admin-only fields, inactive rows, or tool-scoped entries.
- **New `useGlobalFaqs(language)` hook** (`src/App.tsx`) used by both `HomePage` and `FAQPage`: renders the static fallback immediately (never blank), then swaps in the live API result — and silently keeps the fallback if the fetch fails, so a transient D1 outage never blanks the public FAQ page.
- `shared/seo/global-faq.ts` re-documented as fallback-only — Admin (`/admin/faq`) is now the actual place to change what visitors see; the static file is kept content-identical to the D1 seed on purpose so it never becomes a second, silently-diverging FAQ source.
- **No new migration** — `site_faqs` (migration 0009) already had everything this needed; only a new read query was added.
- **A second, independent security review** of the new public route found no high-confidence vulnerabilities: allowlisted/parameterized `language` input, hardcoded `scope`/`status` filter (not attacker-controlled), response shape limited to `question`/`answer`, no shadowing of the admin routes' auth.
- **New tests**: `tests/faq-source-of-truth.test.ts` (6 cases, against a real Worker-handler + extended `FakeD1` harness) proving the full admin-write → public-read data flow: active/global-only reads, inactive exclusion, language filtering, `sortOrder`-driven reordering, a full create→edit→deactivate cycle observed through the public endpoint, and 400 on a missing/invalid language.
- **A real, self-caught regression was found and fixed while extending the test harness**: `tests/helpers/fake-d1.ts`'s mock `.run()`/`.all()` no longer satisfied `worker/types.ts#D1Result` after that type's earlier `meta` field addition — `npm run typecheck:tests` was silently broken (not caught by `npm run typecheck`/`npm test`, which don't cover `tests/`). Fixed by adding real `meta.last_row_id` tracking to the mock.
- **Test status:** `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS (was broken, now fixed — see above). `npm test` — PASS, 290/290 (284 pre-existing + 6 new). `npm run build` — PASS.
- **Bundle impact (measured):** main chunk 467.29 kB/136.33 kB gzip → 467.57 kB/136.46 kB gzip (+0.28 kB raw / +0.13 kB gzip) — negligible, as expected for one small hook plus one fetch call.
- `f48b9f8` — committed, pushed (`59677c5` → `f48b9f8`), no new migration (0009 already covered `site_faqs`), deployed (Cloudflare Version ID `341ff54f-4666-4eb0-9405-9639eae9cf11`). Live-verified: `/faq` → 200; `/api/health` connected; `/api/admin/faqs` → 401; `GET /api/faqs?language={en,az,tr}` each return exactly 12 real items — conclusively confirming migration 0009's seed data is live in production (resolves the prior handoff's unverified row-count item). No authenticated Admin UI testing performed against production (no credentials available in this environment) — the admin-write → public-read flow is proven by `tests/faq-source-of-truth.test.ts` instead.

## Phase 3.15 — Professional SEO + AI SEO Master Audit

- **Audit-only checkpoint — zero application code changed, no commit, no deploy.** Full 50-item handoff delivered; see `PROJECT_STATE.md`'s "Phase 3.15" entry for the summarized findings.
- **Overall result: DEGRADED.** No CRITICAL/security findings, no fabricated content. Real, live-evidenced gaps: a genuine **soft 404** (a nonexistent path returns HTTP 200, not 404 — the Worker's SPA fallback always serves `index.html` with 200); the **standing CSR pre-render metadata limitation already Change-Controlled in Phase 3.8** re-confirmed with fresh, concrete live evidence (raw HTML byte-identical across every route — wrong canonical, wrong robots, until JS hydrates); `www.codivio.online` returning **HTTP 525** (Cloudflare SSL handshake failure — DNS/zone config, no code fix possible from this repo); the Contact page's still-nonfunctional contact channel (standing, Phase 3.9); the 1.58 MB logo asset (standing, Phase 3.8) plus a new, low-severity observation that it isn't long-cached; no `<lastmod>` in `sitemap.xml` (real dates unknown, not fabricated); the tool-category label still untranslated in AZ/TR (standing, Phase 3.13).
- **External verification performed via WebSearch against current (2026) Google/Schema.org guidance** — confirmed the project's own standing decisions remain correct today, not stale: FAQ rich results were fully retired May 7, 2026 (feature-appearance only; `FAQPage` markup still valid; ranking unaffected) validates the no-FAQPage-schema decision; Google's June 2026 confirmation that `llms.txt` has zero ranking/AI Overview effect validates Codivio's own `llms.txt` disclaimer as accurate.
- GSC: DNS verification TXT record re-confirmed live; no API/dashboard access available — indexing/queries/performance all UNKNOWN, not inferred from HTTP 200. PSI API and browser automation: BLOCKED, unavailable in this environment.
- **No fixes were implemented** — every real finding needs either a Change Control architecture/infrastructure decision (soft 404, pre-render metadata, `www` DNS/SSL) or real business input this environment cannot supply (a real Contact channel) or would require fabricating unknown data (sitemap `lastmod`) — none of which this task is authorized or able to safely do unilaterally.
- Test/build status (fresh run, no code changed): `npm run typecheck` — PASS. `npm test` — PASS, 290/290 (unchanged). `npm run build` — PASS, byte-identical to the currently-deployed build.

## Phase 3.15 Remediation — Change Control & Root-Cause Investigation

- **Soft 404 — root cause confirmed, safely fixed, released.** `wrangler.jsonc`'s `not_found_handling: "single-page-application"` is Cloudflare's correct, documented setting for this SPA (verified via official docs) — but it meant *any* unmatched path, including a genuinely nonexistent page and an unmatched `/api/*` path, returned HTTP 200. New `worker/route-guard.ts` recognizes real pages using existing source-of-truth data (`PAGE_SEO`, `TOOL_SEO`, the same published-page D1 query `worker/pages.ts` already uses — no second route list) and corrects the status to 404 for a genuinely unknown page, while serving the identical SPA-shell body (client-side rendering fully unaffected). An unmatched `/api/*` path now gets a real JSON 404. `/admin/*` and asset paths with a file extension are explicitly excluded (documented reasoning in the new file). Independent security review found no high-confidence vulnerabilities.
- **New tests**: `tests/route-guard.test.ts` (9 cases) — unit-level coverage of the route-classification functions, plus full `worker/index.ts` `fetch()` integration tests proving the real HTTP status for known-good routes (unchanged, 200) and genuinely unknown ones (now 404), including `/admin/*` explicitly staying unchanged.
- **Test status:** `npm run typecheck` — PASS. `npm test` — PASS, 299/299 (290 pre-existing + 9 new). `npm run build` — PASS; client bundle byte-identical (Worker-only change); Worker's own bundle 75.97 kB → 112.29 kB (server-side, no client/visitor performance impact).
- `ccc6e93` — committed, pushed (`ee01255` → `ccc6e93`), no new migration, deployed (Cloudflare Version ID `01007806-462d-43af-a8aa-79f79ed28eb6`). **Live-verified, zero regressions**: `/nonexistent-page`, `/tools/not-a-real-tool`, `/foo/bar/baz`, `/api/totally-invalid-endpoint`, `/FAQ` (case mismatch) → all now 404; all 10 real static pages, a real tool page, trailing-slash variants, `/admin/login`, `/admin/some-unknown-subpage`, `/api/health`, `/api/faqs`, `robots.txt`, `sitemap.xml` → all still 200; `/api/admin/faqs` unauthenticated → still 401.
- **CSR pre-render metadata**: investigated further (5 options compared — see the Phase 3.15 remediation handoff), **not implemented** — recommended a narrow Worker-level `HTMLRewriter` metadata pass as the safest option if pursued, explicitly proposed as a Change Control item requiring separate approval, not built this task.
- **`www.codivio.online` (HTTP 525)**: root cause narrowed with real evidence — DNS correctly resolves `www` to the same Cloudflare IPs as the apex, and the client↔Cloudflare-edge TLS handshake succeeds; the 525 originates from Cloudflare's edge failing to reach an "origin" for that specific hostname, consistent with no Workers Route/Custom Domain being bound to `www` for the `codivio` Worker. The actual fix requires Cloudflare dashboard access unavailable in this environment — marked **UNVERIFIED — REQUIRES EXTERNAL/DASHBOARD CHECK**.
- **Contact/trust**: re-confirmed no real contact channel exists anywhere in the repo (standing Phase 3.9 finding) — nothing invented, nothing changed.
- **Logo optimization**: confirmed via direct PNG header inspection — 1254×1254px, 8-bit truecolor, 1,616,911 bytes. No image-processing tool available in this environment (Windows' `convert.exe` is an unrelated disk-format utility, not ImageMagick) — not modified; recommended a future dedicated task with a real, vetted image tool, or a separately-designed asset.
- **Sitemap `lastmod`**: investigated using git history — rejected, because all 10 static pages share one file (`src/App.tsx`), so a file-level git date would misrepresent per-page change history rather than genuinely inform it. A reasoned decision, not an open gap.
- **Addendum**: the "CSR pre-render metadata — not implemented" line above was accurate only at the moment this entry was written. Later in the same session, the recommended narrow Worker-level `HTMLRewriter` metadata pass **was** implemented, tested, committed, and pushed (`4fb8516`, `6f34bb0`, `d2000b9`) — this entry was not updated at the time to reflect that. See "Phase 3.15 HTMLRewriter Route Metadata + Critical Content Injection" below for the real, documented record of that work, added here now specifically to close that gap.

## Phase 3.15 HTMLRewriter Route Metadata + Critical Content Injection (2026-09-16, documented retroactively)

**Committed and pushed to `origin/main`; NOT deployed** (production remained, and remains, at `f48b9f8` at the time of this retroactive documentation pass). This is the Change-Control-approved follow-up to the "CSR pre-render metadata" finding from the Phase 3.15 Remediation entry above — three commits made in the same session but never previously recorded in this file.

- **`4fb8516` — "feat: prototype HTMLRewriter route SEO metadata injection".** New `worker/seo-rewrite.ts` + `worker/html-rewriter.d.ts` (ambient types, since this project deliberately avoids the `@cloudflare/workers-types` dependency): a narrow Worker-level rewrite of `<title>`/meta description/canonical/robots/OG-title/OG-description for known static and tool routes, reusing `PAGE_SEO`/`TOOL_SEO` as the source of truth, feature-detecting the real `HTMLRewriter` global and no-opping where it's absent (this project's own Vitest/Node test environment, unconditionally). Fixed `DEFAULT_LANGUAGE` used (no reliable per-request language signal exists yet — documented as a known limitation, not solved here).
- **`6f34bb0` — "feat: add JSON-LD and og:url/twitter metadata to HTMLRewriter SEO rewrite".** Extended the rewrite with route-specific JSON-LD (Organization/WebSite/WebPage/BreadcrumbList), built with the exact same `shared/seo/schema.ts` builders `src/App.tsx` already calls client-side and sharing one script id (`JSON_LD_SCRIPT_ID`, extracted to `shared/seo/schema.ts` so client and server agree on it) so client-side hydration updates the server-rendered tag in place rather than duplicating it. Also added `og:url`/`twitter:title`/`twitter:description`, reusing already-computed values. `og:image`/`twitter:image`/`og:type`/`twitter:card` deliberately left untouched (already-correct sitewide defaults, no per-page image exists).
- **`d2000b9` — "SEO: inject critical H1 and intro content".** Extended the rewrite further to inject a real, visible `<h1>` + one intro paragraph into `#root` for known static/tool routes, using only existing `PAGE_SEO`/`TOOL_SEO`/content-blueprint data. Safe by construction because `src/main.tsx` uses `createRoot()`, not `hydrateRoot()` — React replaces `#root`'s children on mount rather than reconciling against them, so there is no hydration-mismatch risk. New, narrowly-scoped `shared/seo/tool-intro.ts` extracts just the `introduction` field out of the ~1,900-line `shared/seo/content.ts#TOOL_CONTENT` dataset specifically to keep that much larger dataset out of the Worker bundle. Also added the WebP upload policy to `DECISIONS.md` (see that file's "Image Upload Standard" entry) — no upload system exists, so nothing was implemented, only the standing requirement recorded for Phase 6/8/Media.
- **Explicitly out of scope for all three commits, unchanged**: FAQPage schema, CMS pages, real per-language resolution (still fixed `DEFAULT_LANGUAGE`), `www` redirection, full SSR/SSG/prerendering.
- **Test status at the time**: all three commits' own tests passed (`tests/seo-rewrite.test.ts` grew across all three); the final state before Phase 3.15-A began was verified again as part of that sub-phase's own baseline check.
- **Deployment status**: **NOT deployed.** No `wrangler deploy` was run for any of these three commits — production remained on `f48b9f8` throughout, and still is as of this retroactive documentation pass.

## Phase 3.15-A/B/C — Admin SEO Preview, RBAC, and Override Architecture

**Implemented, tested, and committed in one combined commit** (this entry is being written and committed together with that same combined commit; see `PROJECT_STATE.md`'s "Phase 3.15-A/B/C" section for full detail). **Migration 0010 has NOT been applied to any database. NOT deployed** — production remains at `f48b9f8`, unaffected.

**Implemented this sub-phase group:**
- Admin SEO Google Search Preview (desktop + mobile mockup) and color-coded title/description length counters (3.15-A), reusing the existing `shared/seo/duplicates.ts` length-guidance thresholds.
- `seo.view`/`seo.manage` RBAC permissions (3.15-B), granted to `admin`+`editor` only, matching the `pages.*`/`tools.*`/`faq.*` pattern exactly; an inline permission gate on `AdminSeoPage.tsx` (necessary since this page previously had no backend call to protect it).
- A full SEO Override Architecture (3.15-C): new `seo_overrides` D1 table (migration 0010, **file created, not applied**), `shared/seo-overrides.ts` validation, `worker/seo-overrides.ts` CRUD (`seo.view`/`seo.manage`-gated) plus `fetchActiveOverride` (the function the public rendering path calls), audit logging (`SEO_OVERRIDE_CREATED`/`UPDATED`/`DELETED`), and a real Admin UI edit/reset control. The public rendering path (`worker/seo-rewrite.ts`) now looks up an active override before falling back to the compile-time default, with the D1 lookup wrapped in try/catch so any failure is indistinguishable from "no override" — the existing, already-tested default rendering is the guaranteed floor.

**Deferred / explicitly not implemented this sub-phase group** (see `DECISIONS.md`'s three new Phase 3.15-A/3.15-B/3.15-C entries for the full reasoning on each):
- A caching layer for the new D1 override lookup.
- Extending an override's effect to the injected H1/intro paragraph or the JSON-LD `name`/`description` — these always render the compile-time default regardless of an active override, by design, this sub-phase.
- Any change to `tools.seo_title`/`seo_description` (still confirmed disconnected from public rendering — not this sub-phase's concern to fix).
- Migration application (local or production D1) and deployment of any kind.

**Documentation synchronization performed as part of this same commit**: corrected a stale `PROJECT_STATE.md` line that read "3.15 ... zero code changes made" (inaccurate — see the new HTMLRewriter entry above); added the three new `DECISIONS.md` entries for 3.15-A/B/C; corrected a stale comment in `shared/rbac.ts` that said `seo.manage` had no enforcing code path (3.15-C added one).

- Test status: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, 334/334. `npm run build` — PASS (Worker 228.46 kB / gzip 61.08 kB; client CSS 36.90 kB / gzip 7.11 kB; `ToolPage` chunk unchanged at 166.18 kB; client main JS 473.91 kB / gzip 137.97 kB).
- Migration status: **NOT APPLIED** (file only).
- Deployment status: **NOT DEPLOYED.**

## Phase 3.15 Production Readiness Audit (2026-09-17)

**Audit only — no code, documentation, migration, or deployment changed.** Full read of `CLAUDE.md`/`PROJECT_STATE.md`/`DECISIONS.md`/`SECURITY_BASELINE.md`/`DEPLOYMENT.md`, plus a fresh, full-file read of `worker/seo-rewrite.ts`, `worker/seo-overrides.ts`, `shared/seo-overrides.ts`, `worker/index.ts`, and `worker/route-guard.ts`.

- Confirmed git state (HEAD `4e0a450`, local/remote synchronized, 8 commits since production `f48b9f8`, unrelated WIP files untouched) and re-ran `typecheck`/`typecheck:tests`/`test`/`build` fresh — all PASS, 334/334, byte-identical build output.
- Security review of the combined 3.15-A/B/C surface found no SQL injection, mass-assignment, privilege-escalation, or XSS defect. Confirmed the new `seo_overrides` D1 lookup (`fetchActiveOverride`) fails safe (any D1 error is indistinguishable from "no override," falling back to the compile-time default).
- **New finding (LOW), not previously documented**: `worker/route-guard.ts#isPublishedCmsSlug` and `worker/pages.ts`'s D1 queries have no try/catch — a D1 outage while serving a CMS-page-shaped path (not one of the 9 static pages/34 tools) would surface as an unhandled exception rather than a graceful fallback. Pre-existing (in the already-shipped soft-404 code, not introduced by 3.15-C) — flagged for a future hardening task, not fixed here.
- **New finding (LOW)**: deploying 3.15-C's code before applying migration 0010 would make the Admin SEO override list/save/reset show an empty/error state rather than crash (confirmed `AdminSeoPage.tsx`'s fetch/save/reset calls all have their own `.catch()`) — recommended applying the migration in the same release window as the code, per `DEPLOYMENT.md` §7.1's own sequence.
- Migration 0010 structurally reviewed again: purely additive, idempotent, reversible; `database/schema.sql`'s block confirmed identical (aside from comments) to the migration file.
- Result: **READY WITH CONDITIONS** — no BLOCKER/HIGH finding; conditions were migration-before-deploy sequencing and the two standing MEDIUM findings (`www` DNS/SSL, Contact channel), both requiring input outside this environment.

## Phase 3.15 Production Release (2026-09-17)

**Migration applied, code deployed, live-verified — Phase 3 closed.** Explicitly authorized by the user for this task, following the audit above. Full detail in `PROJECT_STATE.md`'s "Phase 3.15 Production Release" section; summary here:

- **Migration**: `npx wrangler d1 migrations apply codivio --remote` applied `0010_seo_overrides.sql` (4 SQL commands, ✅). `npx wrangler d1 migrations list codivio --remote` confirmed zero pending migrations afterward. Direct query (`SELECT COUNT(*) FROM seo_overrides`) confirmed the table exists with 0 rows.
- **Deploy**: `npx wrangler deploy` — Cloudflare Version ID `780d5f46-45cb-47e2-8cec-14607eb015ad`, deploying commit `4e0a450` (code content identical to `8063302`, the last code-bearing commit; `4e0a450` itself is docs-only).
- **Live-verified via real HTTP requests against `https://codivio.online`**: homepage/`/faq`/`/tools/qr-code-generator` all 200; a genuinely nonexistent path 404; `/api/health` connected; `/api/admin/seo-overrides` (and `/:id`) unauthenticated both 401; `/api/admin/pages` unauthenticated still 401 (regression check); `/admin/seo` 200; an unmatched `/api/*` path 404; all 6 security headers present on `/`; `robots.txt`/`sitemap.xml` both 200. **Directly inspected raw HTML** confirming the HTMLRewriter work is genuinely live: `/faq`'s response contains the correct route-specific `<title>`, meta description, `robots` (`index,follow`), canonical, a valid parseable JSON-LD graph (`id="codivio-jsonld"`), OG/Twitter tags, and a real injected `<h1>`+`<p>` inside `#root`; `/tools/qr-code-generator` correctly shows `noindex,follow` and its own tool-specific title/JSON-LD.
- **Not verified, stated honestly**: the authenticated Admin SEO override CRUD flow was not exercised live (no production admin credentials available in this environment, the same standing limitation as every prior Phase 3 release) — proven instead by `tests/seo-overrides.test.ts`'s real-handler test harness.
- **Deferred by explicit user decision, not part of this release's success criteria**: `www.codivio.online` HTTP 525 (Cloudflare dashboard task); the Contact page's channel (pending real business input); the D1-outage error-handling gap in `route-guard.ts`/`pages.ts` (future hardening task).
- Test/build re-confirmed immediately before this release: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, 334/334. `npm run build` — PASS.
- **Phase 3 is now CLOSED.** Phase 4 (QR Tools) was not started.

## Pre-Phase 4 UI/UX and Accessibility Remediation Checkpoint (2026-09-17)

**Implemented, committed, and pushed — NOT deployed.** Not a numbered roadmap phase; sits between the closed Phase 3 and the not-yet-started Phase 4. Investigated 7 user-provided responsive/accessibility hypotheses directly against source code (no browser/screenshot tool available in this environment — each finding classified honestly, including two left as UNKNOWN pending real rendering verification), then implemented the 5 confirmed-safe fixes:

- Fixed a real CSS-specificity bug (`.admin-tools-table .admin-pages-row`'s unconditional 8-column desktop grid was more specific than the intended `≤650px` single-column override) that prevented the Admin Tools list from collapsing on mobile — added a matching-specificity mobile override.
- Removed an oversized mobile `.hero-grid` `min-height:550px` override (higher than desktop's 510px despite less mobile content) — mobile now falls back to the already-live 510px tablet value.
- Added a modest `≤650px` reduction to `.section`/`.content-section`/`.tool-slider-section` spacing (no prior responsive reduction existed at any breakpoint).
- Added matching `id`/`aria-controls` pairs, `Escape`-key close, and a body-scroll lock to both the public and Admin mobile navigation toggles (none of the three existed before, confirmed by a full-file grep). Existing Admin sidebar click-outside-to-close preserved.
- Added a `.faq summary:focus-visible` style matching the existing tool-page FAQ pattern.

Deferred (require real browser/screenshot verification): tool-card height/width proportions on mobile; `.section-heading`'s lack of `flex-wrap` and its real-world visual severity at narrow widths with AZ/TR/EN string lengths. Untouched, per explicit scope: CSR/SSR architecture, SEO/AI-discoverability architecture, the Contact form, Cloudflare configuration, and all Phase 4/QR functionality.

- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, 334/334. `npm run build` — PASS (Worker unchanged 228.34 kB; client CSS 36.90→37.13 kB; client main JS 473.91→474.58 kB; `ToolPage` chunk unchanged).
- **Git**: commit `416b93c` ("fix: improve responsive UI and mobile navigation accessibility"), pushed to `origin/main` (`7916a1d..416b93c`). **Not deployed at the time of this entry** — production remained at `7916a1d` / Cloudflare Version ID `780d5f46-45cb-47e2-8cec-14607eb015ad`. See the following entry for the subsequent deployment. Phase 4 was not started.

## Pre-Phase 4 UI/UX Checkpoint — Production Deployment (2026-09-17)

**DEPLOYED and VERIFIED at source-level/HTTP-level.** Deployed commit `32380c4` (code content identical to `416b93c`) via `npx wrangler deploy` — **Cloudflare Version ID `ffb3f339-c65b-4bfe-a75d-19f03dd83ef8`**, production URL `https://codivio.online`. `npm run build` — PASS immediately before deployment, byte-identical to the previously validated build. Post-deployment verification used real HTTP requests (homepage/`/faq`/`/tools/qr-code-generator`/`/admin/login` all 200; a genuinely nonexistent path still 404; `/api/health` connected; `/api/admin/seo-overrides` unauthenticated still 401; security headers present) **plus direct byte-for-byte comparison of the live-fetched JS/CSS bundles against the local build** — both identical, with the live CSS bundle confirmed to contain the new Admin Tools mobile-grid fix and confirmed to no longer contain the removed `.hero-grid{min-height:550px}` override. **Visual/browser rendering remains `UNKNOWN — NOT VERIFIED`** — no browser or screenshot tool is available in this environment; every claim above is source/HTTP/bundle-level evidence only. No database, authentication, RBAC, SEO, or Cloudflare configuration change was part of this deployment. **Phase 4 remains NOT STARTED** — next roadmap step is Phase 4.1 — Shared QR Engine.

## Global Mobile Responsive Fixes — Investigation, Fix, and Deployment (2026-09-17)

**Implemented, committed, pushed, and deployed.** Follow-up to the Pre-Phase 4 UI/UX checkpoint after the reported mobile symptom was clarified as sitewide, not confined to one page. `src/styles.css` only, no other file changed.

- **Arrow clearance fix**: `.tool-card p`/`.tool-slider-card p`'s mobile `max-width:none` (from the prior checkpoint) was refined to `max-width:calc(100% - 44px)`, computed from the real arrow icon footprint (18px offset + up to 18px icon width, plus an ~8px safety margin) — prevents wrapped description text from ever reaching under the absolutely-positioned arrow icon, while still eliminating most of the original wasted mobile space. Affects every page rendering `ToolCard` (Homepage's 4 sliders, the `/tools` listing page's 4 category grids).
- **CSS Grid defensive fix**: `.tool-grid` and `.tool-slider`'s mobile rule switched from bare `1fr`/`repeat(N,1fr)` to `minmax(0,1fr)`, matching the convention already used by every other grid in the codebase (`.blog-page-grid`, `.about-grid`, `.pricing-grid`) — prevents a CSS Grid track from being forced wider than its container by an unshrinkable child. Zero behavioral change for any content that already fits.
- Checked and ruled out (not bugs): `flex:0 0 100%` vs. box-sizing (no conflict, `border-box` is global); any parent `overflow:hidden` clipping text (none found anywhere in public components); a separate `/faq` routing/rendering defect (none found — `/faq` renders normally via `PageShell`, shares the same `.faq` class as the Homepage's embedded FAQ preview).
- Test/build: `npm run typecheck` — PASS. `npm test` — PASS, 334/334. `npm run build` — PASS (Worker unchanged; client CSS 37.13→37.29 kB; main JS unchanged).
- **Git**: commit `4bc370b` ("fix: finalize global mobile responsive layout"), pushed to `origin/main` (`9d5908f..4bc370b`).
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `b95f78ab-928a-405b-a8cf-ba48de447084`**. Post-deployment: homepage/`/faq`/`/tools/qr-code-generator`/`/admin/login` all 200, `/api/health` connected, all 6 security headers present, live CSS bundle confirmed byte-for-byte identical (37,293 bytes) to the local build containing both new rules.
- **Visual/browser rendering remains `UNKNOWN — NOT VERIFIED`** across this entire checkpoint — no browser or screenshot tool was available at any point. Phase 4 was not started.

## Phase 4.1 — Shared QR Engine (2026-09-17)

**Implemented, committed, and pushed — NOT deployed, no UI wired to it.** Foundation-only sub-phase for future QR tool pages (Generator, URL/Text/WiFi/vCard/Email/SMS/WhatsApp/Phone/Location/Calendar QR); does not itself build any tool page or UI.

- Added `qrcode` (MIT) as the project's first runtime npm dependency beyond React/react-router/lucide-react, plus `@types/qrcode` (devDependency) — verified via `npm view` (v1.5.4, published 2025-11-13, actively maintained) and `npm install` (0 vulnerabilities).
- New pure, framework-agnostic module `shared/qr/{types,validate,index}.ts` (types + validation, mirrors the existing `shared/seo/` pattern, importable by both Worker and frontend) and a new browser-only wrapper `src/lib/qr-engine.ts` (the only file importing `qrcode`, since the Worker has no Canvas API and imports neither this file nor exposes a QR endpoint).
- Public API: `generateQrCode(payload, config?, format?)` — validates via `validateQrRequest` (collects all errors at once) before rendering, returns a tagged `{format, data}` result (`png-data-url` or `svg`), throws a typed `QrGenerationError` on invalid input. Scope this phase: generic text/URL payload only (`QrTextPayload`) — no vCard/WiFi/Email/SMS/etc. payload builders, no logo embedding, no download UI, no `ToolPage.tsx` wiring.
- Validation limits: payload non-empty, ≤2,000 chars (soft app-level cap); size 16–2048px; margin 0–20 modules; colors strict hex only.
- New test file `tests/qr-engine.test.ts` (6 cases: valid default generation, empty/over-length rejection with multi-error collection, config defaulting/per-field validation, real PNG-data-URL/SVG output structurally verified, invalid-input rejection, determinism).
- Architectural decision recorded in `DECISIONS.md` ("Shared QR Engine — First Runtime Dependency").
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **340/340** (334 pre-existing + 6 new). `npm run build` — PASS, client main JS **unchanged** (474.58 kB / gzip 138.19 kB) — measured (not estimated) 0 KB bundle impact, confirmed by grepping the built JS for `"qrcode"` (zero matches), since nothing yet imports `src/lib/qr-engine.ts` from the running app.
- **Git**: commit `b51abd7` ("feat: add shared QR engine foundation"), pushed to `origin/main` (`c2850f9..b51abd7`).
- **Not deployed.** Production remains at commit `4bc370b` / Cloudflare Version ID `b95f78ab-928a-405b-a8cf-ba48de447084`. Mobile carousel, homepage, Admin UI, SEO, and auth were not touched. Phase 4.2 (QR Generator UI and the rest of Phase 4) was not started.

## Phase 4.1 — Production Deployment (2026-09-18)

**DEPLOYED.** Deployed commit `b51abd7` (code content identical to the pushed implementation; the intervening `3991eb6` docs-only commit changed no app code) via `npx wrangler deploy` — **Cloudflare Version ID `6080b0cc-2336-4873-8512-c4bfbc70d6f5`**, production URL `https://codivio.online` (Worker URL `https://codivio.azbloknot.workers.dev`). Wrangler reported "No updated asset files to upload" — expected, since Phase 4.1 added no app-reachable code, so the client bundle is byte-identical to the previously deployed release; only the Worker script itself was re-uploaded.

- **Pre-deployment validation**: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm run build` — PASS (output unchanged from Phase 4.1's implementation build). `npm test` — one run showed 2 intermittent failures in `tests/auth.test.ts` (a login-lockout test sensitive to real wall-clock timing under full-suite parallel load); investigated and found unrelated to this checkpoint (neither `worker/auth.ts` nor `tests/auth.test.ts` was touched) — the file alone passed 20/20, and a full-suite re-run passed **340/340**; deployment proceeded on that clean re-run. Not fixed, per that task's explicit no-unrelated-fixes scope.
- **Post-deployment smoke test (real HTTP requests against `https://codivio.online`)**: homepage, `/faq`, `/tools`, `/tools/qr-code-generator`, `/admin/login` all `200`; a genuinely nonexistent path `404`; `/api/health` connected; unauthenticated `/api/admin/pages` still `401`; all 6 security headers present on `/`. **Deployed-build correspondence directly confirmed**: the live JS (`index-SjR0-SdH.js`) and CSS (`index-x4Iv9mnB.css`) bundles were fetched and diffed byte-for-byte against the local build output — both identical; the live JS bundle grepped for `"qrcode"` returned 0 matches, confirming the new dependency is genuinely unreachable from the live app.
- **Visual/browser rendering remains `UNKNOWN — NOT VERIFIED`** — no browser or screenshot tool is available in this environment, and no UI consumes the QR engine yet, so there is nothing user-visible to check regardless.
- No database, authentication, RBAC, SEO, or mobile-carousel change was part of this deployment. Phase 4.2 was not started.

## Phase 4.2 — QR Code Generator (2026-09-18)

**Implemented, committed, pushed, and DEPLOYED.** The first functional tool UI on the platform, built entirely on the unmodified Phase 4.1 shared engine.

- `src/pages/ToolPage.tsx` now branches only for `slug === "qr-code-generator"`, lazily loading a new `src/tools/QrCodeGeneratorTool.tsx` (its own dynamic-import chunk, separate from the already-lazy `ToolPage` chunk) instead of the generic "Tool coming soon" placeholder — every other tool's render path is byte-for-byte unchanged. This keeps the `qrcode` dependency out of the other 33 tool pages and the main bundle; it loads only for a visitor who opens `/tools/qr-code-generator`.
- Functionality: labeled text/URL textarea with a live 2,000-char counter; error-correction/size/margin selects and native color pickers, all restricted to values always valid under the shared engine's schema; debounced (300ms), stale-response-safe, unmount-safe local generation via `generateQrCode` (no network calls, no payload logging — grep-enforced by a new test); accessible error (`role="alert"`, reusing the existing `.admin-auth-error` pattern), empty, and loading states; a responsive preview image (`max-width:100%`) with descriptive alt text.
- New, self-contained `.qr-generator*` CSS (responsive grid, `minmax(0,1fr)` throughout) plus one specificity-safe `.tool-workspace.qr-generator-workspace{display:block}` override — chosen to touch only a property no existing responsive `.tool-workspace` rule sets, avoiding the earlier Admin-Tools-grid specificity-conflict pattern. Mobile carousel untouched.
- No new CSP directive needed — `img-src 'self' data:` was already present (added proactively in Phase 2.11 for exactly this future QR-preview use case) and is now genuinely exercised.
- New test file `tests/qr-generator-tool.test.ts` (6 structural checks: dynamic-not-static lazy import, slug-gated rendering, unchanged placeholder for other tools, no `console.*` calls, reuse of the shared engine rather than a duplicate `qrcode` import).
- One real (non-flaky) pre-existing test fragility found and fixed: `tests/admin-ui.test.ts`'s fixed-pixel-width guard uses a non-position-aware `indexOf` lookup; the originally-chosen `.qr-generator-empty-state{max-width:220px}` coincidentally collided, textually, with the unrelated `.admin-sidebar{width:220px}` rule. Not a real responsiveness defect — fixed by changing the new rule's value to `240px`, not by modifying the pre-existing test.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **346/346** (340 previous + 6 new). `npm run build` — PASS. Bundle impact measured: Worker unchanged; client CSS +2.08 kB; main JS +0.02 kB (negligible — `qrcode` did not reach the eager bundle); `ToolPage` chunk +0.50 kB; new isolated `QrCodeGeneratorTool` chunk 28.91 kB / gzip 10.64 kB.
- **Git**: commit `36e7815` ("feat: implement phase 4.2 qr code generator"), pushed to `origin/main` (`9ffc4c7..36e7815`).
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `239642a3-3b03-4f0d-8e87-1f90c4bdaa3d`**, production URL `https://codivio.online`. 5 assets uploaded. Post-deployment: homepage/`/tools/qr-code-generator`/`/tools/qr-code-scanner` (control)/`/faq`/`/admin/login` all `200`; nonexistent path `404`; `/api/health` connected; unauthenticated `/api/admin/pages` `401`; the new asset chunk fetched directly `200`; all 6 security headers present; live main JS/CSS/new chunk all confirmed byte-for-byte identical to the local build.
- **Visual/browser rendering, and whether generated codes actually scan, remain `UNKNOWN — NOT VERIFIED`** — no browser or QR-scanning tool is available in this environment. Phase 4.3 (remaining QR tool variants, scanner, customization/export, analytics) was not started.

## Phase 4.3 — URL to QR Code (2026-09-18)

**Implemented, committed, pushed, and DEPLOYED.** Extends the Phase 4.2 QR Code Generator with a Text/URL mode toggle, fulfilling what Phase 4.1's own `DECISIONS.md` entry explicitly deferred (URL protocol validation).

- `shared/qr/types.ts`: added `QrUrlPayload` (`{kind:"url", value:string}`), widened `QrPayload` to `QrTextPayload | QrUrlPayload`, added `"invalid_url"` error code. `shared/qr/validate.ts`: added `isValidQrUrl` (native `URL`-constructor check, http/https only) and refactored the empty/length bounds check into a shared helper reused by both payload kinds — not duplicated. `src/lib/qr-engine.ts#generateQrCode`'s parameter type widened to `QrPayload`; no other change, since a URL payload is rendered by the exact same code path as text. `shared/qr/index.ts` re-exports the two new symbols — purely additive, no existing export changed.
- `QrCodeGeneratorTool.tsx`: added a Text/URL toggle (reusing the existing `.category-filter` pill-button styling and `aria-pressed` pattern); URL mode swaps the textarea for a single-line `<input type="url">`. Config controls, preview, debounce, stale-request guard, and unmount guard are unchanged and shared by both modes.
- New CSS: `.category-filter.qr-generator-mode-toggle` + a `:focus-visible` for the toggle buttons; extended the existing input-styling selector list to include `input[type="url"]`. Mobile carousel untouched.
- New test file `tests/qr-url-builder.test.ts` (9 tests: URL format validation, `"url"` kind through `validateQrRequest`/`generateQrCode`, confirms `"text"` payloads are never URL-validated, structural checks on the toggle and no-payload-logging). All pre-existing `qr-engine.test.ts` cases re-verified passing unchanged.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **355/355** (346 previous + 9 new). `npm run build` — PASS. Bundle impact measured: Worker/main JS/`ToolPage` chunk all unchanged; client CSS +0.30 kB; isolated `QrCodeGeneratorTool` chunk +1.03 kB (28.91→29.94 kB) — still downloaded only by a visitor of this one tool page.
- **Git**: commit `2d9092f` ("feat: add url mode to qr generator"), pushed to `origin/main` (`99670e6..2d9092f`).
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `4b44cd3a-7213-4520-a296-5dacc40c1ada`**, production URL `https://codivio.online`. 5 assets uploaded. Post-deployment: homepage/`/tools/qr-code-generator`/`/tools/qr-code-scanner` (control)/`/faq`/`/admin/login` all `200`; nonexistent path `404`; `/api/health` connected; unauthenticated `/api/admin/pages` `401`; the new asset chunk fetched directly `200`; all 6 security headers present; live main JS/CSS/new chunk all confirmed byte-for-byte identical to the local build.
- **Visual/browser rendering, and whether generated URL codes actually scan, remain `UNKNOWN — NOT VERIFIED`** — no browser or QR-scanning tool is available in this environment. Phase 4.4 (WiFi QR Code, per `CODIVIO_MASTER_PLAN.md`) was not started.

## Phase 4.4 — WiFi QR Code (2026-09-18)

**Implemented, committed, pushed, and DEPLOYED.** Adds a third "WiFi" mode to the Phase 4.2/4.3 QR Code Generator — the first genuinely structured (non-flat-string) payload kind, encoding SSID/password/security/hidden-network fields into the standard scannable `WIFI:T:<WPA|WEP|nopass>;S:<SSID>;P:<password>;H:<true|false>;;` format.

- `shared/qr/types.ts`: added `QrWifiSecurity`/`QrWifiPayload` (structured fields, no single `.value`), widened `QrPayload` to a 3-member union, added 5 WiFi error codes, and added `encodedValue: string` to `QrValidationResult`'s success case — pre-computed by `validateQrRequest` (raw `.value` for text/url, the built `WIFI:` string for wifi) so `generateQrCode` needs zero branching per payload kind.
- New file `shared/qr/wifi.ts`: `escapeWifiValue` (backslash-escapes `\;,:` in one regex pass), `buildWifiQrValue` (always emits an empty password for an open network, regardless of what's on the object), `validateWifiPayload` (SSID required/≤32 chars; password required for WPA/WEP but not open networks; WPA enforces the real 8–63 char passphrase bounds, WEP only requires non-empty — deliberately not enforcing WEP's stricter legacy length rules). `shared/qr/validate.ts` delegates to it for `kind==="wifi"`; text/url logic unchanged. `shared/qr/index.ts` re-exports the new symbols — purely additive.
- `QrCodeGeneratorTool.tsx`: added a WiFi toggle option with SSID input, a security `<select>` (WPA/WPA2, WEP, open), a password field (masked, with a show/hide toggle, disabled + auto-cleared for an open network, `autoComplete="off"`), and a hidden-network checkbox. All existing config controls, preview, debounce, stale-request guard, and unmount guard are unchanged and shared across all 3 modes. The preview's `alt` text names the SSID only — the password is never included.
- New CSS: `.qr-generator-password-field`/`.qr-generator-password-toggle`/`.qr-generator-checkbox-field`, plus an extended input-styling selector list. One deliberate specificity fix (`.qr-generator-field .qr-generator-password-field input`, matching the specificity of the rule it needed to override rather than a weaker one) — a direct reapplication of the CSS-cascade lesson from the earlier Admin-Tools mobile-grid bug. Mobile carousel untouched.
- New test file `tests/qr-wifi-builder.test.ts` (17 tests: escaping, format-building, per-security-type validation, engine integration, structural UI checks including "password never in alt text"). One pre-existing test updated, not weakened: `tests/qr-url-builder.test.ts`'s literal-source-substring check was updated to match the necessary payload-construction refactor (a `payload` variable now branches on mode before the single `generateQrCode(payload, ...)` call), with an inline comment explaining why.
- WiFi credentials exist only in component state, are never logged (grep-enforced), never appear in a URL/query string, and are never sent to any server — generation is entirely client-side, identical to Text/URL modes.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. Targeted QR tests — PASS, 38/38. `npm test` — PASS, **372/372** (355 previous + 17 new). `npm run build` — PASS. Bundle impact measured: Worker/main JS/`ToolPage` chunk all unchanged; client CSS +1.2 kB; isolated `QrCodeGeneratorTool` chunk +3.83 kB (29.94→33.77 kB, gzip 10.96→12.12 kB) — still downloaded only by a visitor of this one tool page; confirmed via grep for a `qrcode`-library-specific string that the library stays isolated to that one chunk.
- **Git**: commit `aa46e5e` ("feat: add wifi qr builder"), pushed to `origin/main` (`8caa0e2..aa46e5e`).
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `78957ce0-e32c-45fd-b4b4-d3cccb954664`**, production URL `https://codivio.online`. 5 assets uploaded. Post-deployment: homepage and `/tools/qr-code-generator` both `200`; `/api/health` connected; live main JS/CSS/`ToolPage`/`QrCodeGeneratorTool` chunks all confirmed byte-for-byte identical to the local build; live grep confirmed the `qrcode` library present only in the live QR chunk.
- **Known limitations**: no real-device verification that a generated WiFi QR code is recognized/auto-joins on a phone (environment has no browser or QR-scanning tool); SSID length is a 32-*character* cap, not an exact 32-*byte* 802.11 implementation; WEP password validation is intentionally shallow (non-empty only), not WEP's real stricter length/format rules.
- **Visual/browser rendering, and whether a generated WiFi QR code actually scans/auto-joins, remain `UNKNOWN — NOT VERIFIED`** — no browser, screenshot, or QR-scanning tool is available in this environment. Phase 4.5 (vCard QR Code, per `CODIVIO_MASTER_PLAN.md`) was not started.

## Phase 4.5 — vCard QR Code (2026-09-18)

**Implemented, committed, pushed, and DEPLOYED.** Adds a fourth "vCard" mode to the Phase 4.2–4.4 QR Code Generator — a second structured (non-flat-string) payload kind, encoding a "digital business card" into a standards-compatible vCard 3.0 (RFC 2426) `BEGIN:VCARD...END:VCARD` format.

- `shared/qr/types.ts`: added `QrVCardPayload` (`{kind:"vcard", firstName, lastName, organization, jobTitle, phone, email, website}` — 7 structured fields, no single `.value`), widening `QrPayload` to a 4-member union, plus 7 vCard error codes. `QrValidationResult`'s existing `encodedValue` field (introduced Phase 4.4) now also carries the built vCard text block for `kind:"vcard"` — `src/lib/qr-engine.ts` needed **zero changes**, confirming the architecture generalizes cleanly to a second structured payload kind.
- New file `shared/qr/vcard.ts`: `escapeVCardValue` (escapes `\`, `,`, `;`, newline per RFC 2426, backslash escaped first to avoid double-escaping), `validateVCardPayload` (at least one of first/last name required; the other 5 fields optional with per-field length caps only — no format validation, deliberately avoiding over-validation of legitimate international phone numbers/emails), `buildVCardQrValue` (`N`/`FN` always emitted; `ORG`/`TITLE`/`TEL`/`EMAIL`/`URL` only when present). `shared/qr/validate.ts` delegates to it for `kind==="vcard"`; text/url/wifi logic unchanged. `shared/qr/index.ts` re-exports the new symbols — purely additive.
- **Deliberate scope decision — no postal address (`ADR`) field.** A complex 7-component structured property, excluded as disproportionate to the core contact-card use case (recorded in `DECISIONS.md`).
- `QrCodeGeneratorTool.tsx`: added a vCard toggle option with 7 fields (first/last name, organization, job title, phone, email, website — each with a native `autoComplete` hint), reusing the existing field/config/preview/debounce/stale-request/unmount infrastructure unchanged across all 4 modes. The preview's `alt` text names the contact only — phone and email are never included.
- New CSS: extended the shared input-styling selector list to also cover `input[type="tel"]`/`input[type="email"]` — no new CSS component needed. Mobile carousel untouched.
- vCard contact data exists only in component state, is never logged, never appears in a URL/query string, and is never sent to any server — generation is entirely client-side, identical to Text/URL/WiFi modes.
- New test file `tests/qr-vcard-builder.test.ts` (18 tests: escaping, format-building, per-field required/length validation, engine integration, structural UI checks including "phone/email never in alt text" and "no download/export controls"). No pre-existing test needed modification this time.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **390/390** (372 previous + 18 new). `npm run build` — PASS. Bundle impact measured: Worker/main JS/`ToolPage` chunk all unchanged; client CSS +0.24 kB; isolated `QrCodeGeneratorTool` chunk +4.56 kB (33.77→38.33 kB, gzip 12.12→13.09 kB) — still downloaded only by a visitor of this one tool page.
- **Git**: commit `a92bcc7` ("feat: add vcard qr builder"), pushed to `origin/main` (`2af3100..a92bcc7`).
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `c3492750-c110-4bc9-a37b-d626822adc17`**, production URL `https://codivio.online`. 5 assets uploaded. Post-deployment: homepage and `/tools/qr-code-generator` both `200`; `/api/health` connected; live main JS/CSS/`ToolPage`/`QrCodeGeneratorTool` chunks all confirmed byte-for-byte identical to the local build; live grep confirmed the `qrcode` library present only in the live QR chunk.
- **Known limitations**: no postal address (`ADR`) field (deliberate scope decision); phone/email/website format is unvalidated by design (length caps only); no real-device verification that a generated vCard QR code triggers an "Add Contact" prompt.
- **Visual/browser rendering, and whether a generated vCard QR code actually triggers an "Add Contact" prompt, remain `UNKNOWN — NOT VERIFIED`** — no browser, screenshot, or QR-scanning tool is available in this environment. Phase 4.6 (QR Code Scanner, per `CODIVIO_MASTER_PLAN.md`) was not started.

## Phase 4.6 — QR Code Scanner (2026-09-18/19)

**Implemented, committed, pushed, and DEPLOYED.** Adds a browser-side QR scanner (camera + image upload) alongside the existing generator, decoding entirely client-side.

- New dependency: `jsqr` (Apache-2.0, zero sub-dependencies, ships its own TypeScript declarations) — chosen over `qr-scanner` (stale, larger, opinionated camera/worker handling) and over the native `BarcodeDetector` API (inconsistent browser support, absent in Safari/Firefox).
- `src/lib/qr-scanner-engine.ts` (new, the only file importing `jsqr`): `decodeQrFromImageData` — a pure function over a minimal structural pixel-data type, unit-tested via a real round-trip (rasterizing a genuine QR bit matrix from the existing `qrcode` package and decoding it back), not mocked.
- `shared/qr/classify.ts` (new): pure, prefix-only best-effort labeling of decoded content (Link/Wi-Fi network/Contact card/Calendar event/Email/Phone/Text message/Location/Text) for the UI — never a full parser.
- `src/tools/QrCodeScannerTool.tsx` (new): camera access via `getUserMedia`, image upload via a file input, a 10-state UI (idle/camera-starting/camera-active/camera-denied/camera-unavailable/camera-error/decoding-image/image-invalid/no-code-found/success), a Copy action via the real Clipboard API, and a reset/"Scan again" action. Camera tracks are stopped on unmount, on Stop, and on a successful decode. Decoded content is never logged, never sent to a server, never placed in a URL, never used to auto-navigate, and is always rendered as plain text (never `dangerouslySetInnerHTML`).
- **Critical production blocker found and fixed during release**: the existing `Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=()` header (Phase 2.11) denied camera access to the site's own page, which would have silently broken the camera path in production. Fixed via a focused Change Control review and a one-line change to `camera=(self)` — `microphone`/`geolocation`/`payment` unchanged. Verified via direct source-reading (no `<iframe>`/third-party camera usage exists anywhere in the codebase) and confirmed live after deployment.
- Test/build: `npm run typecheck` — PASS. `npm test` — PASS, **417/417** (390 previous + 27 new). `npm run build` — PASS. Bundle impact: Worker 228.34→228.35 kB (the `Permissions-Policy` string change); client CSS 41.11→43.29 kB; new isolated `QrCodeScannerTool` chunk 139.37 kB / gzip 50.70 kB; main JS/`ToolPage` chunk unaffected — `jsqr` isolation confirmed via grep for a library-specific marker string, present only in the scanner chunk.
- **Git**: security-header fix commit `7c0f844` ("fix(security): allow same-origin camera for QR scanner"), scanner implementation commit `6ef264c` ("feat(qr): add QR code scanner"), both pushed to `origin/main`.
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `166bc8db-6c05-49f3-bc04-1921c38926d2`** (the deployment confirmed, via live byte-for-byte bundle diffing and Content-Type checks — not status-code-only — to actually carry the complete scanner build), production URL `https://codivio.online`. A real Cloudflare deployment-architecture investigation during this release found that an automatic deployment mechanism (mechanism itself unconfirmed — see `PROJECT_STATE.md`'s "Deployment workflow and automatic-deployment investigation") redeploys shortly after every `git push`; the recommended safe workflow (always commit+push the complete change before `wrangler deploy`, always re-verify live Content-Type/byte-diff afterward) was followed and is now documented.
- **Known limitations**: no real-device verification that scanning works end-to-end (camera permission grant, live decode); the automatic-deployment mechanism remains unconfirmed.
- **Visual/browser/camera/real-device verification remains `UNKNOWN — NOT VERIFIED`** — no browser or camera-capable device is available in this environment. Phase 4.7 (QR Customization + Export, per `CODIVIO_MASTER_PLAN.md`) was not started at the time this entry was first drafted.

## Phase 4.7 — QR Customization + Export (2026-09-19)

**Implemented, committed, pushed, and DEPLOYED.** Adds PNG/JPG/SVG download to the existing QR Generator. Size/margin/error-correction/color customization was already complete since Phase 4.2 and required no changes — verified by inspection, not re-implemented.

- `src/lib/qr-engine.ts`: `QrOutputFormat` widened from `"png-data-url" | "svg"` to also include `"jpg-data-url"` — reuses the existing `qrcode` dependency (`toDataURL(..., {type:"image/jpeg"})`), no new dependency added. No existing PNG/SVG branch changed.
- `src/lib/download-file.ts` (new): a generic, tool-agnostic browser download utility (`triggerDownload`, `downloadTextAsFile`) — deliberately not QR-specific, reusable by a future PDF/Image tool without modification.
- `QrCodeGeneratorTool.tsx`: payload construction extracted into a shared `buildPayload()` (used by both the existing live-preview effect and the new export path, guaranteeing a download always matches the current preview exactly), a `downloadAs(format)` handler, and 3 download buttons (PNG/JPG/SVG) shown once a QR is successfully generated — one function handling all formats and all 4 payload kinds, no per-kind or per-format duplication.
- **Explicitly deferred, documented, not implemented**: PDF export (Phase 5's own reserved scope), EPS export (no reliable browser-side encoder exists), logo embedding (scannability/security-review surface disproportionate to this phase).
- **SVG safety verified by reading the actual renderer source** (`node_modules/qrcode/lib/renderer/svg.js`): it never embeds the original encoded text anywhere in the output, only numeric module-grid/color/size data — enforced by a new permanent regression test using a sentinel value and a WiFi SSID/password.
- Test/build: `npm run typecheck` — PASS. `npm test` — PASS, **430/430** (417 previous + 14 new − 1 legitimately-removed obsolete Phase 4.5 test guard, whose "no export exists yet" premise this phase correctly makes false). `npm run build` — PASS. Bundle impact: Worker/main JS/`ToolPage`/scanner chunk unchanged; client CSS +0.28 kB; isolated `QrCodeGeneratorTool` chunk +1.75 kB (38.33→40.08 kB, gzip 13.09→13.58 kB).
- **Git**: commit `d475d80` ("feat(qr): add PNG/JPG/SVG export for QR generator"), pushed to `origin/main`.
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `de4063be-b14e-48a6-85f6-b167d62ee87c`**, production URL `https://codivio.online`. Post-deployment: homepage/`/tools/qr-code-generator`/`/tools/qr-code-scanner` all `200`; `/api/health` connected; `Permissions-Policy` still exactly `camera=(self), microphone=(), geolocation=(), payment=()`; live main JS/CSS/`QrCodeGeneratorTool` chunk all confirmed byte-for-byte identical to the local build (Content-Type checked, not status-code-only); the live generator chunk was grepped and confirmed to contain the "Download PNG" UI string.
- **Known limitations**: JPG export's genuine JPEG-encoding cannot be verified in this Node-based test/dev environment (`qrcode` resolves to a different, PNG-only renderer under Node/Vitest than the real browser Canvas renderer `vite build` ships — verified by reading both code paths; the browser renderer passes the requested type straight through to the native `HTMLCanvasElement#toDataURL`, which does support JPEG). PDF/EPS/logo embedding not implemented (documented, deliberate).
- **Visual/browser/device/real-download verification remains `UNKNOWN — NOT VERIFIED`** — no browser or device is available in this environment. Phase 4.8 (Validation/Security/Abuse Controls, per `CODIVIO_MASTER_PLAN.md`) was not started.

## Phase 4.8 — Validation / Security / Abuse Controls (2026-09-19)

**Implemented, committed, pushed, and DEPLOYED.** A strict validation/security review of the full QR Generator/Scanner/shared-engine surface, followed by the smallest justified fixes for the real gaps actually found.

- **Confirmed, empirically-reproduced finding (fixed):** `MAX_QR_PAYLOAD_LENGTH` (a flat 2,000-character cap) doesn't account for real QR capacity, which depends on the selected error-correction level — a payload within that limit can still fail to encode at level Q/H (verified directly against the `qrcode` library: a 2,000-character payload throws at "H" but not at "L"/"M"; a maximal 1,457-character vCard also fails at "H"). `src/lib/qr-engine.ts` now wraps the actual encoding calls in a try/catch and converts any such failure into the existing `QrGenerationError` path via a new `payload_exceeds_capacity` code (`shared/qr/types.ts`), instead of a raw library exception reaching the caller.
- **Confirmed finding (fixed):** the QR Scanner's camera-frame capture path had no resolution/resource bound, unlike its own image-upload path (already downscaled to `MAX_CANVAS_DIMENSION = 1600`). `src/tools/QrCodeScannerTool.tsx`'s `getUserMedia` call now requests `{width:{ideal:1280}, height:{ideal:720}}` (advisory only, cannot cause `OverconstrainedError`), and `scanFrame` now applies the same `MAX_CANVAS_DIMENSION` downscale already used by the upload path.
- **Comment-accuracy fix, zero behavior change:** `shared/qr/wifi.ts`'s WEP password comment was corrected — the code already enforced the shared 63-character upper bound for both WPA and WEP; only the comment was misleading.
- **No server-side abuse surface exists** (confirmed via grep: no `/api/qr*` Worker route) — per explicit scope instruction, no rate limiting, CAPTCHA, authentication, or backend infrastructure was added; documented as a deliberate non-finding.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **433/433** (430 previous + 3 new). `npm run build` — PASS. Bundle impact: Worker/main JS/CSS/`ToolPage` chunk unchanged; isolated `QrCodeGeneratorTool` chunk +0.21 kB (40.08→40.29 kB); isolated `QrCodeScannerTool` chunk +0.13 kB (139.37→139.50 kB).
- **Git**: commit `e42c6e9` ("fix(qr): harden validation and scanner resource handling"), pushed to `origin/main`.
- **Deployed**: `npx wrangler deploy` — **Cloudflare Version ID `3aea2e24-fec6-40cc-8235-2737cb8f3cc2`**, production URL `https://codivio.online`. A documented automatic follow-up deployment (Version ID `8dd29f2d-b007-47a5-a014-a1646f3330dc`) occurred ~10 seconds later, consistent with the previously-recorded pattern — not a new anomaly. Post-deployment, run after both deployments completed: `/`, `/tools/qr-code-generator`, `/tools/qr-code-scanner` all `200`; main JS/CSS/generator/scanner chunks all correct `Content-Type` and `Content-Length` matching the local build exactly; all security headers unchanged, `Permissions-Policy` still exactly `camera=(self), microphone=(), geolocation=(), payment=()`.
- **Known limitations**: no real-device verification that a camera honors the `ideal` resolution hint or that the downscale cap measurably reduces resource use. No server-side rate limiting exists for QR features by design (no `/api/qr*` route).
- **Visual/browser/device verification remains `UNKNOWN — NOT VERIFIED`** — no browser or camera-capable device is available in this environment. Phase 4.9 (QR Analytics Architecture, per `CODIVIO_MASTER_PLAN.md`) was not started.

## Phase 3.15 SEO canonicalization follow-up — www → apex redirect (2026-09-19)

**Implemented and tested — NOT committed, pushed, or deployed** (awaiting explicit authorization). Closes the www/non-www duplicate-content gap flagged by the earlier HTTP→HTTPS canonicalization audit.

- **Live audit finding**: `https://www.codivio.online/` no longer returns the previously-documented HTTP 525 (the Phase 3.15 finding, `PROJECT_STATE.md` line 442) — it now serves 200 with a body byte-identical to the apex, and already carries the correct `https://codivio.online` canonical tag, but with no redirect actually consolidating the two hosts. The Cloudflare-side binding fix happened externally, outside this repo.
- `worker/index.ts#route()` now checks the request hostname first: an exact match on `www.codivio.online` (derived from `shared/seo/site.ts#CANONICAL_DOMAIN`, not a second hardcoded string) issues a `301` to the same path+query on the apex. Every other host (the apex itself, `codivio.azbloknot.workers.dev`, `localhost`) is unaffected. No loop is possible — the redirect target's hostname never matches the same check again.
- New `tests/www-redirect.test.ts` (5 tests, same `worker.fetch()` + `makeEnv()` pattern as `tests/route-guard.test.ts`): www homepage redirect, nested-route redirect, query-string preservation, non-www hosts unaffected, no redirect loop.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **438/438** (433 previous + 5 new). `npm run build` — PASS. Bundle impact: Worker 228.35→228.58 kB (+0.23 kB); client bundle byte-identical (Worker-only change).
- **Git**: not committed at the time this entry was first written. **Correction, 2026-09-19 (later the same day)**: approved, committed (`b7efd1e`), pushed, and deployed (Cloudflare Version ID `8a652aa1-0633-44a2-8279-cbfa53b5cb77`), all live-verified — see `PROJECT_STATE.md`'s "www → apex canonicalization redirect" section for the corrected, current record. This entry's original text is left in place above rather than rewritten, matching how this file otherwise preserves history.
- `PROJECT_STATE.md` updated: the stale "`www.codivio.online` → HTTP 525" claims (Phase 3.15 audit, Phase 3.15 remediation, Phase 3.15 Production Release deferred-items list, Phase 3 closure summary, and the Phase 3.2 technical-SEO-audit redirect-behavior note) are each annotated with a dated update pointing to the new "www → apex canonicalization redirect" status section — the original historical findings are preserved as-written, not rewritten, per this project's existing convention for superseded checkpoint records.

## Phase 3.16 — GA4 Analytics Foundation (2026-09-19)

**Implemented and tested — NOT YET committed, pushed, or deployed** (awaiting explicit authorization). Retroactively numbered into Phase 3 (see `CODIVIO_MASTER_PLAN.md` and `PROJECT_STATE.md`'s "GA4 Analytics Foundation status (Phase 3.16)") — Phase 3's own original scope (`CLAUDE.md` §4: "Google / SEO / Analytics... GA4/GTM where appropriate") always included this; it was simply delivered later, after Phase 3.15's closure, as its own explicitly-scoped task.

- New `src/lib/analytics.ts`: the only file that talks to gtag.js. Hardcoded public `GA4_MEASUREMENT_ID` (not a secret). `isProductionHost`/`isTrackablePath` gate GA4 to the real `codivio.online` apex and exclude `/admin/*`. gtag.js loads via a dynamically-appended `<script src=...>` tag — never inline, no CSP `'unsafe-inline'` exception needed.
- `src/App.tsx` gained a small `Analytics` component (mirrors the existing `ScrollRestoration` single-render-point pattern) sending one `page_view` per route change.
- `worker/security-headers.ts` CSP widened by exactly the hosts Google's own gtag.js documentation names (`googletagmanager.com`, `google-analytics.com` + regional subdomains) — nothing broader, no `'unsafe-inline'`/`'unsafe-eval'` added.
- Fixed a pre-existing Phase 3.8 test false positive (`tests/technical-seo-3.8.test.ts`) that a legitimate new CSP comment tripped — now checks the actual computed CSP header value instead of raw file text.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **446/446** (438 previous + 8 new). `npm run build` — PASS.
- **Real compliance finding, resolved by 3.17 (next), not left open**: shipping GA4 unconditionally would have contradicted the already-published `/cookies` page's own promise of a future consent interface.
- **Git**: not committed. **Deploy**: not performed.

## Phase 3.17 — Cookie Consent & Privacy Foundation (2026-09-19)

**Implemented and tested — NOT YET committed, pushed, or deployed** (awaiting explicit authorization). Direct, immediate follow-up to 3.16 — gates GA4 behind real, granular consent and makes `/cookies` describe real behavior for the first time.

- New `src/lib/consent.ts`: generic, analytics-agnostic, `localStorage`-only consent store (same try/catch-degrades-gracefully pattern as `src/i18n/LanguageContext.tsx`). `advertising`/`preferences` are always forced `false` internally regardless of what's requested or stored — neither is a real feature yet.
- New `src/consent/ConsentContext.tsx` (Context/Provider, same shape as `LanguageProvider`) — the one place that calls `enableAnalytics()`/`disableAnalytics()` (3.16's module, extended this task with Google's documented `ga-disable-<id>` opt-out flag + best-effort GA cookie cleanup on revoke) in reaction to a consent change.
- New `src/consent/CookieConsentBanner.tsx` (Accept All / Reject Optional / Cookie Settings) and `src/consent/CookieSettingsModal.tsx` (4 categories — only Analytics is a real, editable toggle; Necessary is always-on/locked; Advertising/Preferences are visible but locked off, "reserved for future use").
- `CookiePolicyPage` updated to real, present-tense behavior with a working "Manage cookie preferences" button. New `shared/i18n/cookieConsent` section — genuinely distinct AZ/TR/EN copy for the banner, modal, and all 4 categories.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **470/470** (446 previous + 24 new). `npm run build` — PASS.
- **Real, executed end-to-end verification** (minimal DOM-stubbed Vitest run, written and deleted within this task, not part of the permanent suite): first visit loads no script and sends nothing; Accept All loads gtag.js and a real page view is sent; `/admin` excluded even with consent granted; Reject Optional stops all further events; re-enabling after a disable works without a reload; host-gating confirmed correct.
- **Git**: not committed. **Deploy**: not performed.

## Mandatory Documentation & Phase Management Rule established (2026-09-19)

**Process/governance change, no application code touched.** `CLAUDE.md` gained a new §27 making it binding, for every future Codivio task, that a new feature/sub-feature/checkpoint is not done until `CLAUDE.md`, `CODIVIO_MASTER_PLAN.md`, `PROJECT_STATE.md`, and `CHANGELOG.md` all agree with what the code actually does — extending §24's existing memory-file discipline to explicitly cover `CLAUDE.md` itself and `CODIVIO_MASTER_PLAN.md`, which §24 didn't previously name. `CODIVIO_MASTER_PLAN.md`'s own "Roadmap qaydaları" section gained a short cross-reference to the same rule rather than a duplicated copy.

- **Applying the new rule surfaced two real, concrete gaps, both fixed in this same pass**: (1) the just-completed GA4/Cookie Consent work had no phase/sub-phase number anywhere — resolved, with explicit user approval, by retroactively reopening Phase 3 and adding 3.16/3.17 (see above); (2) `PROJECT_STATE.md`'s and this file's own "www → apex canonicalization redirect" entries still said "not committed/deployed" after that work had actually been approved, committed, and deployed in a later turn — corrected in place, with the original stale text preserved and annotated rather than silently rewritten.
- No new phase was invented for GA4/Cookie Consent — per the new rule's own "don't fragment work belonging to an existing phase" principle, both were added as sub-phases of Phase 3, whose own original description already named this exact scope.

## Phase 3.17 review — PrivacyPage wording + regression pass (2026-09-19)

**Implemented and tested — NOT YET committed, pushed, or deployed** (review/fix pass on top of the still-pending 3.16/3.17, not a separate release).

- `PrivacyPage`'s "Analytics and advertising" paragraph rewritten from generic future-tense copy to accurately describe 3.17's real behavior: GA4 named explicitly, off by default and not loaded until consent is given, Advertising named as a reserved/inactive placeholder, with a pointer to the Cookie Policy page.
- Regression review: `src/lib/analytics.ts`, `src/lib/consent.ts`, `src/consent/ConsentContext.tsx` re-read fresh and confirmed unchanged since 3.16/3.17; no test asserted the old `PrivacyPage` wording, so none needed updating for the wording change itself.
- Test/build: `npm run typecheck` — PASS. `npm run typecheck:tests` — PASS. `npm test` — PASS, **470/470** (unchanged count). `npm run build` — PASS.
- Documentation sync check (`CLAUDE.md` §27): `CLAUDE.md`/`CODIVIO_MASTER_PLAN.md` needed no change (wording fix within 3.17's already-recorded scope). `PROJECT_STATE.md` had one stale sentence from 3.17's original entry, corrected in place (see "Phase 3.17 review — PrivacyPage wording + regression pass" there).
- **Git**: not committed. **Deploy**: not performed.
