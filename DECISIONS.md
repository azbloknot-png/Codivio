# CODIVIO — DECISIONS LOG

Short, approved architectural/product decisions only. Full rules live in `CLAUDE.md` — this file records *what was decided and why*, not the rule text itself. Newest-relevant decisions grouped by area; no strict chronology required.

## Architecture

- **Modular monolith first.** One deployable Cloudflare Workers app with internal module boundaries; do not split into separate services/repos. *Reason: matches team size and current scale; avoids premature distributed-systems overhead.*
- **Selective microservices only when justified.** Splitting out a service later requires real load/ownership evidence, not speculation. *Reason: keep the option open without paying its cost now.*
- **Cloudflare Workers + D1 direction.** Confirmed via `wrangler.jsonc` + `worker/index.ts` (Phase 2.2). *Reason: aligns with the free/low-cost infrastructure principle and existing Cloudflare Pages/Assets setup.*
- **SQLite-compatible relational design where applicable.** `database/schema.sql` targets D1's SQLite dialect (e.g. `AUTOINCREMENT`, `PRAGMA foreign_keys`). *Reason: D1 is the chosen database; no ORM/abstraction layer added.*

## Homepage / Tool Registry

- **Homepage shows exactly 12 selected tools; all registered tools/routes remain preserved.** `HOMEPAGE_FEATURED_TOOLS` is a filtered view (`tools.filter(t => t.featured)`), never a copy or deletion of the registry. *Reason: curated homepage UX without losing the full catalog or breaking `/tools/:slug` routes.*
- **Popular/QR/PDF tool groups are independently managed.** Separate `popular`/`qr`/`pdf` boolean flags, each its own derived list — not aliases of `featured`. *Reason: lets a future Admin Panel curate each homepage section independently, with no code change. Status: implemented (2026-09, Phase 2 UI work).*

## Admin / Security

- **Admin is intended to become the central no-code/low-code management center**, not just a read-only dashboard — covering homepage tool ordering, Popular/QR/PDF/Other curation, tool active/inactive, content, SEO, ads, users, and settings. *Reason: stated product goal for Phase 2+; drives §7/§14 of CLAUDE.md and the `codivio-admin` skill's checkpoint list.*
- **Security-first admin architecture.** Auth/RBAC/session/audit-log work follows CLAUDE.md §19 Change Control (inspect → risk → smallest safe change → test → security review → diff review → report), not the lighter default Testing Gate. *Reason: Admin is the highest-risk surface built so far.*
- **IP limits are abuse protection, not permanent identity.** Rate-limiting by IP (e.g. login attempts) must not be treated as a durable user/device identifier. *Reason: avoid conflating anti-abuse tooling with identity/authorization logic.*

## Monetization (future, not yet built)

- **Future Free/Pro/Business entitlement architecture** is anticipated but not implemented. *Reason: keep today's data model from blocking it later, without building it prematurely.*
- **Affiliate services and Codivio-owned premium services remain logically separate.** Even in future schema/architecture, these must not be merged into one undifferentiated "monetization" concept. *Reason: different risk, accounting, and disclosure requirements.*
- **Social media integrations use official APIs/webhooks where available**, not scraping or unofficial endpoints. *Reason: reliability and ToS compliance for any future share/post features.*

## Authentication (Phase 2.3)

- **Password hashing via PBKDF2-SHA256 (Web Crypto), not a custom algorithm.** 100,000 iterations, random 16-byte salt, self-describing stored format. *Reason: the Workers runtime has no native bcrypt/argon2; PBKDF2 via the standard Web Crypto API is a recognized, non-invented algorithm that needs no new dependency.*
- **Session tokens are stored as a SHA-256 hash, never the raw value, in D1.** The raw token exists only in the `HttpOnly` cookie. *Reason: a database read alone then can't be replayed as a valid session — defense in depth beyond just "the cookie is HttpOnly."*
- **Failed-login lockout is keyed by the attempted email, not by IP; no IP address is stored.** *Reason: direct application of the existing "IP limits are abuse protection, not permanent identity" decision — this is the minimum mechanism that still protects a specific account from credential guessing.*
- **`vitest.config.ts` is kept separate from `vite.config.ts`.** *Reason: `vite.config.ts` conditionally loads `@cloudflare/vite-plugin`, which tries to boot a Miniflare/`workerd` runtime during `vitest run` (Vitest sets `NODE_ENV=test`, not `"development"`) — this crashes on this machine and would otherwise break every test run, not just Worker-related ones. Status: implemented 2026-09 (Phase 2.3); do not merge these configs back together without addressing the underlying plugin/test interaction first.*

## Admin UI (Phase 2.4)

- **Admin code lives in its own `src/admin/` module, not folded into `App.tsx`.** *Reason: `App.tsx` was already ~1,700 lines before Admin work started; Admin is a genuinely separate, growing concern (2.5 RBAC, 2.9 Pages, 2.10 Tools all extend it) and deserves its own file from the start rather than compounding the existing monolith.*
- **Admin auth state is per-route local hooks (`useSession`), not a global context/store.** *Reason: explicit user instruction to keep it simple; the server session (`GET /api/auth/session`) is the only source of truth regardless, so a shared client store would only cache what's re-checked on each protected route anyway. Revisit only if real duplication/UX cost is observed.*
- **The Admin login page reuses the existing `Code2` icon brand mark (same as the public site header/footer), not the unused `public/assets/branding/codivio-logo.png` file.** *Reason: the real logo PNG has never been wired into any page yet; introducing it for the first time only in Admin would create a visible brand-mark inconsistency between public and admin. Revisit together, sitewide, if/when the real logo is ever adopted.*

## RBAC (Phase 2.5)

- **The role/permission matrix is defined once, in `shared/rbac.ts`, imported by both the Worker and the frontend.** *Reason: explicit instruction to avoid scattered `role === "..."` checks and matrix duplication; a plain, framework-free TS module is importable from both Vite build environments without any bridging code.*
- **The initial permission matrix (super_admin/admin/editor/analyst) was reviewed and implemented exactly as specified — no security or architectural problem was found in it.** Every role's permissions are a coherent subset within the hierarchy (analyst ⊆ editor's base access; admin ⊇ editor's content permissions plus read-only users/settings/analytics/audit visibility; super_admin ⊇ everything), and no permission is granted automatically just because a role exists. *Status: implemented 2026-09 (Phase 2.5) as given; if a future checkpoint wants to change it, that is a new decision to record here, not a silent edit.*
- **A DB-level `CHECK` constraint on `users.role` is deliberately deferred**, relying on `shared/rbac.ts#isValidRole`'s fail-closed application-layer check instead. *Reason: SQLite/D1 cannot add a `CHECK` to an existing column without rebuilding the table (a risky migration, and unverifiable here without live D1 access) for a case no current code path can trigger (only bootstrap creates users, always with `role = "super_admin"`). Revisit once real role-editing exists.*
- **`worker/rbac.ts` depends on `worker/auth.ts` one-directionally, never the reverse.** `auth.ts`'s own `handleSession` checks permissions inline rather than calling `rbac.ts#authorize()`. *Reason: avoids a circular import; `auth.ts` is the lower-level "who is this" primitive, `rbac.ts` the higher-level "are they allowed" orchestration built on top of it. Future protected endpoints (Phase 2.9+) should import `authorize()` from `rbac.ts`.*
- **Admin nav items are hidden entirely if the role lacks the underlying permission, and shown as "Soon" only if the role has the permission but the feature isn't built yet.** *Reason: don't advertise a feature a role will never be allowed to use; do tease one it will eventually get. This is UI convenience only, not a security control.*

## Audit Logging (Phase 2.6)

- **Audit logging is fail-open, uniformly, for every event type.** A failed write is reported via `console.error` (visible in Worker logs) but never blocks, delays, or changes the outcome of the operation being audited. *Reason: audit logging is a detective control, not a preventive one — its own unavailability (a transient D1 hiccup, or right now, no real D1 being provisioned at all) must not become an authentication/authorization outage. One consistent policy, not decided per call site.*
- **The existing Phase 0 `audit_logs` table was extended, not replaced.** Migration `0003` adds `actor_email`/`result`/`ip_address`/`user_agent` columns and indexes to the table already defined in migration `0001`; no new table was created. *Reason: the original design (`user_id`, `action`, `entity_type`/`entity_id`, `metadata_json`, `created_at`) already covered most of what was needed — inspected first, extended only where genuinely missing.*
- **Failed-login audit entries never reference a real `user_id`, even when the email matches an existing account** — only `actor_email` is stored for failures. *Reason: avoids conflating "this account was probed" with "this account acted"; keeps the failure path's data shape identical regardless of whether the email exists, which also avoids a timing/existence side-channel in the logging path itself.*
- **IP address is captured only from Cloudflare's edge-set `CF-Connecting-IP` header, never a client-suppliable one like `X-Forwarded-For`, and is left `null` when absent** (which is always, in this local environment). *Reason: a spoofable IP field would be worse than no IP field for an audit trail; the mechanism's trustworthiness is a documented Cloudflare platform guarantee, not something this project can verify pre-deployment.*
- **Plain 401 (unauthenticated) requests are not audited; only authenticated-but-denied (403) events are.** *Reason: anonymous 401s are the expected default for routine traffic and would grow the log unboundedly with no security signal; a real account being denied a specific permission is the actionable event.*
- **`database/schema.sql` is a maintained canonical snapshot, not a one-time copy of migration `0001`.** Every future migration must update it too. *Reason: it had silently drifted after migration `0002` (Phase 2.3) went in without a matching schema.sql update; caught and corrected in Phase 2.6. Documented here so it isn't lost as a one-off fix and forgotten again.*

## Settings Foundation (Phase 2.7)

- **One generic `settings` table (key/value/value_type/category/is_public/description/updated_at/updated_by), not a table per future integration category.** *Reason: Google/Social/AdSense/Affiliate/etc. don't exist yet; a single flexible table with a centralized, code-level registry (`shared/settings.ts`) lets each be added later as new registry entries + seed rows, never a schema redesign.*
- **True secrets never live in this table.** API keys, OAuth client secrets, SMTP passwords, and similar belong in Cloudflare Worker Secrets. This table only stores non-secret configuration and "is this integration configured" status flags. *Reason: explicit requirement — D1 rows are not an appropriate place for high-value credentials Cloudflare already has a proper mechanism for.*
- **Unregistered setting keys are rejected (400), not silently created.** `shared/settings.ts`'s registry is the single fail-closed allowlist for both read-shaping and write validation. *Reason: prevents arbitrary key/value injection into an otherwise-flexible table; every new setting must be a deliberate code change (registry entry + migration seed row), never an ad-hoc runtime key.*
- **PATCH /api/admin/settings updates exactly one key per call, not a batch.** *Reason: avoids partial-apply/transaction complexity for a foundation this small; a batch endpoint can be added later if a real multi-field settings form needs it.*
- **A setting's `isPublic` (read exposure) and `isSensitive` (audit-metadata redaction) are independent flags.** A setting can be private without being sensitive (e.g. `google.analytics_configured`) or, in principle, public and still audited without its value (none currently are). *Reason: conflating "who can read it" with "should its value appear in logs" would have under- or over-protected one of the two cases.*
- **"Configured" status flags for not-yet-built integrations (Google/AdSense/Affiliate) are rendered read-only in the Admin UI, even for a role with `settings.manage`.** *Reason: no real integration flow exists yet to legitimately set them true; making them manually toggleable now would let an admin create a fake "Configured" status with nothing behind it, which is exactly what CLAUDE.md/this checkpoint prohibit. The underlying PATCH endpoint doesn't special-case this — it's a UI-only restraint until Phase 2.9+ builds the real integration flow that should set these server-side.*

## Pages Management (Phase 2.9)

- **The existing (Phase 0) `pages` table was extended in place, not replaced or duplicated.** It already had the right shape (id/slug/title/content/status/timestamps) for a CMS-style admin page; migration `0005` added `description`/`meta_title`/`meta_description`/`canonical_url`/`is_indexable`/`created_by`/`updated_by`. *Reason: matches the project's own "inspect first" convention (CLAUDE.md §1) rather than assuming a fresh table was needed.*
- **The existing (still unused) `seo_settings` table was deliberately NOT wired into Pages Management.** The four scalar SEO fields (`meta_title`/`meta_description`/`canonical_url`/`is_indexable`) were added directly to `pages` instead. *Reason: `seo_settings` models richer per-page SEO (Open Graph, `schema_json`, granular `robots` directives) intended for the Phase 3 SEO system; wiring it in now would require multi-table transactional writes for a foundation checkpoint that only needs four scalars. Revisit consolidation when Phase 3 SEO work begins.*
- **No DB-level `CHECK` constraint on `pages.status`**, relying on `shared/pages.ts#isValidPageStatus`'s fail-closed application-layer check instead. *Reason: identical to the existing `users.role` deferral — SQLite/D1 cannot add a `CHECK` to an existing column without a full table rebuild.*
- **Content-safety is asymmetric by field, not a blanket filter.** `title`/`description`/`metaTitle`/`metaDescription` reject raw `<`/`>` outright; `content` is stored verbatim with no character filtering. *Reason: the short metadata fields have no legitimate reason to hold markup, so rejecting it is pure upside; `content` is real body text that might legitimately need to quote or discuss markup, and its actual safety boundary is the render side (plain React text interpolation, never `dangerouslySetInnerHTML`) — filtering it at write time would be either redundant with that boundary or actively harmful (blocking a page from documenting a `<script>` tag as an example).*
- **A published page must be archived before it can be deleted** (`DELETE` returns 409 otherwise). *Reason: CLAUDE.md §18's "prefer safe behavior" — avoids an accidental hard-delete of live, possibly-indexed content; draft/archived pages can be deleted directly since they were never public.*
- **The public page endpoint (`GET /api/pages/:slug`) enforces `status = 'published'` inside the SQL `WHERE` clause itself, not as a post-query filter**, and returns the same generic 404 for both a missing slug and a real draft/archived one. *Reason: makes it structurally impossible for a future code change to accidentally leak a non-published page, and avoids a slug-existence side channel.*
- **A public page's slug can never be one of the existing top-level static routes** (`admin`/`api`/`tools`/`blog`/`faq`/`about`/`contact`/`privacy`/`terms`/`cookies`), enforced by an explicit `RESERVED_PAGE_SLUGS` allowlist check in `shared/pages.ts`, independent of React Router's own static-beats-dynamic route ranking. *Reason: defense in depth — don't rely solely on router behavior for a security-relevant routing guarantee; keep the reserved list in sync with `src/App.tsx`'s route table by hand.*

## Tools Management (Phase 2.10)

- **The existing (Phase 0) `tools`/`categories` tables were extended and seeded in place, not replaced or duplicated.** They already had nearly the right shape (slug UNIQUE, name, description, component, status, featured, sort_order, category_id -> categories) but had never been seeded — the frontend has always used its own static array. *Reason: matches the project's "inspect first" convention; the schema was already 80% of what was needed.*
- **Pre-existing column names (`featured`, `status`, `component`, `sort_order`) were kept as-is, not renamed to match the newer `is_`-prefixed convention** used by `pages.is_indexable`/`settings.is_public`. Only genuinely new columns (`is_popular`) adopted the newer style. *Reason: renaming an existing column requires a full SQLite table rebuild for no functional benefit; a small naming inconsistency between an old and new column is cheaper than that migration risk.*
- **The static tool registry in `src/App.tsx` remains the live source for the public site (Homepage, `/tools`, `/tools/:slug`) this phase — the new `tools`/`categories` D1 tables are an Admin-managed configuration surface with no public consumer yet.** *Reason: CLAUDE.md §10/§22's own escape hatch — wiring full public dynamic integration in the same checkpoint as the DB+API+RBAC+validation+audit+UI+security work would be unnecessary architectural risk for a "foundation" checkpoint; the two data sources cannot conflict because only one is ever actually read by public code paths. The remaining integration (point the public site at `GET`-style tool data instead of the static array) is explicit, documented future work, not assumed in progress.*
- **No public `GET /api/tools` endpoint was built.** *Reason: nothing in this checkpoint would consume it (see above) — building unused API surface violates CLAUDE.md §2's "no unrelated functionality." Add it in the same future checkpoint that actually wires the public site to the database.*
- **`TOOL_CATEGORIES` in `shared/tools.ts` is deliberately broader (includes `video`/`gif`/`ai`/`utilities`) than what the `categories` table currently holds (only `qr`/`pdf`/`image`/`other` are seeded).** A category is only actually usable once a real `categories` row exists for it — `worker/tools.ts` looks it up and fails closed (400) rather than auto-creating one. *Reason: documents the intended growth path (CLAUDE.md §4/§11's future tool categories) without prematurely creating unused category rows or over-complicating validation with a live DB round-trip inside the framework-agnostic `shared/` module.*
- **Icons are a controlled string-identifier allowlist (`ICON_NAMES`), never raw HTML/SVG/markup from a database value.** *Reason: explicit requirement — an icon column that accepted arbitrary markup would be a stored-XSS/arbitrary-HTML vector the moment anything renders it; a closed identifier list mirrors the same fail-closed pattern already used for roles/permissions/settings-categories/page-statuses.*
- **Deleting an active tool is blocked (409) until it is deactivated first** — the same safety pattern as Phase 2.9's "archive before delete" for pages. *Reason: CLAUDE.md §24 — a tool may be referenced by the homepage, routes, or future analytics/SEO/affiliate configuration; deactivation is the safe default lifecycle action, hard-delete is for already-inactive tools only.*

## Admin Security + Final Tests (Phase 2.11)

- **A single, uniform security-headers baseline is applied at one point** — `worker/index.ts`'s top-level `fetch()` wraps every response (both `/api/*` handlers and whatever `env.ASSETS.fetch()` returns for the SPA) via `worker/security-headers.ts#withSecurityHeaders`, rather than adding headers per-handler or via a separate `_headers` file. *Reason: one place, impossible to forget on a new route; verified safe by actually inspecting the built `index.html`/bundle (one same-origin script, one same-origin stylesheet, zero external hosts) before writing the CSP, not copied from a generic template.*
- **The Content-Security-Policy is strict now (`script-src 'self'`, `style-src 'self'`, no `unsafe-inline`/`unsafe-eval`) and should only ever be *loosened*, directive by directive, when a real integration genuinely needs it** (e.g. GA4's script host when Phase 3 adds it) — never widened speculatively ahead of that need. *Reason: matches CLAUDE.md §9/§10's "no arbitrary remote scripts, document why each external script exists" — the policy itself should enforce that discipline going forward, not just describe it.*
- **The one pre-existing inline `style={{ whiteSpace: "pre-wrap" }}` (the public CMS page body, Phase 2.9) was replaced with a CSS class (`.cms-page-content`)** specifically so `style-src` could stay `'self'` with no `'unsafe-inline'` exception. *Reason: a one-line, zero-risk change that measurably improves the CSP's strength — cheaper than carrying a permanent policy exception for a single call site.*
- **`img-src` allows `data:`** even though nothing uses it yet. *Reason: a client-rendered QR code preview (a very likely near-term Phase 4 need) would naturally want a `data:` image URI, and a `data:` image poses no script-execution risk — a deliberate, narrow, justified exception, not a general loosening.*
- **No CSRF token mechanism was added.** The existing `SameSite=Strict` + `HttpOnly` + `Secure` session cookie (Phase 2.3), combined with this being a same-origin architecture with no cross-origin embedding, already blocks the cookie from being sent on any cross-site request — including top-level navigations, which `SameSite=Lax` would still allow. *Reason: `SameSite=Strict` is already the strictest available setting; adding a token mechanism on top would be pure complexity with no additional protection for this architecture. Revisit only if a legitimate cross-origin flow (e.g. an OAuth callback) is ever introduced, since `Strict` cookies do not survive that redirect chain either.*
- **PBKDF2 iteration count (100,000) was reviewed, not changed.** Current OWASP guidance (2023) recommends 600,000 for PBKDF2-HMAC-SHA256, well above today's value — but Cloudflare Workers has a hard per-request CPU-time budget (as low as 10ms on the Free plan) that this environment cannot empirically measure locally (the standing `workerd`-on-Windows limitation). *Reason: raising a security-critical, performance-sensitive parameter without being able to verify it doesn't blow the platform's CPU budget would trade a documented, low-severity gap for an unverified, potentially request-failing one. The stored-hash format (`pbkdf2$<iterations>$<salt>$<hash>`) already self-describes its iteration count, so this can be raised later with zero migration cost and zero impact on existing hashes — recorded as a deferred, environment-gated item, not silently left alone.*
- **Deactivation-mid-session was verified, not assumed.** `resolveAuthenticatedUser`'s session/user JOIN already re-reads the live `users.status` on every request (not just at login), so deactivating a user immediately invalidates their still-unexpired session on their very next request. This existing behavior had no direct test until this checkpoint — one was added (`tests/rbac.test.ts`) rather than left as an inspected-but-unverified claim.

## Admin UI + Homepage Redesign (Phase 2.13)

- **`NAV_ITEMS`' `permission` field is now optional; an item without one is visible to any role that reached the Admin shell.** *Reason: 9 of the 16 requested Admin modules (Blog/SEO/Search Console/Advertising/Affiliate/Monetization/Social/Reports/System Health) have no backend or data to protect yet — inventing a permission in `shared/rbac.ts` for a page that shows nothing but a "Coming soon" list would be RBAC theater, not a real security boundary, and would have required touching the RBAC matrix for a purely cosmetic reason. The shell itself is already gated by `admin.access`.*
- **A single shared `AdminComingSoonPage` component, not one bespoke page per module**, represents every not-yet-built Admin module — 12 placeholder routes in total (the 9 permission-less modules above, plus Users/CRM, Analytics, and Audit Log, which have a real permission but no page built yet). *Reason: keeps every placeholder visually consistent and cheap to add/maintain; each just passes a title/description/category-chip list. None fabricate data, charts, or interactive controls.*
- **Tools Management's editor was NOT extended with the requested future fields** (Related tools, Usage, Errors, Performance, Monetization/Ad/Affiliate eligibility, Policy status). *Reason: none of these have a backing database column; an input field that silently saves nothing would be worse than no field at all. Extend the editor only alongside the migration that adds real columns for them.*
- **The official logo PNG (`public/assets/branding/codivio-logo.png`) is used unmodified; its square/dark-background presentation is handled entirely in CSS** (`.brand-logo{border-radius:10px;object-fit:cover}`), never by cropping/editing the source file. *Reason: the only available asset is a square, dark-background app-icon-style lockup, not a horizontal transparent header logo — clipping it into a rounded badge via CSS (a purely presentational, reversible transform) satisfies "use the official logo" and "do not alter the logo artwork itself" simultaneously.*
- **The new "Image & Other Tools" homepage slider is derived from the existing `category` field, not a new boolean flag** (unlike Popular/QR/PDF, which use dedicated flags). *Reason: every Image Tools and Other Tools registry entry belongs on this one shared section — there is no curation split to express with a separate flag, so adding one would be an unused, always-true field for 14 tools.*
- **The Admin Settings category-tab UI uses a simplified ARIA tabs pattern** (`role`/`aria-selected` present, no roving tabindex/arrow-key navigation). *Reason: this project has no component-testing setup to verify complex keyboard interaction live; native button Tab/Enter semantics already make every tab reachable and operable. Revisit if full APG tabs compliance becomes a real requirement.*

## Multilanguage System (Phase 2.15)

- **Language persistence is two intentionally separate layers, not a single mechanism.** The pre-existing public `general.default_language` Setting (Phase 2.7) is the site-wide default for a visitor who has never chosen; `localStorage` holds a given browser's own override and always wins once set. *Reason: satisfies both "changeable by any visitor without an account" and "persisted via the existing server/settings architecture" without adding a new D1 column/table or making every anonymous visitor's language choice a server-side write. Editing the site-wide default still goes through the unmodified `settings.manage` RBAC gate.*
- **`Translations` is a nested-object TypeScript interface, not flat string keys.** *Reason: a missing or misspelled translation key becomes a compile error in all three dictionaries simultaneously, rather than a silent runtime fallback or blank UI text discovered only by manually clicking through every language.*
- **The already-live production value of `general.default_language` (`"en"`) was deliberately left unmigrated.** *Reason: editing data in a migration already applied to production D1 is unsafe drift; the new fail-closed validation (`az`/`tr`/`en` only) applies going forward, and a `super_admin` can change the live value through the normal Settings UI at any time.*
- **Tool registry content, page/blog/FAQ body content, and Admin editor form field labels are explicitly out of scope for this checkpoint.** *Reason: this phase's brief was UI chrome/wording quality ("fix bad wording BEFORE translating", "don't redesign"), not a full content-localization project — translating 34 tool descriptions and all page bodies is a separate, much larger effort with its own review needs (accuracy, tone, native-speaker QA).*
- **No hreflang or language-specific SEO metadata was added.** *Reason: explicitly deferred to Phase 3 (Google/SEO/Analytics) per this checkpoint's own instruction — the `Language` union and per-language dictionaries built now are compatible with adding it later without rework, but a partial/fake hreflang implementation now would be worse than none.*

## SEO Metadata Architecture (Phase 3.1)

- **SEO title/description data lives in a new code-level module (`shared/seo/`), not a new or extended D1 table.** The public site's real source of truth for tool/page *content* is still the static registry in `src/App.tsx` (Phase 2.10's own documented decision — nothing public reads tools from D1 yet), so SEO metadata for that same content follows the identical pattern: a framework-agnostic `shared/` module, imported by the frontend, mirroring `shared/i18n/`. *Reason: matches where the real rendering source of truth already is; a D1-backed SEO table with no public consumer reading tool/page content from D1 at all would be schema churn with nothing real behind it.*
- **The existing `tools.seo_title`/`seo_description` (Phase 2.10) and `pages.meta_title`/`meta_description`/`canonical_url`/`is_indexable`/`seo_settings` (Phase 2.9/Phase 0) D1 columns are left exactly as they are — not extended to multilingual, not migrated, not duplicated into a new table.** They remain the right shape for a *future* Admin-editable, D1-driven SEO layer once (and only once) the broader "wire the public site to read tools/pages from D1" decision is made (still open, see Phase 2.10's "Architecturally important" note in PROJECT_STATE.md). Extending them to 3 languages now, with zero real consumer, would be premature schema growth. `pages.is_indexable` is the one exception: it already existed, was already returned by the public API (`worker/pages.ts#serializePublicPage`), and simply wasn't read by the frontend — Phase 3.1 wires that up (a pure frontend fix, zero backend/schema change).
- **Every tool page is `noindex,follow`, not `index,follow`.** `src/pages/ToolPage.tsx` renders a "Tool coming soon" placeholder for all 34 tools — none has real processing yet. Indexing a page that implies a working tool it doesn't have would be exactly the misleading-metadata/thin-content risk Google's guidelines (and this phase's own instructions) warn against. `follow` is kept so internal links/discovery still work. Flipping a specific tool to `index,follow` the day it ships real functionality is a one-line change in `shared/seo/tools.ts`, not a rewrite.
- **No hreflang was implemented.** Codivio's language selection (Phase 2.15) is a per-browser/site-setting choice, not a distinct URL per language (no `/az/`, `/tr/`, `/en/` path prefixes or subdomains exist) — hreflang requires real, distinct URLs per language to be correct, and faking it against one shared URL would be exactly the "incomplete/fake" implementation this phase's instructions explicitly forbid. Building real hreflang requires a prior, separate architecture decision (path-prefix vs. subdomain vs. query-param per-language URLs) that has not been made — documented here as required future Phase 3 technical SEO work, not started.
- **The Admin "SEO" module (`/admin/seo`) is a read-only preview (`AdminSeoPage.tsx`), not an editable form.** It renders live from `shared/seo/` (title/description/canonical/robots preview, plus duplicate/missing/length-outlier detection) with no save button, because there is nothing to save into yet — the underlying data is code, not a database row. Building an editable-looking form with no working save path would itself be the "fake UI field" this phase's instructions explicitly forbid. No new RBAC permission was added; it's visible to any role reaching the Admin shell, same as the other not-yet-backed modules (see the Phase 2.13 decision above).
- **`public/sitemap.xml` is a static file, not Worker-generated**, matching the existing `robots.txt`/`llms.txt` pattern, and lists only the 9 real static pages (never the 34 noindex tool pages — a sitemap should only list pages meant to be indexed). *Reason: a dynamic, D1-driven sitemap would require the same public tools/pages API that doesn't exist yet; a static file is the smallest useful implementation for today's actually-indexable page set.*
- **Fixed a real, pre-existing production bug found during this phase's audit**: `robots.txt`'s `Sitemap:` directive and `llms.txt` both pointed at `codovio.online` (missing the "i") instead of `codivio.online`. Corrected as part of this phase's technical SEO review, not a new feature.

## SEO Search-Intent Audit (Phase 3.2)

- **Length-outlier measurement bug found and fixed**: `shared/seo/duplicates.ts#findLengthOutliers` compared the raw pre-suffix title against the 15-65 char guidance, which both flagged short titles as false positives (the real rendered `<title>` always has " | Codivio" appended, +10 chars) and missed real over-length titles. Fixed to measure `buildTitle(copy.title)` — the actual rendered title — instead. *Reason: the guidance exists to approximate what a browser tab/SERP actually shows; measuring the wrong string made the check both noisy and blind to real problems.*
- **18 tool/page titles were trimmed** (TR/AZ titles mostly, which run longer than English for the same meaning due to agglutination) after the fix above surfaced them as genuinely exceeding ~65 rendered characters. Trims removed redundant words already implied elsewhere in the title (e.g. dropping a repeated "PDF" in "PDF'den JPG'ye Dönüştürücü – PDF Sayfalarını..." → "...– Sayfaları...") rather than cutting meaning — verified afterward that no trim introduced a new duplicate-title collision (full `tests/seo.test.ts` re-run). No title outside this specific over-length list was touched, per this phase's own "don't unnecessarily change already-good metadata" instruction.
- **Search-intent audit is a new, separate, editorial dataset (`shared/seo/intent.ts`), not folded into `SeoEntity` itself.** Every tool gets a `primary` intent + 2 `secondary`/long-tail phrasings per language (102 + 204 phrases); every page gets `primary` only (informational/navigational pages don't carry the same long-tail commercial variety). *Reason: intent-mapping is a distinct concern from the rendered title/description, explicitly not allowed to claim search volume or ranking potential (Phase 3.1 Step 16) — keeping it in its own file makes that boundary explicit and keeps `SeoEntity` itself simple.*
- **Cannibalization review: reviewed and cleared, not silently assumed clean.** The obvious candidates — format-specific converter pairs (`pdf-to-jpg` vs `pdf-to-image`, `jpg-to-pdf` vs `image-to-pdf`, `jpg-to-png` vs `png-to-jpg`) and the generic QR generator vs. the 11 content-specific QR generators — were checked and found to target genuinely distinct primary intents (a specific-format query like "pdf to jpg" is a real, distinct search from a generic "pdf to image" query, the same pattern real converter sites like Smallpdf/CloudConvert use). No primary-intent string collides with another tool's in any language (test-enforced in `tests/seo-intent.test.ts`). No copy was changed for this — the existing copy already differentiated correctly.
- **Misleading-claim re-audit: clean.** Grepped all 258 title/description strings (43 entities × 3 languages × 2 fields) for superlative/guarantee language ("best", "#1", "guaranteed", "ən yaxşı", "en iyi", "garanti", etc.) — zero matches, confirming Phase 3.1's original copy already avoided this without needing correction now.

## Keyword & Search-Intent Strategy (Phase 3.3)

- **No keyword-data API is connected in this environment.** Checked explicitly: no Google Search Console, Google Ads Keyword Planner, or similar credentialed integration exists anywhere in the codebase or environment. Every keyword/intent field in `shared/seo/keywords.ts`/`intent.ts` is qualitative editorial judgment grounded in the tool's real function, never a fabricated search-volume/CPC/difficulty/ranking number — fields for those metrics were deliberately never added (test-enforced in `tests/seo-keywords.test.ts`).
- **`shared/seo/keywords.ts` reuses `TOOL_INTENT`'s primary/secondary keywords rather than re-authoring them.** *Reason: Phase 3.2 already audited those 2 fields per tool per language; re-writing them in a new file would be exactly the "unnecessary rewriting of 3.1/3.2 metadata" this phase's own instructions forbid. Only genuinely new fields (long-tail phrasing, intent classification, commercial-intent applicability, content/related-tool opportunity, cannibalization rating) were added.*
- **Competitor research used real, live web search (QRCode Monkey, Smallpdf, iLovePDF, TinyPNG, iLoveIMG)** — qualitative findings only (page-title patterns, feature emphasis, blog content angles), never fabricated or inferred numeric data (no claimed traffic/rankings/volume for any competitor). Findings: (1) QR competitors lead with customization (logo/colors/shapes) and "never expires / no scan limit" trust messaging — Codivio's current tools don't yet have that functionality, so this is a *product* gap to note for later, not something to claim in SEO copy today; (2) PDF/image competitors split between action-first titles (Smallpdf: "Merge PDF: Combine PDF Files...") and brand-suffixed titles (iLovePDF: "...Online for Free | iLovePDF") — Codivio already uses the iLovePDF-style pattern, confirmed as a reasonable, already-correct choice, not changed; (3) image-compression competitors tie content to a "faster website" performance angle — adopted as `image-compress`'s `futureContentOpportunity` in `shared/seo/keywords.ts`; (4) comparison ("X vs Y") content is a common competitor blog pattern — noted as a future Phase 3.13 (Topical Authority) opportunity, not built now.
- **Cannibalization risk uses a 3-tier SAFE/WATCH/CONFLICT rating, not a binary flag.** *Reason: Step 6's own instruction ("do not declare every similar keyword a collision") — the format-specific converter pairs and the QR generator hub-vs-specific relationship are real, close-but-distinct relationships that deserve ongoing monitoring (WATCH) without being falsely treated as either fully unrelated (SAFE) or as an actual conflict requiring content changes now (CONFLICT). Zero CONFLICT-tier findings exist today — an honest result, not softened or inflated.*
- **Category-page and blog-content opportunities are documented as data only (`CATEGORY_KEYWORD_OPPORTUNITIES`, `futureContentOpportunity` per tool) — no routes or pages were created.** *Reason: explicit instruction ("do not create category routes... define the architecture, not prematurely implement future pages"); this feeds Phase 3.4 without building ahead of it.*

## Structured SEO Content Architecture (Phase 3.4)

- **`shared/seo/content.ts` is deliberately NOT re-exported through `shared/seo/index.ts`.** *Reason: nothing in the live app (public site or Admin) reads tool content blueprints yet — this phase establishes the architecture for Phase 3.5/3.6 to consume later, not a UI redesign of `ToolPage.tsx` now. Since Vite only bundles what's actually imported from a reachable entry point, keeping `content.ts` un-barreled means its ~34×3 content blocks cost the production bundle exactly 0 bytes today (verified: the build hash and size were byte-identical to Phase 3.3's, before this file existed) — a concrete, measured answer to this phase's own performance-impact question rather than a guess.*
- **`getContentBlueprint(slug, lang)` combines `TOOL_CONTENT` with `shared/seo/keywords.ts`'s `getToolKeywordProfile`, reusing `relatedToolOpportunity`, `futureContentOpportunity`, `primaryKeyword` etc. rather than storing second copies.** *Reason: this checkpoint's own instruction against "separate sources of truth" — a related-tool list edited in `keywords.ts` (Phase 3.3) automatically stays correct here without a second edit.*
- **`howToSteps` describe the general real-world workflow, not a claim that Codivio's implementation performs it today**, paired with a single shared `TOOL_STATUS_NOTE` (one string per language, not repeated 34 times) making the "in development" status explicit wherever the how-to content is eventually rendered. Similarly, `SHARED_TRUST_MESSAGE` is one honest, sitewide privacy statement pointing to the real Privacy Policy — not 34 fabricated per-tool privacy claims about file handling that doesn't exist yet. *Reason: Step 3's explicit ban list (no "we process your file", no "automatically deleted", no "100% private") can't honestly be satisfied per-tool today; a shared, honest disclaimer is the correct alternative to either silence or fabrication.*
- **FAQ content includes exactly 2 tool-specific questions per tool** — one genuinely educational/functional question (answerable truthfully regardless of live-processing status) and one "is this free?" question (true today, consistent with Codivio's existing free-tools positioning) — rather than questions implying file-handling behavior ("is my file deleted automatically?") that would require fabricating an answer.
- **Comparison-article content opportunities live at the category level (`CATEGORY_CONTENT_BLUEPRINT.supportingTopics`), not per-tool.** *Reason: a real, natural comparison ("PDF to Word vs PDF to Excel — which to use") is inherently between sibling tools in one category, not a property of a single tool — a per-tool "comparison opportunity" field would either duplicate the category-level one 34 times or force an artificial 1:1 framing that doesn't match how comparison content actually gets written.*
- **Introductions and value propositions are new, distinct on-page content — not a copy of the Phase 3.1 meta title/description.** Test-enforced (all 34×3 introductions and all 34×3 value propositions are pairwise unique within each language) so this phase's content additions can never silently collapse into Phase 3.1's already-audited metadata.

## AI Discoverability / GEO Foundation (Phase 3.5)

- **`shared/seo/ai.ts` is an orchestration/answer layer, not a new content dataset.** Every fact and every "answer" function reads from Phase 3.1's `TOOL_SEO`, Phase 3.3's `TOOL_KEYWORDS`, and Phase 3.4's `TOOL_CONTENT`/`CATEGORY_CONTENT_BLUEPRINT` — nothing was re-authored. *Reason: this checkpoint's own instruction to reuse rather than duplicate, and because the underlying facts (tool count, categories, descriptions) were already audited in earlier phases — re-authoring them here would risk silent drift between two "sources of truth" for the same fact.*
- **`ai.ts` is deliberately NOT re-exported through `shared/seo/index.ts`**, matching the Phase 3.4 pattern for `content.ts` — nothing in the live app reads it yet, so it costs the production bundle 0 bytes (measured: the build hash and size came back byte-identical to Phase 3.3/3.4's, `422.57 kB / 124.58 kB gzip`).
- **`SITE_IDENTITY.toolCount` is computed as `Object.keys(TOOL_SEO).length`, never hardcoded to `34`.** *Reason: a future tool addition/removal should never require remembering to update a second hardcoded count — the fact stays correct by construction. `verifiedSocialProfiles` is an empty array, not omitted or filled with a placeholder, since no social profile exists anywhere in the codebase to verify.*
- **The "answer" functions (`answerWhatIsCodivio`, `answerWhatIsTool`, etc.) are plain deterministic string builders, not a chatbot or AI API.** They compute the same sentence a human editor would type by hand into a static FAQ, just derived from structured data instead of copy-pasted — no model call, no external service, no new dependency.
- **Fixed a real, pre-existing bug in `robots.txt`, found during this phase's mandated audit (not a new feature): the `Disallow: /admin/` and `Disallow: /api/private/` lines applied only to the last-declared `User-agent` group.** Per the robots.txt spec, a named `User-agent` group follows only its own directives — there is no fallback to `User-agent: *`. Since the file's last group was `User-agent: OAI-AdsBot` (see next point), the disallow rules never actually reached `User-agent: *`, meaning `/admin/` was not actually disallowed for Googlebot, Bingbot, or any other general crawler via robots.txt. Fixed by repeating the two Disallow lines inside every group. This was never a real security exposure — admin routes are protected by application authentication/authorization regardless of robots.txt (CLAUDE.md §9) — but it was a real SEO-hygiene gap, now closed and test-enforced.
- **`OAI-AdsBot` (present in the file since an earlier phase) is not a real, documented OpenAI crawler token** — verified via live web search against OpenAI's own documented crawlers (`GPTBot` for training, `OAI-SearchBot` for search/answer retrieval, `ChatGPT-User` for user-triggered fetches). Removed and replaced with the real, verified set: `OAI-SearchBot`, `ChatGPT-User` (OpenAI), `Claude-SearchBot`, `Claude-User` (Anthropic), `PerplexityBot` (Perplexity) — all Class B (AI answer/retrieval), matching this phase's explicit discoverability goal. Training-only crawlers (`GPTBot`, `Google-Extended`, `ClaudeBot`, `CCBot`) are intentionally left unlisted, falling under the general `User-agent: *` allow — a deliberate non-decision (Codivio has not made a business/legal decision to opt content out of third-party AI training), documented rather than silently defaulted; see "New Findings for Future Phases" in the Phase 3.5 handoff report.
- **`public/llms.txt` was updated, not left as-is** — the previous version was missing language support, the real public-page list, the real tool list, and any status disclosure, all explicitly requested by this phase's brief. Every fact added (languages, page list, 34-tool/4-category list, "in development" status) is independently verifiable from the existing codebase; no founding date/user count/awards/testimonials were added. The file explicitly states it does not affect Google ranking and does not guarantee AI visibility, per this phase's anti-overclaiming requirement.
- **No Schema.org, no internal-linking engine, no Search Console/GA4 integration, no external AI API, and no LLM dependency were added** — all explicitly out of scope for this phase and deferred to their designated future phases (3.6, 3.7, 3.12).

## Internal Linking Architecture (Phase 3.6)

- **`shared/seo/internal-links.ts` is a thin route-safety + anchor-text layer over existing relationship data, not a new relationship graph.** `getRelatedToolLinks`/`getCategoryToolLinks` read directly from Phase 3.3's `TOOL_KEYWORDS.relatedToolOpportunity` (via Phase 3.5's `ai.ts` query functions) — audited to have zero self-links, zero duplicates, and zero invalid slugs before this phase built on top of it, so no correction was needed. *Reason: this checkpoint's own instruction against a competing/duplicate relationship dataset.*
- **`isLinkableRoute(path)` is the single gate every link path in the module passes through**, computed from the real `PAGE_SEO`/`TOOL_SEO` registries rather than a hardcoded route list — it can never return `true` for `/admin`, `/api`, or a nonexistent tool slug/route by construction, not by convention. This directly satisfies the "admin/API routes must never be surfaced" requirement as a testable guarantee rather than a documentation promise.
- **Category breadcrumb segments are non-linkable (`path: null`), not a fabricated route.** No `/tools/category/*`-style route exists yet (confirmed: `src/App.tsx` has no such route) — inventing one here would violate this phase's explicit "do not create category routes prematurely" instruction. The category label is rendered as plain breadcrumb text; the day a real category route exists, only `getToolBreadcrumb`'s one line needs to change.
- **Future content opportunities (`getFutureContentOpportunities`) always carry `path: null, isLinkable: false`** — reusing Phase 3.3's `futureContentOpportunity` string verbatim rather than inventing a fake `/blog/...` URL, since no blog exists yet.
- **No rendering change was made to `ToolPage.tsx`/`App.tsx` in this phase.** This checkpoint's own brief explicitly allows (but doesn't require) minimal rendering changes "if necessary" — none were judged necessary, since every acceptance criterion is about relationship-data correctness, not rendered UI, and wiring this into the actual tool page would be a larger, separate content-rendering task (consistent with how Phase 3.4's `content.ts` and Phase 3.5's `ai.ts` were also built as pure data without UI wiring). `Homepage`'s existing curated tool selection (`HOMEPAGE_FEATURED_TOOLS`, a filtered view over the registry) was reviewed and confirmed unchanged/still curated — not touched.
- **`internal-links.ts` is not re-exported through `shared/seo/index.ts`**, matching the Phase 3.4/3.5 pattern — measured 0-byte bundle impact (production build hash/size byte-identical to Phase 3.3/3.4/3.5's).
- **No new translation strings were added.** Site-level anchor text (`getToolsIndexLink`, `getHomeLink`) reuses the existing `shared/i18n` dictionaries (`site.navTools`, `site.navHome`); tool anchor text reuses Phase 3.5's `getAiToolProfile(slug, lang).name` (the already-correctly-localized short tool name) — no fourth language, no duplicated translation dataset.

## Schema.org / Structured Data (Phase 3.7)

- **`shared/seo/schema.ts` builds JSON-LD from already-audited data only** — `PAGE_SEO`/`TOOL_SEO` (Phase 3.1), `SITE_IDENTITY` (Phase 3.5), and `getToolBreadcrumb` (Phase 3.6). No new content dataset, no second list of 34 tools.
- **Tool pages use plain `WebPage`, not `WebApplication`/`SoftwareApplication`.** Verified via current (2026) Google documentation: `SoftwareApplication`'s rich-result eligibility requires `offers.price` and either `aggregateRating` or `review`. Codivio's 34 tools are "coming soon" placeholders with no real pricing or ratings — satisfying those properties honestly is impossible today, and fabricating them is explicitly forbidden. `WebPage` avoids implying a working, installable, or ratable application that doesn't exist yet. Revisit once a tool has real functionality (see New Findings in the Phase 3.7 handoff report).
- **`FAQPage` schema: DEFERRED / NOT APPLICABLE, not merely skipped.** Verified via current (2026) search: Google fully retired FAQ rich results in Search as of May 7, 2026, for every site, including previously-favored authoritative government/health sites — nobody is eligible anymore. The `FAQPage` vocabulary itself isn't "dead," but produces zero search-appearance benefit today. Implementing it now would add ongoing maintenance surface (keeping schema Q&A in sync with the real, visible FAQ content) for a verified-zero SEO return. The FAQ page's real, visible Q&A content (Phase 1) is untouched and still useful to users and to AI answer engines reading the rendered page directly.
- **`Article` schema: DEFERRED / NOT APPLICABLE.** `/blog` is confirmed index-only — no `/blog/:slug` route exists, every post preview links back to `/blog` itself. There is no real per-article URL/entity for `Article` schema to describe honestly. Revisit when Blog CMS (CLAUDE.md Phase 8) ships real per-post pages.
- **`WebSite` has no `SearchAction`.** The homepage's search input filters the tool list client-side with no query-navigable URL (no `/search?q=...`-style route exists) — `SearchAction` describes a capability Codivio doesn't have.
- **`BreadcrumbList`'s un-routed category segment is dropped from the schema entirely, not represented as a linkless `ListItem`.** Only segments with a real URL become a `ListItem`, plus the current page (name only, no `item`, the standard way to represent "you are here"). Inventing a `/tools/category/*` URL, or emitting a spec-ambiguous middle item with no URL, would both misrepresent real site structure to search engines — Phase 3.6 already established no category route exists, and this phase does not create one.
- **Real, measured bundle-size finding, not hidden**: wiring `schema.ts` into real page rendering (this phase's own explicit mandate — "structured data must be present in public HTML to be useful") required importing `shared/seo/schema.ts` from `src/App.tsx` for the first time. `schema.ts` imports Phase 3.6's `getToolBreadcrumb`, which internally calls Phase 3.5's `getAiToolProfile`, which internally calls Phase 3.4's `getContentBlueprint` — pulling the *entire* `TOOL_CONTENT` dataset (34 tools × 3 languages × introduction/benefits/how-to/use-cases/FAQ, ~1,400 lines) into the client bundle for the first time, even though schema/breadcrumb generation only ever reads a tool's short name and one-line description. Measured result: client JS grew from `422.57 kB / 124.58 kB gzip` (Phase 3.3–3.6 baseline) to **`527.65 kB / 149.79 kB gzip`** — a real +105 kB raw / +25.21 kB gzip (~20%) increase, confirmed by re-running the build. Vite's own build output now warns "Some chunks are larger than 500 kB after minification." **This was not fixed in Phase 3.7**: doing so would mean modifying Phase 3.4/3.5/3.6 files (adding a lightweight name/category lookup that bypasses the full content blueprint), which this phase's own instructions treat as work to preserve, not rework, without a discovered defect forcing the change — this is an architecture-efficiency finding, not a correctness bug. Documented as a New Finding assigned to Phase 3.8 (Technical SEO + Performance), the next phase and the correct owner of bundle-splitting/lazy-loading work.
- **JSON-LD is rendered via `document.createElement("script") + .textContent`, never `innerHTML`/`dangerouslySetInnerHTML`.** `.textContent` on an already-attached DOM element writes the text node directly without HTML re-parsing, so a literal `</script>` inside a JSON string value cannot prematurely close the tag; `<` is additionally escaped to `<` as a cheap defense-in-depth layer on top of that guarantee. The script tag is removed entirely (not left stale) whenever a page passes no `schemaGraph`, and is never emitted on `/admin/*` routes regardless of what's passed, mirroring `computeDefaultRobots`'s existing admin exclusion.
- **Schema was wired into 10 call sites**: the homepage plus all 9 static pages (via one shared `buildStandardPageGraph(pageKey, lang)`, keyed to the right Schema.org subtype — `CollectionPage` for `/tools`, `AboutPage` for `/about`, `ContactPage` for `/contact`, generic `WebPage` elsewhere) and all 34 tool pages (via `buildToolPageGraph(slug, lang)`, `ToolRouteContent` in `src/App.tsx`). CMS-driven dynamic pages (`/​:slug`) and the 404 page were deliberately left without schema this phase — smaller, less-common surface, kept out of scope to avoid unnecessary rendering-code growth beyond what this phase's audit list actually named.

## Technical SEO + Performance (Phase 3.8)

- **Fixed the Phase 3.7 bundle regression by adding `getToolDisplayName(slug, lang)` to `ai.ts`** — a lightweight lookup reading only `TOOL_SEO` (already loaded), used by `internal-links.ts#getToolBreadcrumb` and `schema.ts#buildToolPageGraph` in place of the heavy `getAiToolProfile` (which pulls Phase 3.4's entire `TOOL_CONTENT` dataset via `getContentBlueprint`). `getAiToolProfile` itself is unchanged and still available for genuine "full AI profile" consumers — it now calls `getToolDisplayName` internally too, so the name-extraction logic exists in exactly one place. **Result, measured**: client JS `527.65 kB / 149.79 kB gzip` → `424.97 kB / 125.43 kB gzip` — recovering all but +2.40 kB raw / +0.85 kB gzip of the Phase 3.7 regression, a fully justified residual cost for schema.ts + the lightweight parts of `ai.ts`/`internal-links.ts` now being genuinely reachable. Verified two ways: source-level (no production file imports `getAiToolProfile` anymore) and empirically (grepped the actual built `dist/client/assets/*.js` for distinctive `TOOL_CONTENT`-only strings — absent; grepped for real Phase 3.1 tool titles and `application/ld+json` — present, proving schema still works).
- **`buildToolPageGraph`'s JSON-LD `description` now sources `TOOL_SEO`'s own meta description directly, not Phase 3.4's `introduction` field via `getAiToolProfile`.** *Reason: keeps the whole tool-page schema path (name + description + breadcrumb) inside the lightweight `TOOL_SEO`/`TOOL_KEYWORDS` dependency graph, and keeps the JSON-LD description consistent with what's already in `<meta name="description">` for the same page — one fact, one source, rather than two independently-worded descriptions of the same tool.*
- **JSON-LD's CSP-compatibility was verified, not assumed**: confirmed via research that `Content-Security-Policy`'s `script-src` directive only restricts script elements with an executable (JavaScript) MIME type — `<script type="application/ld+json">` is inert data and is not subject to `script-src`/`'unsafe-inline'` restrictions in any current browser. Cross-checked against the actual deployed CSP (`worker/security-headers.ts`, unchanged, still `script-src 'self'` with no `unsafe-inline`) — no CSP loosening was needed or made.
- **Real, verified finding, deliberately not fixed this phase: the CSR (client-side-rendering-only) architecture means per-page title/description/canonical/OG/JSON-LD are only set after JavaScript executes (via `useEffect` in `useSeo.ts`).** Google's crawler executes JavaScript and should see the correct per-page data after rendering, but crawlers with a more limited or absent JS-execution budget — including several of the AI answer-engine crawlers Phase 3.5 explicitly tried to welcome (`ChatGPT-User`, `Claude-User`, and potentially `PerplexityBot`/`Claude-SearchBot` depending on their current rendering behavior) — may see only `index.html`'s static homepage-oriented fallback for every page, not the real per-page content. This is a real architectural limitation, not a bug in any single phase's work. **Not fixed now**: introducing SSR/SSG/prerendering would be a genuine architecture change (Cloudflare Workers + Vite currently serve a pure SPA) requiring a Change Control review (CLAUDE.md §19), not a "technical SEO audit" fix — documented as a New Finding rather than attempted here, especially since all 34 tool pages are still "coming soon" placeholders, which lowers the current real-world stakes of this gap until tools actually ship.
- **Real, verified finding, deliberately not fixed this phase: the Codivio logo PNG (`public/assets/branding/codivio-logo.png`, ~1.58 MB) is used simultaneously as the favicon, the default Open Graph/Twitter image, and the 34×34px header/footer/Admin logo.** The browser downloads the full 1.58 MB regardless of the small CSS-rendered size. **Not fixed now**: this phase's own instructions explicitly forbid replacing or altering the official logo's visual identity, and no image-processing tool/library exists in this environment to safely recompress or resize it without either violating that constraint or guessing at pixel data. A proper fix means either a build-time image-optimization step or a separately-designed, correctly-sized favicon/OG-image asset — both real, but out of a code-agent's safe reach without either new tooling or a design decision. Documented as a New Finding.
- **No new dependency, no CSP change, no security-header change, no route/auth change made this phase** — the fix is entirely a refactor of which existing function three already-existing files call.

## E-E-A-T + Trust (Phase 3.9)

- **The prompt's claim of a "known verified" Instagram/Facebook identity ("Codivio.online") was independently checked, not taken on trust, and found unverifiable.** Searched the entire repository (no match) and ran live web searches for a matching Instagram/Facebook account (no match — the searches returned only unrelated accounts with similar names). Per this phase's own explicit rule ("if exact URLs are not verified, do not invent URLs") and Phase 3.5's standing `SITE_IDENTITY.verifiedSocialProfiles: []` finding, **no `sameAs` was added to Organization schema and no social link was added to the footer.** This is flagged transparently rather than silently complied with or silently ignored — see the Phase 3.9 handoff report's findings section.
- **Fixed one real overclaim found during the tool-page trust audit**: `ToolPage.tsx`'s trust row said "Fast in-browser tools" — an operational performance/architecture claim for a tool that has zero implementation (every one of the 34 tools still shows only the "Tool coming soon" placeholder). Changed to "In-browser by design," which states the real, verifiable thing (the intended architecture) without claiming anything currently works or is fast. "Privacy-focused" (the sibling trust badge) was left unchanged — it's a design-philosophy statement already consistent with the Privacy Policy's own hedged wording, not a specific unverified technical claim.
- **The Homepage hero's "Fast tools" trust-row item was reviewed but deliberately left unchanged.** Unlike ToolPage's claim (which specifically paired "fast" with "in-browser tools," asserting a processing characteristic), the homepage's claim is reasonably read as being about the site's own browsing experience (page loads, verified fast and lightweight per Phase 3.8's bundle work) rather than about unimplemented tool processing. Given "do not rewrite unnecessarily" and that Homepage content wasn't in this phase's named audit list (About/Contact/Privacy/Terms + footer/global signals), this was left as a documented, lower-severity observation rather than an in-scope fix.
- **The Contact page's non-functional contact form was reviewed and left unchanged.** It already honestly discloses itself ("The contact form is currently a UI placeholder. Backend submission will be added in a later phase.") — not deceptive. No real, verified Codivio support email exists anywhere in the repository (checked via grep for `@codivio.online`/`mailto:`/`support@`/`contact@`/etc. — zero matches), so per this phase's explicit "do not invent one" instruction, none was added. The Privacy Policy's "For privacy questions, please use the Contact page" line is technically accurate (the page exists) but points to a form that cannot currently deliver a message — documented as a real, practical trust gap rather than fixed by fabricating a contact method.
- **No author/publication-date system was built for the Blog.** Confirmed `/blog` is still index-only (no per-post routes, no author/date fields on `blogPosts`) — building a real author-attribution system requires actual editorial identities that don't exist yet. Documented as belonging to Phase 3.13 (Content Expansion), not implemented speculatively here.
- **No testimonials, reviews, ratings, user counts, or case studies were added anywhere** — none exist today, and Codivio has no mechanism yet to collect genuine ones. Documented as a future opportunity, contingent on the platform actually having real users/tools to draw from.
- **No worker/D1/auth/RBAC/CSP/security-header file was touched this phase** — confirmed via `git diff --stat` showing zero changes under `worker/` or `src/admin/`.

## Competitor SEO Gap Analysis (Phase 3.10)

- **Research-only checkpoint — no production code changed.** Live research (WebFetch/WebSearch) was used against real competitor pages (QRCode Monkey confirmed reachable and inspected directly; qr-generator.ai's homepage returned HTTP 403 to direct fetch, so it was researched via search-engine-cached summaries instead — flagged as lower-confidence evidence, not silently treated as equal to a direct inspection) plus one PDF (Smallpdf) and one image (iLoveIMG) tool page as adjacent/benchmark competitors.
- **Two real, verified Codivio advantages surfaced by direct inspection, not assumed**: QRCode Monkey's `robots.txt` is bare-minimum (`User-agent: *` / `Allow: /` only, no AI-crawler classification, no sitemap line) and it has no `llms.txt` at all (confirmed 404) — Codivio's Phase 3.5 AI-crawler-classified `robots.txt` and maintained `llms.txt` are ahead of this specific direct competitor on that one dimension. This is not generalized into "Codivio's AI/GEO strategy beats the market" — it's one data point against one competitor, reported as such.
- **No competitor text, titles, descriptions, code, or design was copied anywhere** — research was used only to identify structural patterns (e.g., Smallpdf's tool pages render step-by-step instructions + FAQ + related-tool links + breadcrumbs directly on the page, all real and rendered; Codivio has the equivalent *data* prepared since Phase 3.4/3.6 but does not yet render any of it on `ToolPage.tsx`) — this DATA-READY-vs-RENDERED gap is the single most actionable finding of this phase, and is assigned to Phase 3.13 (Content Expansion) rather than implemented here, per this phase's explicit "research only" scope.
- **No fabricated competitor metrics were used anywhere** — where a competitor page displayed its own trust claims (e.g., Smallpdf's "4.8/5 — 705,045 votes" and "trusted by over 1 billion users since 2013"), these are reported as *observed competitor claims*, not verified facts, and were never treated as a benchmark Codivio should match or copy — Codivio has no mechanism to produce a comparable genuine number today (Phase 3.9's own finding, reconfirmed here as still accurate).
- **No roadmap phase was reordered, merged, or invented.** Every prioritized gap in the Phase 3.10 handoff report is mapped either to an existing 3.11–3.15 phase or explicitly marked FUTURE / CHANGE CONTROL when no existing phase fits (e.g., a genuine dynamic-QR-code capability, which several competitors offer and Codivio's registry doesn't currently include at all).

## Backlink / Off-Page SEO Strategy (Phase 3.11)

- **Research/strategy-only checkpoint — no production code changed, no external action taken.** No outreach sent, no accounts created, no directories submitted to, no external content published.
- **Verified, not assumed: Codivio currently has zero discoverable backlinks or brand mentions anywhere.** A direct search for "codivio.online" and a negated self-site search both returned no matching results at all (the search engine substituted an unrelated, similarly-named product, "Codiva.io") — confirming Codivio has no external web footprint yet. This is an honest baseline, not a failure to search hard enough — consistent with a pre-launch product with no live tool functionality.
- **The only concrete, real off-page opportunity found this phase requiring no further verification is a live ecosystem of "best free QR code generator" roundup/comparison articles** (e.g. Bitly's blog, TxtImpact, the-qrcode-generator.com, and others found via search) that actively test and list real, working QR tools. This is a genuine Category B (resource-page) / Category A (editorial) opportunity — but explicitly gated as **READY AFTER TOOL LAUNCH**, since these roundups test actual functionality and Codivio's QR tools are still "coming soon." Reaching out before real functionality exists would be premature and could damage credibility rather than build it.
- **No backlink was claimed, no DA/DR/traffic/ranking was invented anywhere** — every metric in the Phase 3.11 handoff report is either a real, sourced observation (e.g., Product Hunt's and AlternativeTo's real, verified submission requirements) or explicitly marked `UNKNOWN — NOT VERIFIED`.
- **No roadmap phase was reordered or invented.** Every prioritized off-page opportunity is mapped to an existing 3.12–3.15 phase or explicitly marked FUTURE / CHANGE CONTROL.

## Production Deployment (2026-09-13)

- **`assets.run_worker_first: true` was added to `wrangler.jsonc`.** *Reason: discovered live, not locally — Cloudflare's Workers-with-Assets platform serves a matching static asset directly from its edge cache without invoking the Worker's `fetch()` unless this is set, which meant `worker/security-headers.ts`'s headers never reached real HTML page responses (only `/api/*` had them). This is a documented, deliberate cost (every static asset request now runs through the Worker first, a small added latency) accepted specifically to make the security-headers guarantee actually true in production, not just in the code.*
- **`MAX_FAILED_ATTEMPTS` was raised from 5 to 25 (5×) for normal admin login use, with the same 15-minute rolling window and rate-limit mechanism kept unchanged.** *Reason: explicit user request after live use showed 5 was too easy to hit during normal admin work; still bounded, still fails closed above the threshold — not a removal of brute-force protection, a recalibration of it.*
- **A one-off production password reset used a local, ephemeral, uncommitted script that replicates `hashPassword()`'s exact algorithm, rather than any change to the application.** *Reason: needed to apply a real PBKDF2-SHA256 hash directly to one row in production D1 without inventing a new admin-facing "reset password" feature under time pressure; the script was deleted immediately after use and never committed. If password reset becomes a recurring need, it should become a real, tested Admin feature — not repeat this one-off pattern.*

## SEO Content Expansion + Topical Authority (Phase 3.13)

- **`src/pages/ToolPage.tsx` is now lazy-loaded (`React.lazy` + `Suspense`) instead of statically imported by `src/App.tsx`.** *Reason: it now renders Phase 3.4's full content blueprint (intro/benefits/how-to/use-cases/FAQ, ~1,400 lines across 34 tools × 3 languages) via `shared/seo/content.ts#getContentBlueprint`. Without code-splitting, that dataset would load on every route (home, admin, static pages), regressing the client bundle back toward the Phase 3.7 size Phase 3.8 fixed. Splitting it into its own chunk keeps it out of every other route's bundle, loading only when a user actually visits `/tools/:slug`.*
- **`getAiToolProfile`, `getAiCategoryProfile`, `answerWhatIsTool` (and their `AiToolProfile`/`AiCategoryProfile` types) were moved from `shared/seo/ai.ts` to `shared/seo/content.ts`.** *Reason: measured, not theorized — introducing the lazy `ToolPage.tsx` chunk alone did not keep the main bundle small (it measured 527.55 kB, matching the original Phase 3.7 regression), because `ai.ts` (imported eagerly by every page via `schema.ts`/`internal-links.ts`) still had a static import edge into `content.ts` for these three functions, even though nothing in the eager path called them. The bundler was not reliably eliminating that edge across the dynamic-import boundary. Moving the functions themselves into `content.ts` (removing the edge from `ai.ts` entirely, not just leaving it unused) fixed it: main chunk measured 438.05 kB / 128.20 kB gzip afterward — close to the Phase 3.8 baseline (424.97 kB / 125.42 kB gzip), with the lazy `ToolPage` chunk (92.88 kB / 22.66 kB gzip) carrying the content dataset instead. Behavior of all three functions is unchanged; only their file location and import path changed (`tests/seo-ai.test.ts` updated accordingly).*
- **The visible tool-page breadcrumb reuses the exact same `getToolBreadcrumb` call `shared/seo/schema.ts#buildToolPageGraph` already used for JSON-LD** — one data source, so the visible trail and the structured data can never disagree. The category segment is rendered as plain, non-linkable text (no category route exists), matching `internal-links.ts`'s own established rule.
- **No FAQPage schema was added for the newly-visible FAQ content** — Phase 3.7 explicitly deferred FAQPage structured data after Google narrowed FAQ rich-result eligibility; visible FAQ text is content for users/crawlers, not a rich-result manipulation attempt. Verified via a regression test asserting `shared/seo/schema.ts` contains no `FAQPage` type.
- **Category display labels (breadcrumb's category segment, and the pre-existing tool-page eyebrow) remain English-only** — this is a pre-existing, already-shipped limitation (the `ToolCategory` type itself is English-only, used as both a TypeScript value and a display label since Phase 2), not something introduced or newly fixed this phase. Rendering the breadcrumb visibly surfaces it in a new place, but doesn't create it; a real fix would mean adding a category-label translation layer, out of this phase's scope. See PROJECT_STATE.md's Phase 3.13 findings.

## Free Tool → Premium Monetization Funnel (Phase 3.14)

- **No `user_entitlements` or `subscriptions` table was created.** *Reason: Codivio has no public/customer account system today — the `users` table (migration 0001) is the ADMIN account table (Super Admin/Admin/Editor/Analyst), not a customer identity. A table describing "which plan a user has" cannot mean anything real without a customer identity to attach it to; creating one now would be schema with no way to be genuinely populated. Documented as a HIGH-importance finding for a future phase — real per-user entitlements need a customer-account system first, which does not exist and was not built here.*
- **No `usage_events` table was created.** *Reason: no tool has real processing yet, so there is no real usage event this table could legitimately hold. `shared/monetization/usage.ts` defines the shape a future usage record would have and a pure `evaluateQuota()` comparison, but nothing constructs or records an event.*
- **`plans` and `plan_entitlements` D1 tables WERE created** (migration 0007), seeded with the same FREE/PRO/BUSINESS/API catalog `shared/monetization/plans.ts`/`entitlements.ts` define in code, pricing columns NULL. *Reason: unlike per-user entitlements, plan/entitlement catalog data is business-configurable content with no customer-identity dependency — the same category of thing `tools`/`pages`/`settings` already model in D1. No admin UI reads/writes these tables yet (deliberately, per "do not build an enormous admin system") — the schema exists ahead of its UI, matching this project's own `seo_settings`-table precedent from Phase 0.*
- **`getAiToolProfile`/`getAiCategoryProfile`/`answerWhatIsTool` are unaffected by this phase; the monetization module lives entirely in a new, separate `shared/monetization/` directory**, not inside `shared/seo/`, to keep business/plan logic and SEO/content logic architecturally distinct.
- **`canUseFeature()` (the central access-decision function) currently returns `FEATURE_UNAVAILABLE` for every entitlement, for every plan, always** — not a bug, the honest and only correct result while every entitlement's catalog status is `NOT_AVAILABLE`/`PLANNED`. Verified by a test that calls it with the highest plan and `authenticated: true` for all 12 entitlements and asserts none returns `ALLOWED`.
- **No self-upgrade attack surface exists to secure yet** — grepped the entire codebase and confirmed no request handler, query-param reader, cookie reader, or localStorage reader anywhere reads a client-supplied "plan" value. There is no live caller of `canUseFeature()` outside tests, so there is currently nothing for a malicious `plan=pro` value to reach.
- **`/pricing` was added as a real, informational static page** (10th static page, alongside the existing 9) — reuses the existing `PAGE_SEO`/`shared/seo/schema.ts` architecture exactly like About/Contact (plain `WebPage` JSON-LD, `index,follow`), with no `Product`/`Offer`/`AggregateRating` schema added (verified by a regression test). Every plan card shows real, defined data only (name, description, entitlement list with honest status labels) and a disabled "Coming soon" button — never a real or simulated purchase flow.
- **No admin UI was built for plan/entitlement management this phase** — the existing `/admin/monetization` "Coming soon" placeholder (Phase 2.13) is unchanged and remains accurate; a real CRUD editor is deferred to a future checkpoint (Phase 12, per CLAUDE.md's own roadmap, or a dedicated future Admin checkpoint).

## Phase 3.14 Remediation — Findings #1-#4 (2026-09-15)

- **Checked 5 newly-added reference documents** (`CODIVIO_MASTER_PLAN.md`, `ARCHITECTURE.md`, `SEO_STRATEGY.md`, `DATABASE_SCHEMA.md`, `DEPLOYMENT.md`) before making any change, per instruction. **No conflict found** between them and actual code/tests/Git/production state — they read as a formalized restatement of `CLAUDE.md`/`PROJECT_STATE.md`, and `DATABASE_SCHEMA.md` §12 independently confirms the same "don't create all monetization tables, entitlement ≠ billing" reasoning already applied in migration 0007. The only staleness found (`CODIVIO_MASTER_PLAN.md` still listing "Phase 3.14 — CURRENT" rather than PASS) is expected — these documents explicitly defer to `PROJECT_STATE.md` as the live source of truth, which is correctly updated instead.
- **`customer_accounts` table created** (migration 0008) — the smallest possible customer-identity anchor (`id`, `email` UNIQUE, `status`, timestamps only). *Reason: closes the exact gap Phase 3.14's own handoff flagged (no customer identity exists to attach a real plan to) without building a premature account system. No `password_hash`/profile fields (no registration/login mechanism exists yet — building one is a separate, larger, unscoped task); no `plan_key` column or `customer_plan_assignments` table yet (same reasoning already applied to `subscriptions`/`usage_events` in migration 0007 — a linking table with no way to be genuinely populated is not a foundation). No foreign key to/from `users`/`sessions`/`audit_logs` — verified structurally and by test that `worker/auth.ts`/`worker/rbac.ts` never reference it.*
- **`/pricing` fully localized** — moved every user-visible string (headings, plan descriptions, entitlement labels, status labels, button, FAQ) into `shared/i18n`'s existing `Translations` type/dictionaries under a new `pricing` namespace. *Reason: explicit Finding #2. Plan/entitlement key sets are hardcoded literal unions in `shared/i18n/types.ts` (matching `PLAN_KEYS`/`ENTITLEMENT_KEYS` exactly, verified by a parity test) rather than importing from `shared/monetization`, keeping `shared/i18n` dependency-free — consistent with its role as the most foundational shared module. Plan identifiers (Free/Pro/Business/API) intentionally stay untranslated in every language, per instruction.*
- **`shared/monetization/funnel-events.ts` added as a type-only foundation** (`FUNNEL_EVENT_NAMES`: `pricing_view`/`upgrade_cta_view`/`upgrade_cta_click`/`plan_selected`) — no firing/recording function, no UI wiring. *Reason: explicit Finding #3, and grepped confirmation that no Worker endpoint writes to the existing `analytics_events` table yet — wiring a real event now would mean inventing a second, throwaway analytics path. Verified by a test that none of these event names is fired anywhere in `src/App.tsx`.*
- **`tsconfig.test.json` created (Option B) rather than adding `tests` to `tsconfig.json` directly.** *Reason: `npm run build` runs `tsc -b` against `tsconfig.json` directly — adding `tests` there would make any test-file type error also fail the production build, which the remediation explicitly forbids ("do not break the existing build"). A separate config, wired to a new `npm run typecheck:tests` script, decouples the two completely.*
- **`@types/node` added as a devDependency** (dev-only, type-declarations-only, zero runtime/bundle impact) — required for `tsconfig.test.json` to actually pass; test files use real `node:fs`/`node:sqlite`/`node:path`/`node:url` imports that had no type declarations anywhere in the project before this.
- **Two small, genuine, pre-existing type errors fixed in `tests/seo-schema.test.ts`** (an unused, incorrectly-narrowed variable removed; a type cast routed through `unknown` first, exactly as TypeScript's own error message suggested) — both surfaced only once test files were actually type-checked for the first time; neither changes any test's behavior or assertions.
- **No subscriptions, billing, or payment code was added anywhere in this remediation**, consistent with its own explicit scope limit.

## Image Upload Standard — WebP Policy (Phase 3.15 Change Control, documented ahead of Phase 6/8/Media)

Standing policy only — **no upload system, image-processing library, or R2 binding exists anywhere in this codebase today** (verified: `wrangler.jsonc` has no `r2_buckets` entry; no `multipart`/`FormData`/upload-handling code exists in `worker/`; no image-processing dependency exists in `package.json`). This section records the mandatory requirements for the *first* real image-upload workflow, whenever one is actually built (Phase 6 Image Tools, Phase 8 Blog CMS, or an Admin Media module) — nothing here is implemented yet, and none of it applies to the existing static `public/assets/branding/codivio-logo.png` (a checked-in repo asset, never "uploaded" through any workflow).

- **Newly uploaded raster images must be converted/stored as WebP whenever technically supported**, preserving transparency/alpha channel. *Reason: explicit product requirement; WebP gives a real, measurable size/performance benefit over PNG/JPG for the same visual quality.*
- **Actual file content must be validated via magic bytes, not file extension or client-declared MIME type alone**, before any conversion is attempted. *Reason: extension/MIME-type spoofing is a well-known upload-validation bypass; this is the same "validate at the boundary, never trust client input" principle CLAUDE.md §9 already states for every other upload category (PDF/file tools).*
- **File-size and image-dimension limits must be enforced** before processing, not only after. *Reason: prevents a single oversized upload from being an expensive, uncontrolled processing/storage cost.*
- **Original alt text must be preserved through conversion** — a format change is not an excuse to drop or regenerate accessibility metadata. *Reason: matches this project's existing zero-violation alt-text discipline (verified Phase 3.15 audit: all 4 existing `<img>` tags have real alt text).*
- **Existing image URLs must not break**, and **existing originals must not be automatically deleted** when this policy is eventually implemented. *Reason: a format-conversion feature should be additive/reversible by default; silent deletion of the only copy of an asset is an unrecoverable action CLAUDE.md's "prefer reversible steps" principle forbids without explicit approval.*
- **Migrating already-existing assets to WebP is explicitly a separate, future, separately-approved task** — implementing the upload-time policy above does not itself trigger any retroactive migration. *Reason: a bulk migration of live assets is a higher-risk, harder-to-reverse action than validating new uploads going forward; the two should never be bundled into one change.*
- **No upload endpoint, image-processing library, or WebP-conversion code was written in Phase 3.15** — writing that code now, with no real caller/route to attach it to, would be dead code with nothing exercising it. *Reason: CLAUDE.md §2 ("no unrelated functionality," smallest useful implementation) and the redesign-skill's "no half-finished implementations" rule both apply directly; this policy exists so the requirement isn't lost or reinvented differently when Phase 6/8/Media actually starts.*

## Admin SEO Google Preview + Length Guidance (Phase 3.15-A)

- **`AdminSeoPage.tsx` gained a Google-style desktop + mobile search-result mockup and title/description length counters, reusing the existing `shared/seo/duplicates.ts#DEFAULT_LENGTH_THRESHOLDS` guidance rather than a second, invented set of numbers.** *Reason: the page already computed duplicate/missing/length-outlier findings against those exact thresholds (Phase 3.1) — a second hardcoded 15/65/50/165 set anywhere else would be a silent second source of truth for the same guidance.*
- **No D1 migration, no override/save system, no favicon/OG-image upload, no slug/sitelink editor were built this sub-phase** — this was explicitly a preview/guidance-only UI pass; the page remained read-only at the end of 3.15-A.
- **`classifyLength` was added to `shared/seo/duplicates.ts` (not a new file)** as the one shared short/good/long classifier for both the existing outlier-detection logic and the new per-field color-coded counters, so the two can never disagree about the same value at a boundary (test-enforced: `classifyLength` treats the exact boundary as "good," matching `findLengthOutliers`'s own strict `</>` semantics).

## SEO RBAC Integration — seo.view / seo.manage (Phase 3.15-B)

- **`seo.view`/`seo.manage` were added to `shared/rbac.ts#PERMISSIONS` and granted to `admin`+`editor` (never `analyst`)** — the exact same pairing convention already used for `pages.*`/`tools.*`/`faq.*`. *Reason: consistency with the existing matrix rather than inventing a new grant pattern for one module.*
- **`AdminSeoPage.tsx` needed an inline `hasPermission()` component gate, not just a hidden nav item.** *Reason: unlike Pages/Tools/FAQ (which fetch from an already-protected API), the page had zero backend calls at the time — hiding the nav link alone would leave the page fully viewable via direct URL navigation for an authenticated-but-unauthorized role. The gate reuses `AdminSettingsPage.tsx`'s existing `useAdminUser()`+`hasPermission()` pattern rather than inventing a new one, and is placed after every hook call (Rules of Hooks compliance, test-enforced).*
- **`seo.manage` was defined and granted in this sub-phase ahead of having any enforcing code path** — at the time, no SEO-editing endpoint existed yet. *Reason: matches the project's own precedent of defining a permission or schema row ahead of its consumer (e.g. `plans`/`plan_entitlements` in Phase 3.14) rather than adding the permission and the endpoint in the same breath. `seo.manage` gained a real enforcing path one sub-phase later — see Phase 3.15-C below.*

## SEO Override Architecture (Phase 3.15-C)

- **A dedicated `seo_overrides` table (migration 0010) was chosen over reusing `pages.meta_title`/`meta_description` or `tools.seo_title`/`seo_description`.** *Reason, verified by direct inspection, not assumed: the `pages` table structurally cannot represent the 9 built-in static pages — `shared/pages.ts#RESERVED_PAGE_SLUGS` deliberately prevents a CMS page from shadowing them. `tools.seo_title`/`seo_description` (migration 0006) are single-language and were already confirmed disconnected from public rendering — `worker/seo-rewrite.ts` never reads them. Reconnecting either would mean either dropping multilingual support or repurposing columns whose current meaning nothing consumes; a new table with the right shape from the start was the smaller, safer change.*
- **Schema shape (`entity_type`, `entity_key`, `language`, `title`, `description`, `status`) mirrors `site_faqs` (migration 0009) directly** — one row per `(entity_type, entity_key, language)`, enforced by a unique index (`idx_seo_overrides_entity_lang`), the same row-per-language pattern already proven for the FAQ system rather than per-field language columns. `entity_type` is `"page" | "tool"`, discriminating the same way `site_faqs.scope` does. No DB-level `CHECK` constraint on `entity_type`/`status` — the same deliberate deferral already applied to `users.role`/`pages.status`/`tools.status` (SQLite/D1 cannot add a `CHECK` to an existing column without a full table rebuild); `shared/seo-overrides.ts`'s fail-closed validation is the real enforcement.
- **Compile-time fallback is the guaranteed floor, by construction, not by convention.** `worker/seo-overrides.ts#fetchActiveOverride` deliberately does not catch its own errors (a clean contract); its sole caller, `worker/seo-rewrite.ts#injectStaticSeoMetadata`, wraps the call in try/catch, so a missing row, an `inactive` row, and any D1 failure are all indistinguishable from "no override exists" and always fall back to the untouched compile-time `PAGE_SEO`/`TOOL_SEO` default. *Reason: guarantees the already-shipped, already-tested public rendering behavior can never regress because of this addition — the new table can only add a value, never take one away or crash a page render.*
- **RBAC and audit logging reuse existing infrastructure exactly**: `seo.view` gates the 2 read endpoints, `seo.manage` gates create/update/delete (the enforcing path `seo.manage` was defined ahead of in Phase 3.15-B); `SEO_OVERRIDE_CREATED`/`SEO_OVERRIDE_UPDATED`/`SEO_OVERRIDE_DELETED` were added to `worker/audit.ts`'s existing closed action vocabulary, same fail-open audit policy as every other module. No new auth mechanism, no new middleware.
- **No caching layer (Cache API/KV) was added for the new D1 lookup this sub-phase**, even though it is a real, measured cost increase (one D1 read per static/tool page view where previously there were zero). *Reason: this task's own scope explicitly excluded new infrastructure; the lookup is indexed and only runs for already-known routes (never 404s or `/api/*`). Documented as a real, deliberate deferral — a future optimization once real traffic patterns are observable — not an oversight.*
- **Known, accepted scope limitation: an override changes only title/description (and their OG/Twitter mirrors) — never the injected H1/intro paragraph (`resolveCriticalContent`) or the JSON-LD `name`/`description` (`resolveJsonLdGraph`), both of which always render the raw compile-time default regardless of an active override.** *Reason: the task's own brief scoped this to "SEO title... SEO description" specifically; extending the same override to H1/JSON-LD would be a larger, separately-justified change. Accepted as a real, visible inconsistency risk if an admin edits a title without realizing the on-page H1 won't follow — documented here and in `worker/seo-rewrite.ts`'s own comments, not silently designed around.*
- **The override's effect on actual rewritten HTML cannot be exercised by any runtime test in this environment** — `injectStaticSeoMetadata` no-ops immediately whenever the real `HTMLRewriter` global is absent, which is unconditionally true in this project's Vitest/Node setup (the same pre-existing limitation already documented for the Phase 3.15 HTMLRewriter prototype). Mitigated by testing `fetchActiveOverride` (the exact function the rendering path calls) directly and exhaustively, plus the full CRUD/RBAC surface — not by claiming a runtime test proves something it structurally cannot.
- **`tools.seo_title`/`seo_description`'s disconnected status was reconfirmed, not touched.** *Reason: fixing or removing that pre-existing dead field is a separate cleanup decision, out of this sub-phase's scope — noted here so it isn't mistaken for something this change silently addressed.*

## Phase 3 Closure (2026-09-17)

- **Phase 3 was closed with three known, real findings explicitly deferred as separate, out-of-phase tasks rather than blocking closure**: `www.codivio.online` HTTP 525 (a Cloudflare dashboard/DNS configuration item with no representation in this repository — no commit can fix it), the Contact page's non-functional contact channel (requires a real business decision — a real email/phone address — which fabricating would violate CLAUDE.md's anti-fabrication rule), and the pre-existing D1-outage error-handling gap in `worker/route-guard.ts#isPublishedCmsSlug`/`worker/pages.ts` (a future hardening task, found during the Phase 3.15 Production Readiness Audit, not introduced by any Phase 3.15 change). *Reason: none of the three is a defect in Phase 3's actual subject matter (SEO/AI discoverability) — two are infrastructure/business items this environment cannot resolve, and the third is a pre-existing architectural characteristic unrelated to what Phase 3.15 built. Holding Phase 3 open indefinitely for items outside its own scope would block Phase 4 for reasons Phase 3 work cannot fix. This is an explicit user decision, not a unilateral judgment call.*
- **Migration 0010 was applied to production before deploying the code that depends on it**, per `DEPLOYMENT.md` §7.1's documented sequence and the Phase 3.15 Production Readiness Audit's own recommendation (LOW finding: deploying the code first would have left the Admin SEO override UI showing an empty/error state, not a crash, until the migration followed — avoided entirely by ordering it first).

## Shared QR Engine — First Runtime Dependency (Phase 4.1)

- **`qrcode` (npm, MIT license) was added as the project's first runtime dependency beyond React/react-router/lucide-react.** *Reason: QR encoding (Reed-Solomon error correction, mode/version/mask selection) is a real algorithm, not something to safely hand-roll for a production-quality module — a hand-rolled encoder risks subtly-wrong output that renders like a QR code but fails to scan. Verified before adding: `npm view qrcode license version time.modified` → MIT, version 1.5.4, last published 2025-11-13 (actively maintained); `@types/qrcode` exists on the registry for typed usage. `npm install` reported 0 vulnerabilities.*
- **All QR generation happens client-side, in the browser, with zero network calls.** *Reason: matches CLAUDE.md's local/browser-side-processing and privacy-preservation preference — no payload is ever sent to or logged by the server; the Worker never sees QR content at all.*
- **The engine is split into a pure `shared/qr/` module (types + validation, framework-agnostic) and a browser-only `src/lib/qr-engine.ts` wrapper (the only file importing `qrcode`)**, mirroring the existing `shared/seo/` vs. frontend-only-lib pattern. *Reason: `shared/` is imported by both the Worker and the frontend, but the Worker runtime has no Canvas/browser rendering API — keeping the `qrcode` import confined to one frontend-only file means the Worker can never accidentally pull in a rendering path it cannot execute, the same boundary discipline already applied to every other `shared/*` module.*
- **The Worker does not import `src/lib/qr-engine.ts` and has no QR endpoint.** *Reason: nothing server-side needs to generate a QR code today (no dynamic/trackable QR feature exists yet, and building one now would be speculative); the pure `shared/qr/` validation module remains available to a Worker endpoint if one is ever justified, without needing to restructure this boundary.*
- **Phase 4.1 scope is a generic text/URL payload only (`QrTextPayload`) — no vCard/WiFi/Email/SMS/WhatsApp/Phone/Location/Calendar structured payload builders, no logo embedding, no download/export UI, and no wiring into `ToolPage.tsx` or any tool page.** *Reason: this sub-phase builds only the shared foundation; per-tool payload builders and UI are Phase 4.2+ work and would be premature here. The `QrEncodingConfig`/`generateQrCode` API is intentionally extensible (configurable error-correction level, size, margin, colors, and a tagged `{format, data}` return shape supporting both PNG data-URL and SVG output) so later phases can build on it without a breaking change.*
- **Measured, not estimated, bundle impact: 0 KB added to the shipped client bundle at the end of Phase 4.1** (confirmed by grepping the built `dist/client/assets/*.js` for `"qrcode"` — zero matches), because nothing in the running app imports `src/lib/qr-engine.ts` yet. *Reason: recorded here so the real bundle cost is measured, not guessed, once a future phase actually wires a tool page to call it.*

## vCard QR Code — Contact Field Scope (Phase 4.5)

- **vCard 3.0 (RFC 2426) was used, not vCard 4.0 or MECARD.** *Reason: vCard 3.0 is the format most broadly recognized by Android/iOS "Add Contact" QR scanning today, matching the same standards-compatibility bar set by Phase 4.4's WiFi format choice.*
- **No postal address (`ADR`) field was implemented.** *Reason: `ADR` is a complex 7-component structured property (PO box/extended address/street/city/region/postal code/country), each with its own escaping surface — out of proportion to the core "digital business card" use case when phone/email/website/organization/job title already cover it. Revisit only if explicitly requested as its own scoped addition.*
- **Phone/email/website fields are validated for length only, never format.** *Reason: avoids over-validating legitimate international phone numbers/emails/websites — a rejected-but-legitimate format would be a worse outcome than accepting a slightly malformed value on a non-critical, optional business-card field.*
- **The `encodedValue` pattern (introduced Phase 4.4 for WiFi) was reused unchanged for vCard, requiring zero changes to `src/lib/qr-engine.ts`.** *Reason: confirms the pattern generalizes to any future structured payload kind — a precedent worth preserving for Phase 4.7's export-module design and any future QR variant.*

## QR Code Scanner — Dependency and Permissions-Policy (Phase 4.6)

- **`jsqr` (npm, Apache-2.0) was added over `qr-scanner` and the native `BarcodeDetector` API.** *Reason: `qr-scanner` was last published in 2022 and pulls in an extra type dependency; `BarcodeDetector` has inconsistent browser support (absent in Safari/Firefox) which would have produced inconsistent scanning behavior across browsers. `jsqr` is a pure decoder with zero sub-dependencies, actively maintained, and lets the app own the camera/canvas plumbing directly — consistent with the existing "pure decode function, app owns the browser API surface" pattern already used for `qrcode` (encoding).*
- **`Permissions-Policy`'s `camera` directive was changed from `camera=()` to `camera=(self)`** — the first loosening of this header since it was set in Phase 2.11. *Reason: the original blanket denial (set before any feature needed `camera`) blocked the site's own same-origin page from ever requesting camera access, which would have silently broken the Phase 4.6 scanner in production. `(self)` is the minimum necessary widening — same-origin only, no third-party/iframe camera access granted — verified safe because no `<iframe>` or third-party embed exists anywhere in the codebase. `microphone`, `geolocation`, and `payment` remain `()` since nothing uses them. This is the same "loosen one directive, only when a real integration genuinely needs it" precedent already established for the CSP in Phase 2.11.*
- **Decoded QR content is classified (Link/Wi-Fi network/Contact card/etc.) via a pure, prefix-only regex match (`shared/qr/classify.ts`), never a full per-format parser.** *Reason: the Master Plan's own Phase 4.6 scope only requires safely displaying decoded content, not fully parsing every possible payload type — a full parser per format would be premature scope expansion with no current use case.*

## QR Export — Format Scope and Architecture (Phase 4.7)

- **PNG, JPG, and SVG export were implemented; PDF and EPS were not.** *Reason: PDF generation would require a new dependency and overlaps with Phase 5 (PDF Tools)'s own reserved scope — building it here would pre-empt that phase's own architecture decisions. EPS has no reliable browser-side encoding path (no native API, no commonly-used lightweight library) — documented as unsupported rather than faked.*
- **Logo embedding was not implemented.** *Reason: compositing a logo over a QR code safely requires careful error-correction-level enforcement to preserve scannability, canvas image compositing, and a new security-review surface for arbitrary image input — disproportionate to this phase's "smallest production-quality scope." Documented as a future enhancement, not silently dropped.*
- **Export re-runs `generateQrCode` on demand at download-click time, rather than pre-rendering and storing every format.** *Reason: only the format used for the live on-screen preview needs to exist continuously; generating JPG/SVG only when their download button is actually clicked avoids wasted work and guarantees the downloaded file always matches the current preview's payload/config exactly, since both come from the same `buildPayload()`/config values.*
- **JPG export reuses the existing `qrcode` dependency's `toDataURL(..., {type:"image/jpeg"})` — no new dependency was added.** *Reason: the real browser build (`qrcode`'s `lib/browser.js`, per its own `package.json` `browser` field) delegates directly to the native `HTMLCanvasElement#toDataURL(type)` API, which has supported JPEG encoding natively for over a decade — verified by reading `node_modules/qrcode/lib/renderer/canvas.js`. Node's own server-side renderer (`lib/index.js`, what this project's Vitest suite exercises) has no JPEG support and silently substitutes PNG instead — a real, documented divergence between the test environment and the actual shipped browser code path, not a defect in either.*

## QR Validation/Security Hardening (Phase 4.8)

- **Encoding-time capacity failures (a payload within `MAX_QR_PAYLOAD_LENGTH` but too much data for the selected error-correction level) are caught in `src/lib/qr-engine.ts` and converted to a `QrGenerationError` with a new `payload_exceeds_capacity` code, rather than adding a second, duplicate capacity-estimation layer.** *Reason: empirically confirmed reachable (a 2,000-character payload fails at error-correction level "H") — a real robustness gap, not a theoretical one. The `qrcode` library is already the authoritative source of real QR capacity math; re-implementing that math in `shared/qr/` would risk it drifting out of sync with the library's own version/mode tables for no real benefit, when routing the library's own error through the app's existing clean error path is both smaller and more correct.*
- **The QR Scanner's camera capture requests an `ideal` (not unconstrained, and not a hard `min`/`max`/`exact`) resolution, and downscales captured frames to the same `MAX_CANVAS_DIMENSION` cap already used by the image-upload path.** *Reason: defense-in-depth against unbounded per-frame client-side processing cost on a high-resolution camera device. No server-side abuse surface exists for QR features (confirmed: no `/api/qr*` route), so this is a client-side resource-hygiene measure only, not a security boundary — no rate limiting/CAPTCHA/backend infrastructure was added, consistent with the project's free, browser-side-processing-first architecture.*
- **No artificial rate limiting, CAPTCHA, authentication, or backend abuse-control infrastructure was added anywhere in QR tooling.** *Reason: CLAUDE.md's own no-paid-service/smallest-useful-implementation principles, plus a confirmed absence of any server-side QR route to abuse — adding backend controls for a purely client-side feature would be unjustified infrastructure for a theoretical risk, not a real one.*

## Process

- **Real statistics only; never fake/random production statistics.** Every usage/traffic/revenue number shown anywhere must come from real event/database data once that infrastructure exists. *Reason: explicit trust requirement — see CLAUDE.md §2 and §23.*
- **20% milestone requires a comprehensive audit and PASS before advancing.** Enforced via the `codivio-test-gate` skill. *Reason: prevent silent drift across phase boundaries.*
- **Testing philosophy: one appropriate test per topic; add another only when failure/uncertainty requires it.** *Reason: avoid redundant, token-costly re-testing of unrelated or already-verified areas.*
- **No commit/push before required tests/checkpoint approval**, and never without the user's explicit go-ahead regardless. *Reason: CLAUDE.md §18/§19; the user controls when work lands in Git history.*
